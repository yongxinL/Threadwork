
can you please read the article Harness engineering: leveraging Codex in an agent-first world (https://openai.com/index/harness-engineering/) , and let me know what are the core mental models (or question and resolutions) shared by the domain experts?  please review the Threadwork project and let me know what's we have implement the prevent those questions/issues and what's we need to improve
Let me fetch the article and explore the Threadwork codebase in parallel.

Web Fetchhttps://openai.com/index/harness-engineering/
OUT
Request failed with status code 403
Agent:Explore Threadwork architecture
IN
I need a comprehensive understanding of the Threadwork project's architecture and capabilities. This is a Claude Code harness/extension project. Please explore:

1. The overall architecture - what is Threadwork, how does it work as a harness around Claude Code?
2. Context management - how does it manage context windows, token budgets, progressive disclosure?
3. Quality gates / Ralph Loop - how does it ensure output quality?
4. Tool design - what tools/commands does it expose, how are they designed?
5. Testing strategy - how are things tested?
6. Prompt engineering patterns - how are agent templates structured?
7. Session management - handoffs, recovery, state persistence
8. Spec system - how specs work, injection, routing
9. Any other harness-level patterns (hooks, entropy collection, store, etc.)

Please read key files including:
- package.json for overview
- docs/architecture.md if it exists
- CLAUDE.md files
- lib/ directory files (quality-gate.js, spec-engine.js, token-tracker.js, entropy-collector.js, store.js, state.js, handoff.js)
- hooks/ directory files
- templates/ directory structure
- Key test files for understanding behavior

Give me a detailed summary of each system and how they interconnect.
Bash Find markdown documentation files
IN
find /Users/george.li/Documents/nexora/Threadwork -type f -name "*.md" | grep -E "(README|CLAUDE|architecture)" | head -20

OUT
/Users/george.li/Documents/nexora/Threadwork/node_modules/is-extendable/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/extend-shallow/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/argparse/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/js-yaml/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/esprima/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/kind-of/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/strip-bom-string/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/sprintf-js/README.md
/Users/george.li/Documents/nexora/Threadwork/node_modules/section-matter/README.md
/Users/george.li/Documents/nexora/Threadwork/docs/architecture.md
/Users/george.li/Documents/nexora/Threadwork/README.md
Bash List all files in hooks directory
IN
ls -la /Users/george.li/Documents/nexora/Threadwork/hooks/

OUT
total 104
drwxr-xr-x@  7 george.li  staff   224 Mar 12 11:19 .
drwxr-xr-x@ 20 george.li  staff   640 Mar 12 11:24 ..
-rw-r--r--@  1 george.li  staff  9108 Mar 12 11:19 post-tool-use.js
-rw-r--r--@  1 george.li  staff  9012 Mar 12 11:19 pre-tool-use.js
-rw-r--r--@  1 george.li  staff  6542 Mar 12 11:19 session-start.js
-rw-r--r--@  1 george.li  staff  9497 Mar 12 11:19 subagent-stop.js
-rw-r--r--@  1 george.li  staff  4679 Mar  3 10:05 test-harness.js
Read token-tracker.js
Read subagent-stop.js
Read store.js
Bash Find template files to understand command and agent structure
IN
find /Users/george.li/Documents/nexora/Threadwork/templates -type f -name "*.md" | head -10

OUT
/Users/george.li/Documents/nexora/Threadwork/templates/specs/frontend/react-patterns.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/frontend/styling.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/backend/api-design.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/backend/auth.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/testing/testing-standards.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/index.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-spec-writer.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-debugger.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-executor.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-verifier.md
Bash List integration test files
IN
ls -la /Users/george.li/Documents/nexora/Threadwork/tests/integration/ | head -20

OUT
total 128
drwxr-xr-x@ 12 george.li  staff   384 Mar 11 11:59 .
drwxr-xr-x@  4 george.li  staff   128 Mar  3 10:05 ..
-rw-r--r--@  1 george.li  staff  3135 Mar 11 11:59 entropy-collector.test.js
-rw-r--r--@  1 george.li  staff  4330 Mar  3 10:05 handoff.test.js
-rw-r--r--@  1 george.li  staff  5831 Mar  3 10:05 hooks.test.js
-rw-r--r--@  1 george.li  staff  5035 Mar  3 10:05 install.test.js
-rw-r--r--@  1 george.li  staff  3934 Mar 11 11:59 progressive-disclosure.test.js
-rw-r--r--@  1 george.li  staff  3766 Mar 11 11:59 remediation-loop.test.js
-rw-r--r--@  1 george.li  staff  4014 Mar 11 11:59 store.test.js
-rw-r--r--@  1 george.li  staff  5082 Mar  3 10:05 token-tracker.test.js
-rw-r--r--@  1 george.li  staff  7196 Mar 11 11:59 upgrade-v030.test.js
-rw-r--r--@  1 george.li  staff  7963 Mar 11 11:59 upgrade.test.js
The direct fetch got a 403. Let me try alternative approaches to get the article content.

Web SearchOpenAI "Harness engineering" "leveraging Codex" agent-first world 2025 2026
OUT
Web search results for query: "OpenAI "Harness engineering" "leveraging Codex" agent-first world 2025 2026"

Links: [{"title":"Harness engineering: leveraging Codex in an agent-first world | OpenAI","url":"https://openai.com/index/harness-engineering/"},{"title":"OpenAI Introduces Harness Engineering: Codex Agents Power Large‑Scale Software Development - InfoQ","url":"https://www.infoq.com/news/2026/02/openai-harness-engineering-codex/"},{"title":"Harness Engineering","url":"https://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html"},{"title":"Unrolling the Codex agent loop | OpenAI","url":"https://openai.com/index/unrolling-the-codex-agent-loop/"},{"title":"Harness engineering: leveraging Codex in an agent-first world | OpenAI - Jay Taylor's notes","url":"https://jaytaylor.com/notes/node/1770842156000.html"},{"title":"How OpenAI's Codex Team Works and Leverages AI","url":"https://newsletter.eng-leadership.com/p/how-openais-codex-team-works-and"},{"title":"Unlocking the Codex harness: how we built the App Server | OpenAI","url":"https://openai.com/index/unlocking-the-codex-harness/"},{"title":"Harness Engineering: The Complete Guide to Building Systems That Make AI Agents Actually Work (2026) | NxCode","url":"https://www.nxcode.io/resources/news/harness-engineering-complete-guide-ai-agent-codex-2026"},{"title":"Mass Programming Resistance – Harness Engineering","url":"https://mpr.crossjam.net/wp/mpr/2026/02/harness-engineering/"},{"title":"Harness engineering: leveraging Codex in an agent-first world | daily.dev","url":"https://app.daily.dev/posts/harness-engineering-leveraging-codex-in-an-agent-first-world-py6m8jwm4"}]

I found comprehensive search results about OpenAI's "Harness engineering" initiative. Here are the key findings:

## Overview

OpenAI has detailed a new internal engineering methodology called Harness engineering that leverages AI agents to drive key aspects of the software development lifecycle.

## The Experiment

The first commit to an empty repository landed in late August 2025, with the initial scaffold—repository structure, CI configuration, formatting rules, package manager setup, and application framework—generated by Codex CLI using GPT‑5. Five months later, the repository contains on the order of a million lines of code across application logic, infrastructure, tooling, documentation, and internal developer utilities, with roughly 1,500 pull requests opened and merged with a small team of just three engineers driving Codex.

## Key Principles

The system uses Codex, a suite of AI agents, to perform tasks such as writing code, generating tests, and managing observability, based on declarative prompts defined by engineers. The lack of hands-on human coding introduced a different kind of engineering work, focused on systems, scaffolding, and leverage, with the primary job of the engineering team becoming enabling the agents to do useful work.

## Architectural Approach

By enforcing invariants, not micromanaging implementations, engineers let agents ship fast without undermining the foundation, requiring Codex to parse data shapes at the boundary while building the application around a rigid architectural model where each business domain is divided into a fixed set of layers, with strictly validated dependency directions and a limited set of permissible edges.


REMINDER: You MUST include the sources above in your response to the user using markdown hyperlinks.
Web Fetchhttps://webcache.googleusercontent.com/search?q=cache:openai.com/index/harness-engineering/
Fetched from https://webcache.googleusercontent.com/search?q=cache:openai.com/index/harness-engineering/
Let me try fetching from alternative sources that covered this article in detail.

Web Fetchhttps://www.infoq.com/news/2026/02/openai-harness-engineering-codex/
Fetched from https://www.infoq.com/news/2026/02/openai-harness-engineering-codex/
Web Fetchhttps://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html
Fetched from https://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html
Web Fetchhttps://www.nxcode.io/resources/news/harness-engineering-complete-guide-ai-agent-codex-2026
Fetched from https://www.nxcode.io/resources/news/harness-engineering-complete-guide-ai-agent-codex-2026
Let me also fetch the original OpenAI article from a mirror and the newsletter that covered it in detail.

Web Fetchhttps://newsletter.eng-leadership.com/p/how-openais-codex-team-works-and
Fetched from https://newsletter.eng-leadership.com/p/how-openais-codex-team-works-and
Web Fetchhttps://jaytaylor.com/notes/node/1770842156000.html
Fetched from https://jaytaylor.com/notes/node/1770842156000.html
Excellent - I now have comprehensive data from both sides. Let me write the analysis.

Write ancient-wobbling-coral.md
160 lines
# Harness Engineering vs Threadwork: Gap Analysis

## Context

OpenAI's "Harness Engineering" article describes how their Codex team built a 1M+ line production app with zero manually-written code over 5 months. The article surfaces 9 core problems and their resolutions. Below is a systematic comparison against Threadwork's current capabilities (v0.3.0).

---

## The 9 Core Problems & Threadwork's Position

### 1. Underspecified Environments
**Problem**: Early progress lagged not from agent capability gaps but from insufficient tooling and abstractions in the repo itself.
**OpenAI Resolution**: Systems-first approach — break goals into building blocks, identify missing capabilities, make them legible and enforceable for agents.

| Threadwork Status | **Mostly Implemented** |
|---|---|
| What we have | `/tw:new-project` scaffolds PROJECT.md, REQUIREMENTS.md, ROADMAP.md. `/tw:analyze-codebase` maps brownfield repos. Spec library provides domain knowledge. Phase workflow (discuss → plan → execute → verify) structures work into building blocks. |
| Gap | No automated **capability gap detection** — when an agent struggles, Threadwork doesn't automatically identify what's missing (tool, doc, guardrail) and feed fixes back into the repo. OpenAI treats agent failure as a signal to improve the environment, not just retry. The Ralph Loop retries but doesn't systematically upgrade the environment. |

---

### 2. Human QA Bottleneck
**Problem**: As agent throughput scaled, human QA became the limiting constraint.
**OpenAI Resolution**: Made the app UI legible to Codex (Chrome DevTools Protocol, DOM snapshots, screenshots). Exposed observability data (LogQL, PromQL). Agents ran 6+ hours autonomously.

| Threadwork Status | **Partially Implemented** |
|---|---|
| What we have | Ralph Loop automates quality gates (tsc, lint, tests, build, security). Agents self-correct up to 5 retries. `/tw:verify-phase` does goal-backward verification. Entropy collector catches cross-output drift. |
| Gap | **No runtime observability integration** — Threadwork can't give agents access to logs, metrics, traces, or browser state. No ability for agents to boot the app, take screenshots, or navigate the UI. This limits QA to static analysis (lint/type/test). OpenAI's agents can reproduce bugs from telemetry data. |

---

### 3. Context Management & Information Overload
**Problem**: "One big AGENTS.md" failed. Too much guidance becomes non-guidance. Monolithic manuals rot instantly.
**OpenAI Resolution**: AGENTS.md as table of contents (~100 lines). Structured `docs/` as system of record. Mechanical enforcement via linters. "Doc-gardening" agent for staleness.

| Threadwork Status | **Well Implemented** |
|---|---|
| What we have | **Progressive Disclosure** is a first-class feature. Routing map (~150 tokens) acts as table of contents. On-demand `spec_fetch` retrieves full specs only when needed. Saves 20K-80K tokens per phase. Spec library is organized by domain with YAML frontmatter. Session-start injection is compact (~orientation + budget + top-3 store entries). |
| Gap | **No doc-gardening agent**. Threadwork's entropy collector scans code diffs but doesn't scan documentation for staleness or cross-link validation. No mechanical enforcement that docs are up-to-date. Spec proposals are confidence-gated but docs outside the spec system aren't managed. |

---

### 4. Agent Legibility vs Human Readability
**Problem**: Code optimized for humans sometimes obscured intent from agents.
**OpenAI Resolution**: Optimize for agent legibility first. Treat repo as agent's onboarding doc. Favor boring tech. Reimplement over opaque dependencies. "Anything not in-context doesn't exist."

| Threadwork Status | **Partially Addressed** |
|---|---|
| What we have | Spec system + Store make project knowledge accessible in-context. Session-start injects orientation. Handoffs are self-contained (Section 10 has full resume prompt). Everything persists to `.threadwork/` — no state in env vars or human memory. |
| Gap | Threadwork doesn't guide users on **making their codebase agent-legible**. No tooling to audit whether a repo favors agent comprehension (dependency transparency, in-repo docs completeness, opaque library detection). This is more of a consulting/guidance gap than a tooling gap. |

---

### 5. Architectural Coherence Without Manual Enforcement
**Problem**: Agent-generated code replicates and compounds existing patterns, including bad ones.
**OpenAI Resolution**: Rigid layered architecture (Types → Config → Repo → Service → Runtime → UI). Mechanical enforcement via custom linters. Enforce invariants, not implementations.

| Threadwork Status | **Partially Implemented** |
|---|---|
| What we have | Spec system can encode architectural patterns. Quality gates run linters. Entropy collector detects naming drift, import boundary violations, duplicate logic, orphaned artifacts. Specs tagged `taste_invariant: true` are enforced during entropy scans. |
| Gap | **No structural/architectural test framework**. Threadwork doesn't enforce dependency direction rules or layer boundaries mechanically. The spec system is advisory (agents can ignore specs). OpenAI uses deterministic linters that fail the build — Threadwork's architectural specs are injected as context, not enforced as hard gates. Need something like ArchUnit or custom structural tests that run as quality gates. |

---

### 6. Technical Debt / Garbage Collection
**Problem**: 20% of Fridays spent cleaning "AI slop". Manual garbage collection doesn't scale.
**OpenAI Resolution**: Encode golden principles as mechanical rules. Run background Codex tasks on regular cadence to scan deviations, update quality grades, open targeted refactoring PRs. Automerge low-risk cleanup.

| Threadwork Status | **Well Implemented** |
|---|---|
| What we have | **Entropy Collector** is exactly this — a background agent that scans wave diffs post-completion across 6 categories (naming drift, import violations, orphaned artifacts, doc staleness, inconsistent error handling, duplicate logic). Minor issues get auto-fix commits. Warning issues queue for next wave. |
| Gap | Entropy collector only runs **post-wave**, not on a regular cadence. No scheduled/periodic runs. No quality grade tracking over time. No automerge for low-risk cleanups (commits are made but PRs aren't opened/automerged). Could benefit from a **continuous garbage collection mode** that runs independently of the phase workflow. |

---

### 7. Human Judgment Encoding
**Problem**: Capturing subjective team preferences in a form agents can apply consistently.
**OpenAI Resolution**: Capture review comments as doc updates. Promote feedback into tooling. Inject custom linter error messages with remediation instructions.

| Threadwork Status | **Well Implemented** |
|---|---|
| What we have | **Spec Proposal Pipeline** with confidence escalation (0.3 → 0.6 → 0.7 → Store). Ralph Loop learning signals automatically propose specs from failures. Cross-session Store promotes patterns globally. Remediation blocks include `relevant_spec` and `fix_template`. Custom linter-like behavior via quality gate error parsing + spec matching. |
| Gap | **No automated capture from PR review comments**. Human feedback during code review doesn't automatically flow into spec proposals. The pipeline is triggered by Ralph Loop failures, not by human review patterns. Could benefit from a hook that watches for recurring review themes and proposes specs. |

---

### 8. PR Velocity vs Quality Gates
**Problem**: Conventional blocking merge gates became counterproductive at agent throughput.
**OpenAI Resolution**: Minimal blocking merge gates. Short-lived PRs. Address flakes with follow-up runs. "Corrections are cheap, waiting is expensive."

| Threadwork Status | **Conservative Approach** |
|---|---|
| What we have | Ralph Loop is a blocking gate (up to 5 retries before escalation). Quality gates cache per git SHA. Team model enables parallel execution across worktrees. |
| Gap | Threadwork is designed around **blocking quality gates**, which is the right default for most teams. However, there's no **configurable gate strictness** — you can't mark certain gates as non-blocking or set a "flake tolerance". For high-throughput teams, this could become a bottleneck. Consider adding gate policies (blocking/warning/skip) per gate type. |

---

### 9. Agent Review Bottleneck
**Problem**: Human review became throughput limitation.
**OpenAI Resolution**: Push review effort to agent-to-agent. Humans instruct Codex to review its own changes, request additional reviews, iterate until all agent reviewers satisfied.

| Threadwork Status | **Partially Implemented** |
|---|---|
| What we have | Ralph Loop is a form of self-review (quality gates). `/tw:verify-phase` is goal-backward verification. Plan-checker validates plans across 6 dimensions. Entropy collector reviews cross-output coherence. |
| Gap | **No agent-to-agent review workflow**. Threadwork doesn't have a dedicated review agent that examines PRs for semantic correctness, design quality, or specification compliance. The current review is structural (lint/type/test), not semantic. OpenAI has agents reviewing other agents' code for logic, design, and intent alignment. |

---

## Summary Scorecard

| # | Problem | Threadwork Status | Priority |
|---|---------|------------------|----------|
| 1 | Underspecified Environments | Mostly Implemented | Medium |
| 2 | Human QA Bottleneck (observability) | Partially Implemented | Low* |
| 3 | Context Management | **Well Implemented** | - |
| 4 | Agent Legibility | Partially Addressed | Low |
| 5 | Architectural Enforcement | Partially Implemented | **High** |
| 6 | Garbage Collection | Well Implemented | Medium |
| 7 | Human Judgment Encoding | Well Implemented | Medium |
| 8 | PR Velocity vs Gates | Conservative | Low |
| 9 | Agent-to-Agent Review | Partially Implemented | **High** |

*Runtime observability is powerful but requires deep integration with each user's stack — may be out of scope.

---

## Top 3 Improvement Recommendations

### 1. Structural/Architectural Test Gate (Problem #5)
Add a quality gate that enforces dependency direction rules and layer boundaries mechanically. This would:
- Allow users to define architectural rules in specs (e.g., `layer: service, cannot_import: [ui, runtime]`)
- Run as a quality gate alongside lint/type/test
- Fail the build when agents violate architectural boundaries
- This is the biggest philosophical gap: Threadwork's specs are advisory; OpenAI's constraints are mechanical

### 2. Agent-to-Agent Review Agent (Problem #9)
Add a `tw-reviewer` agent that:
- Reviews executor output for semantic correctness against requirements
- Checks design quality against specs
- Runs between executor completion and Ralph Loop quality gates
- Reduces human review burden by catching logic/design issues before PR

### 3. Doc-Gardening + Capability Gap Detection (Problems #1, #3)
Extend the entropy collector or create a new periodic agent that:
- Scans docs for staleness (referenced files that changed, outdated examples)
- When Ralph Loop hits max retries, analyzes *why* and proposes environment improvements (new tool, missing doc, needed guardrail) rather than just escalating
- Validates spec cross-links and completeness

---

## What Threadwork Does Better Than OpenAI's Approach

1. **Token Budget Transparency** — OpenAI doesn't mention token/cost tracking. Threadwork has first-class dual-mode budget tracking with variance analysis.
2. **Session Resumability** — 10-section handoffs with auto-populated decisions. OpenAI assumes long-running single sessions.
3. **Progressive Disclosure** — Threadwork's routing map + on-demand fetch is more sophisticated than OpenAI's "AGENTS.md as TOC" approach.
4. **Confidence-Gated Learning** — The spec proposal pipeline (0.3 → 0.7 → Store) is a novel pattern not described in OpenAI's article.
5. **Skill Tier System** — Adapting verbosity to user expertise. OpenAI doesn't address this.
6. **Model Switching** — Runtime complexity-aware model recommendations. OpenAI uses a single model (GPT-5/Codex).
Here's the analysis. The OpenAI article surfaces 9 core problems their team encountered building a 1M-line app with zero manual code. Here's where Threadwork stands:

What Threadwork Already Nails
Context Management (Problem #3) — Progressive Disclosure with routing maps + on-demand spec_fetch is arguably more sophisticated than OpenAI's "AGENTS.md as table of contents" approach
Garbage Collection (Problem #6) — Entropy Collector covers exactly this use case across 6 categories
Human Judgment Encoding (Problem #7) — The spec proposal pipeline with confidence escalation (0.3 → 0.7 → Store) is a novel pattern OpenAI doesn't even describe
Token budgeting, session resumability, skill tiers, model switching — areas where Threadwork goes beyond what OpenAI discusses
The 3 Key Gaps
Architectural Enforcement (High Priority) — OpenAI uses mechanical enforcement (custom linters that fail builds) for dependency rules. Threadwork's specs are advisory — agents can ignore them. The biggest philosophical gap.

Agent-to-Agent Review (High Priority) — OpenAI pushes "almost all review effort" to agent-to-agent loops. Threadwork's review is structural (lint/type/test) but lacks a semantic review agent that checks logic, design quality, and requirement alignment.

Doc-Gardening + Capability Gap Detection (Medium) — When agents fail, OpenAI treats it as a signal to improve the environment. Threadwork's Ralph Loop retries but doesn't systematically identify what's missing (tool, doc, guardrail) and upgrade the harness.

The full analysis with all 9 problems mapped is in the plan file. Would you like me to go deeper on any of these areas, or should we discuss prioritizing improvements?

Sources:

OpenAI: Harness Engineering (via mirror)
InfoQ: OpenAI Harness Engineering
Martin Fowler: Harness Engineering
NxCode: Complete Guide
Eng Leadership Newsletter
AskUserQuestion
OUT
User has answered your questions: "What would you like to do with this analysis?"="Deep-dive discussion". You can now continue with the user's answers in mind.
Great — let's go deeper. Here's what I think is most worth unpacking:

The Philosophical Divide: Advisory vs Mechanical Enforcement
This is the most interesting tension between the two systems. OpenAI's core insight is:

"By enforcing invariants, not micromanaging implementations, we let agents ship fast without undermining the foundation."

Their constraints are deterministic and blocking — a custom linter fails the build if code imports across layer boundaries. The agent has no choice but to comply.

Threadwork takes a different approach: specs are injected as context and agents are trusted to follow them. The Ralph Loop catches violations after the fact via lint/type/test gates, but architectural rules like "service layer cannot import from UI" aren't enforced mechanically.

The question is: does Threadwork need to become more mechanical, or is the advisory approach actually better for its use case?

Arguments for Threadwork's current approach:

Threadwork serves diverse codebases — it can't assume a specific architecture. OpenAI controlled the entire stack from day zero.
Mechanical enforcement requires users to define rules upfront, which is a high setup cost
The spec confidence pipeline is a softer feedback loop that learns over time
Arguments for adding mechanical enforcement:

Advisory specs create a "soft ceiling" — agents can drift without consequence
The Ralph Loop only catches what lint/type/test catch — architectural violations that don't cause build failures slip through
OpenAI's experience shows agents will compound bad patterns if you don't mechanically prevent it
The "Failure as Signal" Mental Model
This is perhaps the most underappreciated insight from the article:

When agents struggle, treat it as a signal to identify missing components (tools, guardrails, documentation) and feed fixes back into the repository.

Threadwork's Ralph Loop treats failure as "try again harder" (up to 5 retries). OpenAI treats failure as "the environment is broken — fix the harness, not the agent." These are fundamentally different philosophies:

Threadwork (Ralph Loop)	OpenAI (Harness-First)
Agent fails	Retry with remediation hints	Ask: what's missing in the environment?
After max retries	Escalate to human	Improve tooling/docs/guardrails
Learning captured in	Spec proposals (confidence 0.3)	Repo-level infrastructure changes
Feedback target	Future agent runs	The harness itself
Threadwork partially does this — Ralph Loop failures generate spec proposals. But the proposals are about patterns ("use JWT refresh rotation"), not about environment gaps ("agents need a database migration helper tool").

The Agent Review Gap
OpenAI's most radical practice is agent-to-agent review. Their flow:

Agent A writes code
Agent B reviews it (semantic, design, requirement alignment)
Agent A responds to feedback
Loop until Agent B is satisfied
Then human reviews (if needed)
Threadwork's current flow:

Agent writes code
Ralph Loop runs structural checks (lint/type/test)
If pass → done. If fail → retry with same agent.
The missing layer is a semantic review — does the code actually do what was asked? Does it align with the spec's intent? Is the design clean? These are things lint/type/test can't catch.

What OpenAI Doesn't Address (Threadwork's Advantages)
It's worth noting several areas where Threadwork is ahead:

Token Economics — OpenAI never mentions token budgets, cost tracking, or variance analysis. Their agents run "6+ hours" which implies massive token consumption with no visibility. Threadwork's budget system with 80/90/95% thresholds and per-task variance is a significant operational advantage.

Session Continuity — OpenAI assumes long-running single sessions. Threadwork's 10-section handoff with auto-populated decisions enables multi-session projects with no context loss.

Progressive Disclosure — OpenAI's "AGENTS.md as TOC" is a first step. Threadwork's routing map (~150 tokens) + on-demand spec_fetch with token tracking is a more complete solution.

Confidence-Gated Learning — The 0.3 → 0.6 → 0.7 → Store pipeline is genuinely novel. OpenAI encodes human judgment manually; Threadwork has an automated escalation path.

Multi-Model Orchestration — Threadwork assigns Opus/Sonnet/Haiku by task complexity. OpenAI uses a single model throughout.

Want me to drill into any specific area — the mechanical enforcement question, the failure-as-signal philosophy, the agent review layer, or something else entirely?

yes, about the mechanical enforcement,  even we trust the agents to follow the specs, but I believe we still need a way to enfoce the agent to follow, also we'd better to have way to validate and make sure the rule be follow by the agent.  as AI will do the coding so we need to make sure the code is following the spec,  we may need spend more in the plan stage, but this is worth.  may we can have more questions in the plan stage.
You're hitting on something important. Let me dig into the current plan-stage flow to understand exactly where enforcement could be added.

Agent:Explore planning and verification flows
IN
I need to understand Threadwork's current planning and verification stages in detail. Please explore:

1. **tw-planner agent template** (templates/agents/tw-planner.md) — how does it generate plans? What does it check against specs?
2. **tw-plan-checker agent template** (templates/agents/tw-plan-checker.md) — what 6 dimensions does it validate? How does it reference specs?
3. **tw-verifier agent template** (templates/agents/tw-verifier.md) — what does goal-backward verification look like?
4. **tw-executor agent template** (templates/agents/tw-executor.md) — how does it reference specs during execution? What's the Decision Logging Protocol?
5. **Plan phase command** (templates/commands/tw-plan-phase.md) — what's the workflow?
6. **Execute phase command** (templates/commands/tw-execute-phase.md) — how does execution reference plans?
7. **Verify phase command** (templates/commands/tw-verify-phase.md) — what verification steps?
8. **Discuss phase command** (templates/commands/tw-discuss-phase.md) — what questions are asked?
9. **lib/spec-engine.js** — look at buildRoutingMap(), buildInjectionBlock(), and how specs are matched to tasks
10. **lib/quality-gate.js** — look at runAll() and what gates exist, how they could be extended

Read these files thoroughly and give me the exact content/structure of each, especially:
- What questions are asked during planning
- What validation happens before execution starts
- How specs are referenced in plans
- What the plan-checker validates
- How the verifier checks compliance

I need verbatim details, not summaries.
Read tw-execute-phase.md
Read quality-gate.js
Now I have the full picture. Let me lay out the analysis of where enforcement breaks down and what we could do.

Where Spec Enforcement Currently Breaks Down
Looking at the pipeline end-to-end, there are 4 enforcement points today — and all of them are advisory:

Stage	Enforcement Point	Type	Problem
Plan	tw-plan-checker Dimension 6 "Spec Compliance"	Advisory	Checker flags violations but agent can ignore or misinterpret. No mechanical check.
Execute	Executor prompt: "never contradict spec library entries"	Prompt instruction	Relies on agent compliance. No verification that it actually followed the spec.
Execute	Ralph Loop quality gates	Mechanical (lint/type/test)	Only catches structural violations. "Use jose not jsonwebtoken" isn't a lint rule.
Verify	tw-verifier reads specs + decisions	Advisory	Verifier reads specs but checks requirements coverage, not spec compliance.
The fundamental issue: specs contain prose, not machine-checkable rules. A spec says "Use RS256 for JWT signing" — but nothing in the pipeline mechanically verifies that the code actually uses RS256. The planner is told to follow it, the executor is told to follow it, but nobody checks.

The Key Insight: Specs Need a Checkable Layer
Right now a spec looks like:


---
domain: backend
specId: SPEC:auth-001
name: JWT Best Practices
tags: [auth, jwt]
---
# JWT Best Practices
Use RS256 algorithm. Tokens expire in 1 hour...
This is pure prose — useful for context injection but impossible to enforce mechanically. What if specs could also define checkable rules?


---
domain: backend
specId: SPEC:auth-001
name: JWT Best Practices
tags: [auth, jwt]
rules:
  - type: grep_must_exist
    pattern: "algorithm.*RS256|RS256"
    files: "src/**/*.ts"
    message: "JWT must use RS256 algorithm"
  - type: grep_must_not_exist
    pattern: "require\\(['\"]jsonwebtoken['\"]\\)"
    files: "src/**/*.ts"
    message: "Use jose library, not jsonwebtoken"
  - type: import_boundary
    from: "src/services/**"
    cannot_import: "src/ui/**"
    message: "Service layer cannot import from UI"
---
This gives us both: prose for agent context AND rules for mechanical enforcement.

Where to Add More Rigor in the Planning Stage
You're right that investing more in planning pays off. Currently /tw:discuss-phase asks 5 questions. Here's what's missing:

Current questions (discuss-phase):

Preferred libraries/frameworks
Patterns to follow
Known constraints
Known risks
Out of scope
Missing questions that would enable enforcement:
6. Architectural boundaries — "Which layers exist? What are the allowed dependency directions?"
7. Forbidden patterns — "Are there any patterns or libraries that must NOT be used?"
8. Naming conventions — "Any naming rules? (e.g., hooks must start with use, services end with Service)"
9. Import rules — "Any import restrictions between directories/modules?"

These answers would flow directly into spec rules that can be mechanically checked.

Additionally, the plan-checker could be significantly stronger:

Currently Dimension 6 (Spec Compliance) just checks that plans "don't prescribe approaches that contradict loaded specs." But it could:

Extract the checkable rules from specs
Verify each plan task's file targets don't violate import boundaries
Verify library choices match spec rules
Generate a binding constraint list that gets injected into the executor — not as prose, but as a checklist the executor must satisfy and the Ralph Loop can verify
The Enforcement Pipeline (Proposed)

discuss-phase (enhanced: 9 questions)
    ↓ captures architectural rules, forbidden patterns, naming conventions
    ↓ auto-generates spec rules from answers

plan-phase
    ↓ planner receives specs WITH rules
    ↓ plan-checker validates Dimension 6 AGAINST rules (not just prose)
    ↓ plan-checker generates "binding constraints" per task

execute-phase
    ↓ executor receives binding constraints + routing map
    ↓ executor implements task
    ↓ NEW: spec-compliance gate runs in Ralph Loop
    ↓   - grep_must_exist / grep_must_not_exist checks
    ↓   - import_boundary checks
    ↓   - naming_pattern checks
    ↓ If fails → remediation with specific rule + fix template

verify-phase
    ↓ verifier checks requirements AND spec rule compliance
    ↓ full spec compliance report alongside requirements table
The key additions are:

Rules in specs (machine-checkable, not just prose)
Enhanced discuss-phase (captures rules from user)
Spec compliance quality gate (runs alongside lint/type/test in Ralph Loop)
Binding constraints (plan-checker → executor, not just advisory)
AskUserQuestion
OUT
User has answered your questions: "Which enforcement approach resonates most with how you want Threadwork to work?"="Both combined", "How much should we invest in the discuss-phase enhancement?"="Add enforcement + review questions". You can now continue with the user's answers in mind.
Excellent choices — maximum enforcement with rich planning. Let me write the implementation plan.

Write ancient-wobbling-coral.md
357 lines
# Mechanical Spec Enforcement for Threadwork

## Context

OpenAI's "Harness Engineering" article reveals that the biggest differentiator in their agent-driven development is **mechanical enforcement** — deterministic rules that fail the build when agents violate architectural boundaries. Currently, Threadwork's specs are advisory (prose injected as context). Agents are *told* to follow specs but nothing *verifies* they did. This plan adds:

1. A **rules engine** in specs for simple checks (grep, imports, naming)
2. A **structural test gate** for complex invariants
3. An **enhanced discuss-phase** that captures enforcement rules + review criteria from users
4. A **spec compliance quality gate** that runs in the Ralph Loop alongside lint/type/test
5. **Binding constraints** generated by plan-checker and verified post-execution

---

## Part 1: Spec Rules Engine

### 1.1 Extend spec frontmatter with `rules` field

**File**: `lib/spec-engine.js`

Add support for a `rules` array in spec YAML frontmatter. Each rule has:

```yaml
---
domain: backend
specId: SPEC:auth-001
name: JWT Best Practices
tags: [auth, jwt]
rules:
  - type: grep_must_exist
    pattern: "RS256"
    files: "src/lib/auth*.ts"
    message: "JWT must use RS256 algorithm per SPEC:auth-001"

  - type: grep_must_not_exist
    pattern: "require\\(['\"]jsonwebtoken['\"]\\)"
    files: "src/**/*.{ts,js}"
    message: "Use jose library, not jsonwebtoken (SPEC:auth-001)"

  - type: import_boundary
    from: "src/services/**"
    cannot_import: ["src/ui/**", "src/components/**"]
    message: "Service layer cannot import from UI layer (SPEC:arch-001)"

  - type: naming_pattern
    pattern: "^use[A-Z]"
    files: "src/hooks/**/*.ts"
    target: "export_names"
    message: "Hooks must start with 'use' prefix (SPEC:react-001)"

  - type: file_structure
    must_exist: ["src/services/*/index.ts", "src/services/*/types.ts"]
    message: "Services must have index.ts and types.ts (SPEC:arch-002)"
---
```

**Rule types** (start with 5, extensible):

| Type | What it checks | How |
|------|---------------|-----|
| `grep_must_exist` | Pattern MUST appear in matching files | `grep -r` with glob filter |
| `grep_must_not_exist` | Pattern must NOT appear in matching files | `grep -r` inverse |
| `import_boundary` | Files in `from` glob cannot import from `cannot_import` globs | Parse import statements |
| `naming_pattern` | Exported names in matching files must match regex | Parse exports, test regex |
| `file_structure` | Required file patterns must exist | Glob check |

### 1.2 Add `loadRules()` and `evaluateRules()` to spec-engine.js

```javascript
// Load all rules from all active specs (confidence >= 0.7 or in main spec dirs)
loadRules(phase) → Rule[]

// Evaluate rules against current working tree
// Returns: { passed: boolean, violations: Violation[] }
// Each Violation: { specId, rule, message, files: string[], evidence: string }
evaluateRules(rules, projectRoot) → RuleResult
```

**Key files**: `lib/spec-engine.js` (extend), new helper `lib/rule-evaluator.js`

---

## Part 2: Structural Test Gate

### 2.1 Convention: `.threadwork/structural-tests/` directory

Users (or the discuss-phase) can create structural test files — plain JS/TS files that export a `check()` function:

```javascript
// .threadwork/structural-tests/no-circular-deps.js
export const name = 'No Circular Dependencies';
export const specId = 'SPEC:arch-001'; // optional link to spec

export async function check(projectRoot) {
  // Run madge or custom logic
  const { execSync } = await import('child_process');
  try {
    execSync('npx madge --circular src/', { cwd: projectRoot });
    return { passed: true };
  } catch (e) {
    return {
      passed: false,
      errors: [`Circular dependencies found: ${e.stdout}`]
    };
  }
}
```

### 2.2 Add `runStructuralTests()` gate to quality-gate.js

```javascript
// Discover and run all files in .threadwork/structural-tests/
// Each must export: { name, check(projectRoot) → { passed, errors[] } }
runStructuralTests() → GateResult
```

Register as a new gate in the quality gate runner alongside typecheck/lint/tests/build/security.

**Key files**: `lib/quality-gate.js` (extend)

---

## Part 3: Spec Compliance Quality Gate

### 3.1 New gate: `runSpecCompliance()`

**File**: `lib/quality-gate.js`

Combines Part 1 (spec rules) and Part 2 (structural tests) into a single gate:

```javascript
async function runSpecCompliance(options = {}) {
  const specEngine = options.specEngine || new SpecEngine();
  const projectRoot = options.projectRoot || process.cwd();

  // 1. Load spec rules
  const rules = specEngine.loadRules();
  const ruleResult = await evaluateRules(rules, projectRoot);

  // 2. Run structural tests
  const structResult = await runStructuralTests(projectRoot);

  // 3. Merge results
  return {
    gate: 'spec-compliance',
    passed: ruleResult.passed && structResult.passed,
    violations: ruleResult.violations,
    structuralFailures: structResult.errors,
    skipped: rules.length === 0 && structResult.tests === 0,
    reason: rules.length === 0 ? 'No spec rules or structural tests defined' : undefined
  };
}
```

### 3.2 Register in `runAll()` and Ralph Loop

Add `spec-compliance` to the gate sequence. Run it **after** lint/type/test (so basic errors are caught first):

```
Gate order: typecheck → lint → tests → spec-compliance → build → security
```

In `quality-config.json`:
```json
{
  "spec-compliance": { "enabled": true, "blocking": true }
}
```

### 3.3 Extend `buildRemediationBlock()` for spec violations

When spec-compliance fails, the remediation block includes:
- The exact rule that was violated (type, pattern, message)
- The spec ID and spec name
- The file(s) where the violation was found
- A concrete fix template based on rule type

Example remediation for `grep_must_not_exist`:
```javascript
{
  primary_violation: "SPEC:auth-001 rule violated: Use jose library, not jsonwebtoken",
  relevant_spec: "SPEC:auth-001 | backend/jwt-best-practices",
  fix_template: "Replace `require('jsonwebtoken')` with `import { SignJWT } from 'jose'` in src/lib/auth.ts",
  learning_signal: "spec-rule:auth-001:grep_must_not_exist:jsonwebtoken"
}
```

**Key files**: `lib/quality-gate.js`, `hooks/subagent-stop.js`

---

## Part 4: Enhanced Discuss-Phase (9 Questions)

### 4.1 Add 4 enforcement questions to tw-discuss-phase

**File**: `templates/commands/tw-discuss-phase.md`

Current 5 questions + 4 new ones:

| # | Question | Purpose | Feeds into |
|---|----------|---------|------------|
| 1 | Preferred libraries/frameworks | Context | CONTEXT.md |
| 2 | Patterns to follow | Context | CONTEXT.md |
| 3 | Known constraints | Context | CONTEXT.md |
| 4 | Known risks | Context | CONTEXT.md |
| 5 | Out of scope | Context | CONTEXT.md |
| **6** | **Architectural boundaries** — "What layers exist and what are the allowed dependency directions?" | **Enforcement** | **Spec rules (import_boundary)** |
| **7** | **Forbidden patterns** — "Any libraries, patterns, or approaches that must NOT be used?" | **Enforcement** | **Spec rules (grep_must_not_exist)** |
| **8** | **Naming conventions** — "Any naming rules for files, exports, or variables?" | **Enforcement** | **Spec rules (naming_pattern)** |
| **9** | **Code review focus** — "What should code review focus on beyond passing tests? What are the acceptance criteria?" | **Verification** | **Verifier instructions in CONTEXT.md** |

### 4.2 Auto-generate spec rules from answers

After the user answers questions 6-8, the discuss-phase agent automatically:

1. Creates or updates a spec file (e.g., `specs/enforcement/phase-N-rules.md`) with machine-checkable rules derived from the answers
2. Example: User says "services cannot import from UI" → generates:
   ```yaml
   rules:
     - type: import_boundary
       from: "src/services/**"
       cannot_import: ["src/ui/**", "src/components/**"]
       message: "Service layer cannot import from UI layer"
   ```
3. User can review and edit the generated rules before planning begins

### 4.3 Store review criteria in CONTEXT.md

Question 9's answers go into a new `## Review Criteria` section in CONTEXT.md:

```markdown
## Review Criteria
- Error messages must be user-facing (no stack traces in responses)
- All database queries must use parameterized queries
- No hardcoded secrets or API keys
- Functions should not exceed 50 lines
```

This section is injected into the verifier agent, enabling **semantic review** against user-defined criteria.

**Key files**: `templates/commands/tw-discuss-phase.md`, `templates/agents/tw-researcher.md` (if researcher handles discuss)

---

## Part 5: Binding Constraints (Plan-Checker → Executor)

### 5.1 Plan-checker generates binding constraints

**File**: `templates/agents/tw-plan-checker.md`

Enhance Dimension 6 (Spec Compliance). After validating plans against specs, the plan-checker:

1. Extracts all applicable spec rules for each plan's file targets
2. Generates a `<constraints>` block per plan:

```xml
<plan id="PLAN-1-2">
  ...
  <constraints generated-by="plan-checker" timestamp="...">
    <constraint specId="SPEC:auth-001" rule="grep_must_exist">
      Files matching src/lib/auth*.ts MUST contain pattern "RS256"
    </constraint>
    <constraint specId="SPEC:arch-001" rule="import_boundary">
      Files in src/services/** MUST NOT import from src/ui/**
    </constraint>
  </constraints>
</plan>
```

### 5.2 Executor receives constraints at spawn

**File**: `hooks/pre-tool-use.js`

When injecting context into executor agents, include the `<constraints>` block from the plan XML. The executor sees both:
- The routing map (for on-demand spec fetching)
- The binding constraints (non-negotiable rules)

### 5.3 Verifier checks constraint compliance

**File**: `templates/agents/tw-verifier.md`

Add a new verification dimension: **Constraint Compliance**. For each plan's `<constraints>`, verify the constraint was satisfied by reading the actual code. Report as:

```markdown
## Constraint Compliance

| Constraint | Spec | Status | Evidence |
|-----------|------|--------|----------|
| auth*.ts contains RS256 | SPEC:auth-001 | PASS | src/lib/auth.ts:23 |
| services/ no UI imports | SPEC:arch-001 | PASS | grep found 0 violations |
```

---

## Part 6: Enhanced Verifier with Review Criteria

### 6.1 Inject review criteria into verifier

**File**: `templates/agents/tw-verifier.md`, `templates/commands/tw-verify-phase.md`

The verifier already checks requirements coverage and quality gates. Add two new dimensions:

1. **Spec Rule Compliance** — run `evaluateRules()` and report results
2. **Review Criteria** — check each criterion from CONTEXT.md `## Review Criteria` section against the code

The verifier produces a richer report:

```markdown
## Requirements Coverage
(existing table)

## Quality Gates
(existing)

## Spec Rule Compliance
| Rule | Spec | Status | Details |
|------|------|--------|---------|

## Review Criteria
| Criterion | Status | Evidence |
|-----------|--------|----------|
| No hardcoded secrets | PASS | grep found 0 matches for API_KEY= |
| Functions < 50 lines | PARTIAL | 2 functions exceed limit: src/services/auth.ts:processLogin (67 lines) |
```

---

## Files to Modify/Create

| File | Action | Description |
|------|--------|-------------|
| `lib/spec-engine.js` | Modify | Add `loadRules()`, rule parsing from frontmatter |
| `lib/rule-evaluator.js` | **Create** | Rule evaluation engine (grep, import, naming, file-structure checks) |
| `lib/quality-gate.js` | Modify | Add `runSpecCompliance()`, `runStructuralTests()`, extend `runAll()`, extend `buildRemediationBlock()` |
| `hooks/subagent-stop.js` | Modify | Pass specEngine to buildRemediationBlock for rule-aware remediation |
| `hooks/pre-tool-use.js` | Modify | Inject `<constraints>` from plan XML into executor prompts |
| `templates/commands/tw-discuss-phase.md` | Modify | Add questions 6-9, auto-generate spec rules from answers |
| `templates/agents/tw-plan-checker.md` | Modify | Enhanced Dimension 6 + binding constraint generation |
| `templates/agents/tw-executor.md` | Modify | Document constraint awareness, reference constraints in execution protocol |
| `templates/agents/tw-verifier.md` | Modify | Add Spec Rule Compliance + Review Criteria verification dimensions |
| `templates/commands/tw-verify-phase.md` | Modify | Load review criteria from CONTEXT.md, pass to verifier |
| `templates/specs/enforcement/` | **Create** | Starter spec templates with example rules |
| `tests/unit/rule-evaluator.test.js` | **Create** | Tests for all 5 rule types |
| `tests/unit/quality-gate-compliance.test.js` | **Create** | Tests for spec compliance gate |
| `tests/integration/spec-enforcement.test.js` | **Create** | End-to-end: spec with rules → quality gate → remediation |

---

## Verification Plan

1. **Unit tests**: Each rule type evaluator (grep_must_exist, grep_must_not_exist, import_boundary, naming_pattern, file_structure)
2. **Unit tests**: `runSpecCompliance()` gate with mock specs containing rules
3. **Unit tests**: `buildRemediationBlock()` with spec-compliance violations
4. **Integration test**: Full flow — spec with rules → executor writes violating code → Ralph Loop catches it → remediation block references the rule → executor fixes → gates pass
5. **Integration test**: Discuss-phase generates spec rules from user answers → plan-checker generates constraints → executor receives them
6. **Manual verification**: Run `/tw:discuss-phase` on a test project, answer enforcement questions, verify generated spec rules are correct
The plan covers 6 parts:

Spec Rules Engine — Add a rules array to spec frontmatter with 5 rule types (grep_must_exist, grep_must_not_exist, import_boundary, naming_pattern, file_structure)
Structural Test Gate — Convention-based .threadwork/structural-tests/ directory for complex invariants
Spec Compliance Quality Gate — New gate in Ralph Loop that combines spec rules + structural tests, with rule-aware remediation blocks
Enhanced Discuss-Phase — 9 questions (current 5 + architectural boundaries, forbidden patterns, naming conventions, review criteria). Auto-generates spec rules from answers.
Binding Constraints — Plan-checker extracts applicable rules per plan → generates <constraints> XML → injected into executor → verified by verifier
Enhanced Verifier — Two new verification dimensions: Spec Rule Compliance + Review Criteria checking
13 files to modify/create, with unit + integration tests covering the full enforcement pipeline.

Claude’s Plan ancient-wobbling-coral.md
Stayed in plan mode
harness_engineering.md
can you please create the plan file for this in docs/discuss folder
Read ancient-wobbling-coral.md
Bash Check if discussions directory exists
IN
ls /Users/george.li/Documents/nexora/Threadwork/docs/discussions/ 2>/dev/null || echo "directory does not exist"

OUT
directory does not exist
Bash List docs directory contents
IN
ls /Users/george.li/Documents/nexora/Threadwork/docs/

OUT
architecture.md
blueprint_v0.1.0.md
blueprint_v0.2.0.md
blueprint_v0.3.0.md
discuss
upgrade.md
user-guide.md
Bash List discuss directory contents
IN
ls /Users/george.li/Documents/nexora/Threadwork/docs/discuss/

OUT
harness_engineering.md
Read harness_engineering.md
I see — you already have the discussion file started. Let me write the complete analysis document there, incorporating the full article analysis, gap mapping, deep-dive discussion, and the implementation plan.

Write harness_engineering.md
363 lines
# Harness Engineering: Analysis & Threadwork Gap Assessment

> Based on OpenAI's "Harness Engineering: Leveraging Codex in an Agent-First World" and related coverage.
> Discussion date: 2026-03-19

---

## Sources

- [OpenAI: Harness Engineering (via mirror)](https://jaytaylor.com/notes/node/1770842156000.html)
- [InfoQ: OpenAI Harness Engineering](https://www.infoq.com/news/2026/02/openai-harness-engineering-codex/)
- [Martin Fowler: Harness Engineering](https://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html)
- [NxCode: Complete Guide](https://www.nxcode.io/resources/news/harness-engineering-complete-guide-ai-agent-codex-2026)
- [Eng Leadership Newsletter](https://newsletter.eng-leadership.com/p/how-openais-codex-team-works-and)

---

## Part 1: The 9 Core Problems & Threadwork's Position

OpenAI built a 1M+ line production app with zero manual code over 5 months (3 engineers, ~3.5 PRs/engineer/day). The article surfaces 9 core problems and resolutions.

### Problem 1: Underspecified Environments

**Challenge**: Early progress lagged not from agent capability gaps but insufficient tooling and abstractions in the repo.

**OpenAI Resolution**: Systems-first approach — break goals into building blocks, identify missing capabilities, make them "both legible and enforceable for the agent."

**Threadwork Status: Mostly Implemented**
- `/tw:new-project` scaffolds PROJECT.md, REQUIREMENTS.md, ROADMAP.md
- `/tw:analyze-codebase` maps brownfield repos
- Spec library provides domain knowledge
- Phase workflow (discuss -> plan -> execute -> verify) structures work

**Gap**: No automated **capability gap detection**. When an agent struggles, Threadwork doesn't automatically identify what's missing (tool, doc, guardrail) and feed fixes back into the repo. The Ralph Loop retries but doesn't systematically upgrade the environment.

---

### Problem 2: Human QA Bottleneck

**Challenge**: As agent throughput scaled, human QA became the limiting constraint.

**OpenAI Resolution**: Made the app UI legible to Codex (Chrome DevTools Protocol, DOM snapshots, screenshots). Exposed observability data (LogQL, PromQL). Agents ran 6+ hours autonomously.

**Threadwork Status: Partially Implemented**
- Ralph Loop automates quality gates (tsc, lint, tests, build, security)
- Agents self-correct up to 5 retries
- `/tw:verify-phase` does goal-backward verification
- Entropy collector catches cross-output drift

**Gap**: No runtime observability integration. Threadwork can't give agents access to logs, metrics, traces, or browser state. QA is limited to static analysis (lint/type/test). OpenAI's agents reproduce bugs from telemetry data.

---

### Problem 3: Context Management & Information Overload

**Challenge**: "One big AGENTS.md" failed. "Too much guidance becomes non-guidance. When everything is important, nothing is." Monolithic manuals "rot instantly."

**OpenAI Resolution**: AGENTS.md as table of contents (~100 lines). Structured `docs/` as system of record. Mechanical enforcement via linters. "Doc-gardening" agent for staleness.

**Threadwork Status: Well Implemented**
- **Progressive Disclosure** is first-class. Routing map (~150 tokens) acts as table of contents.
- On-demand `spec_fetch` retrieves full specs only when needed. Saves 20K-80K tokens per phase.
- Spec library organized by domain with YAML frontmatter.
- Session-start injection is compact (~orientation + budget + top-3 store entries).

**Gap**: No doc-gardening agent. Entropy collector scans code diffs but not documentation for staleness or cross-link validation. No mechanical enforcement that docs are up-to-date.

---

### Problem 4: Agent Legibility vs Human Readability

**Challenge**: Code optimized for human reading sometimes obscured intent from agents.

**OpenAI Resolution**: Optimize for agent legibility first. Treat repo as agent's onboarding doc. Favor "boring" tech. Reimplement over opaque dependencies. "Anything not in-context doesn't exist."

**Threadwork Status: Partially Addressed**
- Spec system + Store make project knowledge accessible in-context.
- Everything persists to `.threadwork/` — no state in env vars or human memory.
- Handoffs are self-contained (Section 10 has full resume prompt).

**Gap**: Threadwork doesn't guide users on making their codebase agent-legible. No tooling to audit agent comprehension factors (dependency transparency, in-repo docs completeness, opaque library detection). More of a consulting/guidance gap than a tooling gap.

---

### Problem 5: Architectural Coherence Without Manual Enforcement

**Challenge**: Agent-generated code replicates and compounds existing patterns, including bad ones.

**OpenAI Resolution**: Rigid layered architecture (Types -> Config -> Repo -> Service -> Runtime -> UI). Mechanical enforcement via custom linters. "By enforcing invariants, not micromanaging implementations, we let agents ship fast without undermining the foundation."

**Threadwork Status: Partially Implemented**
- Spec system can encode architectural patterns
- Quality gates run linters
- Entropy collector detects naming drift, import violations, duplicate logic
- Specs tagged `taste_invariant: true` enforced during entropy scans

**Gap: HIGH PRIORITY**. No structural/architectural test framework. Threadwork doesn't enforce dependency direction rules or layer boundaries mechanically. Specs are advisory — agents *can ignore them*. OpenAI uses deterministic linters that fail the build. This is the biggest philosophical gap.

---

### Problem 6: Technical Debt / Garbage Collection

**Challenge**: 20% of Fridays spent cleaning "AI slop". Manual garbage collection doesn't scale.

**OpenAI Resolution**: Encode golden principles as mechanical rules. Run background tasks on regular cadence — scan deviations, update quality grades, open targeted refactoring PRs. Automerge low-risk cleanup.

**Threadwork Status: Well Implemented**
- **Entropy Collector** is exactly this — background agent scanning wave diffs across 6 categories (naming drift, import violations, orphaned artifacts, doc staleness, inconsistent error handling, duplicate logic).
- Minor issues get auto-fix commits. Warning issues queue for next wave.

**Gap**: Entropy collector only runs post-wave, not on a regular cadence. No quality grade tracking over time. No automerge for low-risk cleanups. Could benefit from continuous garbage collection mode independent of phase workflow.

---

### Problem 7: Human Judgment Encoding

**Challenge**: Capturing subjective team preferences in a form agents can apply consistently.

**OpenAI Resolution**: Capture review comments as doc updates. Promote feedback into tooling. Inject custom linter error messages with remediation instructions.

**Threadwork Status: Well Implemented**
- **Spec Proposal Pipeline** with confidence escalation (0.3 -> 0.6 -> 0.7 -> Store)
- Ralph Loop learning signals automatically propose specs from failures
- Cross-session Store promotes patterns globally
- Remediation blocks include `relevant_spec` and `fix_template`

**Gap**: No automated capture from PR review comments. Human feedback during code review doesn't flow into spec proposals. Pipeline triggered by Ralph Loop failures, not human review patterns.

---

### Problem 8: PR Velocity vs Quality Gates

**Challenge**: Conventional blocking merge gates became counterproductive at agent throughput.

**OpenAI Resolution**: Minimal blocking merge gates. Short-lived PRs. "Corrections are cheap, waiting is expensive." Caveat: "This would be irresponsible in a low-throughput environment."

**Threadwork Status: Conservative Approach (Appropriate)**
- Ralph Loop is a blocking gate (up to 5 retries before escalation)
- Quality gates cache per git SHA
- Team model enables parallel execution

**Gap**: No configurable gate strictness. Can't mark certain gates as non-blocking or set "flake tolerance." For high-throughput teams this could become a bottleneck. Consider gate policies (blocking/warning/skip) per gate type.

---

### Problem 9: Agent Review Bottleneck

**Challenge**: Human review became throughput limitation.

**OpenAI Resolution**: Push "almost all review effort" to agent-to-agent. Humans instruct Codex to review its own changes, request additional reviews, iterate until all agent reviewers satisfied.

**Threadwork Status: Partially Implemented**
- Ralph Loop is self-review (quality gates)
- `/tw:verify-phase` is goal-backward verification
- Plan-checker validates plans across 6 dimensions
- Entropy collector reviews cross-output coherence

**Gap: HIGH PRIORITY**. No agent-to-agent review workflow. No dedicated review agent examining PRs for semantic correctness, design quality, or specification compliance. Current review is structural (lint/type/test), not semantic.

---

## Summary Scorecard

| # | Problem | Threadwork Status | Priority |
|---|---------|------------------|----------|
| 1 | Underspecified Environments | Mostly Implemented | Medium |
| 2 | Human QA Bottleneck (observability) | Partially Implemented | Low* |
| 3 | Context Management | **Well Implemented** | -- |
| 4 | Agent Legibility | Partially Addressed | Low |
| 5 | Architectural Enforcement | Partially Implemented | **High** |
| 6 | Garbage Collection | Well Implemented | Medium |
| 7 | Human Judgment Encoding | Well Implemented | Medium |
| 8 | PR Velocity vs Gates | Conservative | Low |
| 9 | Agent-to-Agent Review | Partially Implemented | **High** |

*Runtime observability requires deep integration with each user's stack — may be out of scope.

---

## Part 2: What Threadwork Does Better Than OpenAI's Approach

1. **Token Economics** — OpenAI never mentions token budgets, cost tracking, or variance analysis. Their agents run "6+ hours" with no visibility. Threadwork's budget system with 80/90/95% thresholds and per-task variance is a significant operational advantage.

2. **Session Continuity** — OpenAI assumes long-running single sessions. Threadwork's 10-section handoff with auto-populated decisions enables multi-session projects with no context loss.

3. **Progressive Disclosure** — OpenAI's "AGENTS.md as TOC" is a first step. Threadwork's routing map (~150 tokens) + on-demand spec_fetch with token tracking is more complete.

4. **Confidence-Gated Learning** — The 0.3 -> 0.6 -> 0.7 -> Store pipeline is genuinely novel. OpenAI encodes human judgment manually; Threadwork has an automated escalation path.

5. **Multi-Model Orchestration** — Threadwork assigns Opus/Sonnet/Haiku by task complexity. OpenAI uses a single model throughout.

---

## Part 3: Deep Dive — Advisory vs Mechanical Enforcement

### The Philosophical Divide

OpenAI's core insight:

> "By enforcing invariants, not micromanaging implementations, we let agents ship fast without undermining the foundation."

Their constraints are **deterministic and blocking** — a custom linter fails the build if code imports across layer boundaries. The agent has no choice but to comply.

Threadwork's approach: specs are **injected as context** and agents are trusted to follow them. The Ralph Loop catches violations *after the fact* via lint/type/test gates, but architectural rules like "service layer cannot import from UI" aren't enforced mechanically.

**Arguments for Threadwork's current approach:**
- Threadwork serves diverse codebases — can't assume a specific architecture
- Mechanical enforcement requires users to define rules upfront (high setup cost)
- The spec confidence pipeline is a softer feedback loop that learns over time

**Arguments for adding mechanical enforcement:**
- Advisory specs create a "soft ceiling" — agents can drift without consequence
- Ralph Loop only catches what lint/type/test catch — architectural violations that don't cause build failures slip through
- OpenAI's experience shows agents *will* compound bad patterns if not mechanically prevented

### Where Enforcement Currently Breaks Down

The pipeline has 4 enforcement points — all advisory:

| Stage | Enforcement Point | Type | Problem |
|---|---|---|---|
| **Plan** | plan-checker Dimension 6 "Spec Compliance" | Advisory | Checker flags violations but agent can ignore or misinterpret |
| **Execute** | Executor prompt: "never contradict spec library entries" | Prompt instruction | Relies on agent compliance. No verification. |
| **Execute** | Ralph Loop quality gates | Mechanical (lint/type/test) | Only catches structural violations. "Use jose not jsonwebtoken" isn't a lint rule. |
| **Verify** | Verifier reads specs + decisions | Advisory | Checks requirements coverage, not spec compliance. |

**The fundamental issue**: specs contain prose, not machine-checkable rules. A spec says "Use RS256 for JWT signing" but nothing mechanically verifies the code actually uses RS256.

### The "Failure as Signal" Mental Model

> When agents struggle, treat it as a signal to identify missing components (tools, guardrails, documentation) and feed fixes back into the repository.

| | Threadwork (Ralph Loop) | OpenAI (Harness-First) |
|---|---|---|
| Agent fails | Retry with remediation hints | Ask: what's missing in the environment? |
| After max retries | Escalate to human | Improve tooling/docs/guardrails |
| Learning captured in | Spec proposals (confidence 0.3) | Repo-level infrastructure changes |
| Feedback target | Future agent runs | The harness itself |

Threadwork *partially* does this via spec proposals on failure. But proposals target *patterns* ("use JWT refresh rotation"), not *environment gaps* ("agents need a database migration helper tool").

---

## Part 4: Decision — Mechanical Enforcement for Threadwork

### User Decision (2026-03-19)

> "Even though we trust agents to follow specs, we still need a way to enforce and validate that rules are being followed. Since AI does the coding, we need to make sure the code follows the spec. We should invest more in the planning stage — more questions are worth it."

### Chosen Approach: Both Combined

1. **Spec rules engine** — simple checks (grep, imports, naming) via rules in spec frontmatter
2. **Structural test framework** — complex invariants via user-defined test files
3. **Enhanced discuss-phase** — 9 questions (current 5 + enforcement + review criteria)

### Implementation Plan

See: `blueprint_v0.4.0.md` (when created)

**6 Parts:**

#### Part 1: Spec Rules Engine
Extend spec frontmatter with a `rules` array. 5 rule types:

| Type | What it checks |
|------|---------------|
| `grep_must_exist` | Pattern MUST appear in matching files |
| `grep_must_not_exist` | Pattern must NOT appear |
| `import_boundary` | Files in `from` glob cannot import from `cannot_import` globs |
| `naming_pattern` | Exported names must match regex |
| `file_structure` | Required file patterns must exist |

Example:
```yaml
rules:
  - type: import_boundary
    from: "src/services/**"
    cannot_import: ["src/ui/**", "src/components/**"]
    message: "Service layer cannot import from UI layer"
```

New files: `lib/rule-evaluator.js`. Modified: `lib/spec-engine.js` (add `loadRules()`)

#### Part 2: Structural Test Gate
Convention: `.threadwork/structural-tests/` directory with JS files exporting `check(projectRoot)`.
For complex invariants (circular deps, custom business rules).
Modified: `lib/quality-gate.js` (add `runStructuralTests()`)

#### Part 3: Spec Compliance Quality Gate
New gate `runSpecCompliance()` combining spec rules + structural tests.
Gate order: typecheck -> lint -> tests -> **spec-compliance** -> build -> security.
Runs in Ralph Loop. Remediation blocks include violated rule + spec ID + fix template.
Modified: `lib/quality-gate.js`, `hooks/subagent-stop.js`

#### Part 4: Enhanced Discuss-Phase (9 Questions)
New questions:

| # | Question | Feeds into |
|---|----------|------------|
| 6 | Architectural boundaries — layers and dependency directions | Spec rules (import_boundary) |
| 7 | Forbidden patterns — libraries/approaches that must NOT be used | Spec rules (grep_must_not_exist) |
| 8 | Naming conventions — rules for files, exports, variables | Spec rules (naming_pattern) |
| 9 | Code review focus — acceptance criteria beyond tests | Verifier instructions in CONTEXT.md |

Auto-generates spec rules from answers to Q6-8. Review criteria from Q9 stored in CONTEXT.md and injected into verifier.

#### Part 5: Binding Constraints (Plan-Checker -> Executor)
Plan-checker extracts applicable spec rules per plan's file targets.
Generates `<constraints>` XML block per plan.
Injected into executor prompts via `pre-tool-use.js`.
Verifier checks constraint compliance post-execution.

#### Part 6: Enhanced Verifier with Review Criteria
Two new verification dimensions:
1. **Spec Rule Compliance** — run `evaluateRules()` and report
2. **Review Criteria** — check each criterion from CONTEXT.md against code

### The Full Enforcement Pipeline

```
discuss-phase (9 questions)
    |-- captures architectural rules, forbidden patterns, naming conventions
    |-- auto-generates spec rules from answers
    v
plan-phase
    |-- planner receives specs WITH rules
    |-- plan-checker validates Dimension 6 AGAINST rules (not just prose)
    |-- plan-checker generates binding constraints per task
    v
execute-phase
    |-- executor receives binding constraints + routing map
    |-- executor implements task
    |-- NEW: spec-compliance gate runs in Ralph Loop
    |     - grep_must_exist / grep_must_not_exist checks
    |     - import_boundary checks
    |     - naming_pattern checks
    |-- If fails: remediation with specific rule + fix template
    v
verify-phase
    |-- verifier checks requirements AND spec rule compliance
    |-- full spec compliance report alongside requirements table
    |-- review criteria checked against code
```

### Files to Modify/Create

| File | Action | Description |
|------|--------|-------------|
| `lib/spec-engine.js` | Modify | Add `loadRules()`, rule parsing from frontmatter |
| `lib/rule-evaluator.js` | Create | Rule evaluation engine (5 rule types) |
| `lib/quality-gate.js` | Modify | Add `runSpecCompliance()`, `runStructuralTests()`, extend `runAll()` and `buildRemediationBlock()` |
| `hooks/subagent-stop.js` | Modify | Pass specEngine to buildRemediationBlock |
| `hooks/pre-tool-use.js` | Modify | Inject `<constraints>` from plan XML into executor prompts |
| `templates/commands/tw-discuss-phase.md` | Modify | Add questions 6-9, auto-generate spec rules |
| `templates/agents/tw-plan-checker.md` | Modify | Enhanced Dimension 6 + binding constraint generation |
| `templates/agents/tw-executor.md` | Modify | Document constraint awareness |
| `templates/agents/tw-verifier.md` | Modify | Add Spec Rule Compliance + Review Criteria dimensions |
| `templates/commands/tw-verify-phase.md` | Modify | Load review criteria from CONTEXT.md |
| `templates/specs/enforcement/` | Create | Starter spec templates with example rules |
| `tests/unit/rule-evaluator.test.js` | Create | Tests for all 5 rule types |
| `tests/unit/quality-gate-compliance.test.js` | Create | Tests for spec compliance gate |
| `tests/integration/spec-enforcement.test.js` | Create | End-to-end enforcement flow |
Done. The complete analysis is now at harness_engineering.md with 4 parts:

The 9 Core Problems — full gap mapping with scorecard
Threadwork's Advantages — 5 areas where Threadwork is ahead
Deep Dive — advisory vs mechanical enforcement analysis, failure-as-signal philosophy
Implementation Decision — the chosen approach (both combined + enhanced discuss-phase) with the full enforcement pipeline and file manifest
Claude’s Plan ancient-wobbling-coral.md
Stayed in plan mode
The document is ready for your review.

2026-03-19.md
Let's discuss the The "Failure as Signal" Mental Model,
This is the most philosophically interesting gap. Let me lay out why.

The Two Failure Philosophies
Right now, when a Threadwork executor fails, the Ralph Loop asks:

"How can the agent fix this?" — retry with remediation hints, up to 5 times, then escalate.

OpenAI's harness asks a fundamentally different question:

"What's broken in the environment that caused this?" — improve the tooling, docs, or guardrails so the failure can't happen again.

These seem similar but lead to very different outcomes over time:

Ralph Loop (Agent-Fix)	Harness-First (Environment-Fix)
Failure response	Give agent better hints	Add missing capability to repo
Who changes	The agent's next attempt	The harness itself
Compound effect	Same failures can recur across sessions	Each failure permanently improves the system
Where learning lives	Spec proposals (confidence 0.3)	Repo infrastructure (tools, linters, docs)
Time horizon	This session	All future sessions
Why This Matters More Than It Seems
Consider a concrete example. An executor is building an API endpoint and fails because:

It doesn't know the project uses a custom error handler
It writes throw new Error(...) instead of throw new AppError(...)
Tests fail because the error middleware expects AppError
Ralph Loop today:

Tests fail → remediation block says "Fix test failure in error-handler.test.js"
Agent retries, maybe figures out AppError exists, maybe doesn't
Next session, a different executor hits the same problem
Spec proposal created at confidence 0.3 — but it's about the pattern, not the gap
Harness-first approach:

Tests fail → system asks: why did the agent not know about AppError?
Answer: there's no spec covering the error handling pattern
Action: auto-generate a spec with the pattern + a grep_must_not_exist rule for throw new Error(
Next session, every executor knows about AppError from the routing map, and the rule prevents the mistake mechanically
The difference is where the fix lives. Ralph Loop fixes the output (this code, this session). Harness-first fixes the input (the environment, permanently).

What Threadwork Already Has (The Building Blocks)
The interesting thing is Threadwork is close. The pieces exist but aren't connected for this purpose:

Spec proposals from Ralph Loop — when gates fail, proposeSpecUpdate() creates a proposal at confidence 0.3. This is a form of environment improvement, but it's slow (needs human acceptance to reach 0.7) and targets patterns, not gaps.

Learning signals — buildRemediationBlock() generates a learning_signal fingerprint. If the same signal fires 3+ times, the proposal auto-escalates to 0.6. This is compound learning, but again — it captures "what went wrong" not "what was missing."

Decision logging — executors log non-trivial choices in <decisions> XML. These feed into handoffs. But they don't feed back into the harness.

Entropy collector — scans for cross-output drift. Could be extended to scan for recurring failure patterns.

The Missing Piece: Failure Classification
The core thing Threadwork lacks is a failure classifier — when the Ralph Loop hits a failure, it should ask why before asking how to fix it:


Failure detected
    |
    v
What type of failure is this?
    |
    |-- Code bug (agent made a mistake)
    |     --> Retry with remediation (current Ralph Loop)
    |
    |-- Knowledge gap (agent didn't know about X)
    |     --> Auto-generate spec proposal with HIGH confidence
    |     --> Add to routing map immediately for this session
    |
    |-- Missing tool/capability (agent couldn't do X)
    |     --> Log as environment gap in a new gap-report
    |     --> Surface to user in handoff: "Agent needed X but it doesn't exist"
    |
    |-- Architectural violation (agent broke a rule it wasn't told about)
    |     --> Auto-generate spec RULE (not just prose)
    |     --> This is where the new enforcement system connects
The first two categories are the most actionable. A "knowledge gap" failure — where the agent didn't know something that exists in the codebase — is a signal that a spec is missing or the routing map didn't surface the right spec. An "architectural violation" failure is a signal that a rule should be added to the enforcement system.

How This Connects to the Enforcement Plan
This is actually the missing link between the current Ralph Loop and the mechanical enforcement system we just planned. The flow would be:


Ralph Loop failure
    |
    v
Failure classifier (new)
    |
    |-- "Agent used jsonwebtoken instead of jose"
    |     Type: Knowledge gap + Architectural violation
    |     Action 1: Propose spec with grep_must_not_exist rule
    |     Action 2: Propose spec with grep_must_exist rule for jose
    |     Confidence: 0.5 (higher than current 0.3 because it's a verified gap)
    |
    v
Next Ralph Loop iteration
    |-- Agent gets remediation + the NEW rule context
    |-- Fix is more targeted
    |
    v
Session end / Handoff
    |-- Gap report: "1 knowledge gap found, 1 rule auto-proposed"
    |-- User reviews: accept → rule becomes mechanical enforcement
The key difference from today: the failure doesn't just retry — it improves the harness for all future runs.

What's your thinking on this? There are a few directions we could take:

Add failure classification to the Ralph Loop — categorize failures before retrying, so the response matches the failure type
Fast-track spec proposals from classified failures — knowledge gaps start at 0.5 instead of 0.3, with auto-generated rules
Gap reporting in handoffs — a new handoff section that surfaces environment gaps, not just task status
Connect to the enforcement pipeline — classified architectural violations auto-generate spec rules
Or we could keep this as a design discussion and fold it into the v0.4.0 blueprint alongside the enforcement work.

for option 1, 2, 3, can you please give me more details if we implement into our project? and for option 4, shall we need to restructure our design?
Let me look at the actual code paths involved to give you concrete details.

Read subagent-stop.js
Read quality-gate.js
Read handoff.js
Now I can give you concrete details grounded in the actual code. Let me also check how spec proposals work currently.

Grep "export function proposeSpecUpdate" (in /Users/george.li/Documents/nexora/Threadwork/lib/spec-engine.js)
41 lines of output
Good. Now I have the full picture. Here's the detailed analysis:

Option 1: Failure Classification in the Ralph Loop
What Changes
Right now in subagent-stop.js:153-249, when gates fail, the flow is linear:


gates fail → buildRemediationBlock() → retry with hints
We'd add a classification step between failure detection and retry. The classifier examines the error and the agent's output to determine why it failed, not just what failed.

Where It Lives
New function classifyFailure() in quality-gate.js, called from subagent-stop.js at line ~190 (right after buildRemediationBlock()).


// lib/quality-gate.js — new export
export function classifyFailure(gateResults, specEngine) {
  const classification = {
    type: null,        // 'code_bug' | 'knowledge_gap' | 'missing_capability' | 'architectural_violation'
    confidence: 0,     // 0-1 how confident the classification is
    evidence: '',      // why we classified it this way
    recommendation: '' // what to do about it
  };

  const failed = gateResults.results.filter(r => !r.passed && !r.skipped);
  const errors = failed.flatMap(r => r.errors ?? r.failures ?? []);
  const errorText = errors.join('\n');

  // 1. Knowledge gap detection:
  //    Agent used a library/pattern that exists in specs but wasn't fetched
  //    Signal: error references something that a spec covers
  const relatedSpec = specEngine.findRelatedSpec?.(errorText);
  const specWasFetched = specEngine.wasSpecFetchedThisSession?.(relatedSpec);
  if (relatedSpec && !specWasFetched) {
    classification.type = 'knowledge_gap';
    classification.evidence = `Spec ${relatedSpec} covers this pattern but was not fetched by the agent`;
    classification.recommendation = 'Boost routing map relevance for this spec';
  }

  // 2. Architectural violation detection:
  //    Error matches an existing spec rule, or import pattern violates known boundaries
  const ruleViolation = specEngine.checkRulesAgainstError?.(errorText);
  if (ruleViolation) {
    classification.type = 'architectural_violation';
    classification.evidence = `Violates rule in ${ruleViolation.specId}: ${ruleViolation.message}`;
    classification.recommendation = 'Strengthen rule enforcement or add missing rule';
  }

  // 3. Missing capability detection:
  //    Agent attempted something the harness doesn't support
  //    Signal: error about missing tool, unresolved env, missing config
  if (errorText.match(/command not found|ENOENT|not installed|no such file/i)) {
    classification.type = 'missing_capability';
    classification.evidence = `Agent attempted to use unavailable tool or resource`;
    classification.recommendation = 'Add tooling or document workaround';
  }

  // 4. Default: code bug (agent made a mistake it should be able to fix)
  if (!classification.type) {
    classification.type = 'code_bug';
    classification.recommendation = 'Retry with remediation hints';
  }

  return classification;
}
How It Changes the Ralph Loop
In subagent-stop.js, the retry logic at line ~189 becomes classification-aware:


// Current (line ~190):
const remediation = buildRemediationBlock(gateResult, specEngine, tier);
// ...retry with same approach regardless of failure type

// New:
const remediation = buildRemediationBlock(gateResult, specEngine, tier);
const classification = classifyFailure(gateResult, specEngine);

// Attach classification to remediation log entry
remediationLog.push({
  iteration: retries,
  timestamp: new Date().toISOString(),
  classification: classification.type,    // NEW
  evidence: classification.evidence,      // NEW
  primary_violation: remediation.primary_violation,
  relevant_spec: remediation.relevant_spec,
  learning_signal: remediation.learning_signal,
  proposal_queued: false
});

// Classification-aware response:
if (classification.type === 'knowledge_gap') {
  // Inject the missing spec directly into the retry prompt
  // so the agent has the knowledge THIS iteration, not next session
  const specContent = specEngine.fetchSpecById?.(relatedSpec);
  correctionPrompt += `\n\nRELEVANT SPEC (was not in your routing map):\n${specContent}`;
}
What This Gives Us
Every failure gets a type tag in the remediation log
knowledge_gap failures get the missing spec injected immediately (not just a proposal for next time)
architectural_violation failures are distinguished from simple bugs
missing_capability failures are flagged for human attention rather than pointless retries
The classification data flows into the handoff (Option 3) and spec proposals (Option 2)
Effort
New classifyFailure() function in quality-gate.js (~60 lines)
Add wasSpecFetchedThisSession() to spec-engine.js (track which specs were fetched via the existing spec_fetch_log)
Modify subagent-stop.js lines ~189-240 to use classification
Unit tests for each classification path
Option 2: Fast-Track Spec Proposals from Classified Failures
What Changes
Currently in subagent-stop.js:219-231, every Ralph Loop failure creates a spec proposal at confidence 0.3 regardless of failure type:


// Current (line ~223):
proposeSpecUpdate(
  `auto/${gate}`,
  `# Auto-Detected Quality Pattern\n\n${remediation.learning_signal}`,
  remediation.learning_signal,
  { source: 'ralph-loop', learningSignal: remediation.learning_signal }
);
// Always starts at 0.3, always the same format
With classification, proposals become smarter and faster:

Classification-Aware Proposal Strategy

// In subagent-stop.js, after classifyFailure():

if (classification.type === 'knowledge_gap') {
  // Higher starting confidence — this is a verified gap, not a guess
  proposeSpecUpdate(
    `gap/${specDomain}`,
    generateKnowledgeGapSpec(classification, remediation),
    remediation.learning_signal,
    {
      source: 'ralph-loop',
      learningSignal: remediation.learning_signal,
      initialConfidence: 0.5,        // NEW: starts higher
      failureType: 'knowledge_gap'   // NEW: tagged
    }
  );
}

if (classification.type === 'architectural_violation') {
  // Propose a RULE, not just prose
  proposeSpecUpdate(
    `rule/${specDomain}`,
    generateRuleProposal(classification, remediation),
    remediation.learning_signal,
    {
      source: 'ralph-loop',
      learningSignal: remediation.learning_signal,
      initialConfidence: 0.5,
      failureType: 'architectural_violation',
      proposedRules: [{                // NEW: machine-checkable rule
        type: 'grep_must_not_exist',
        pattern: extractViolationPattern(classification),
        files: extractViolationFiles(classification),
        message: classification.evidence
      }]
    }
  );
}

if (classification.type === 'missing_capability') {
  // Don't create a spec — create a gap report entry instead
  appendGapReport({
    type: 'missing_capability',
    description: classification.evidence,
    recommendation: classification.recommendation,
    timestamp: new Date().toISOString(),
    taskId: ralph.lastTaskId
  });
  // No retry — this won't fix itself
}
Changes to proposeSpecUpdate()
In spec-engine.js:140, proposeSpecUpdate needs to accept initialConfidence:


// Current: always starts at 0.3
`confidence: 0.3`,

// New: respect initialConfidence from classified failures
`confidence: ${options.initialConfidence ?? 0.3}`,
Also, when proposedRules is provided, embed them in the proposal frontmatter so they're ready to become mechanical enforcement when promoted:


---
proposalId: 1711123456-rule-auth
specName: rule/auth
confidence: 0.5
source: ralph-loop
failureType: architectural_violation
proposedRules:
  - type: grep_must_not_exist
    pattern: "require\\(['\"]jsonwebtoken['\"]\\)"
    files: "src/**/*.ts"
    message: "Use jose library, not jsonwebtoken"
---
The New Confidence Ladder

Current:     0.3 → (+0.1 per repeat) → 0.6 cap → human accepts → 0.7 → Store
New:
  code_bug:            0.3 → 0.6 cap → human → 0.7 → Store  (unchanged)
  knowledge_gap:       0.5 → 0.6 cap → human → 0.7 → Store  (faster)
  architectural_violation: 0.5 + proposed rules → human → 0.7 → mechanical enforcement
  missing_capability:  No proposal → gap report → handoff
What This Gives Us
Knowledge gap failures reach promotion threshold faster (2 repeats instead of 3)
Architectural violations come with pre-built rules — when human accepts, enforcement is mechanical immediately
Missing capability failures don't pollute the spec system with un-actionable proposals
This is the bridge between the current advisory system and the mechanical enforcement plan
Option 3: Gap Reporting in Handoffs
What Changes
Currently the handoff has 10 sections. We add Section 4b: Environment Gaps between "Key Decisions" and "Files Modified".

New Data Source: gap-report.json

// lib/state.js — new functions
export function appendGapReport(entry) {
  const gapPath = join(process.cwd(), '.threadwork', 'state', 'gap-report.json');
  const existing = existsSync(gapPath)
    ? JSON.parse(readFileSync(gapPath, 'utf8'))
    : { gaps: [] };
  existing.gaps.push(entry);
  writeFileSync(gapPath, JSON.stringify(existing, null, 2), 'utf8');
}

export function readGapReport() {
  const gapPath = join(process.cwd(), '.threadwork', 'state', 'gap-report.json');
  if (!existsSync(gapPath)) return { gaps: [] };
  return JSON.parse(readFileSync(gapPath, 'utf8'));
}
Gap entries come from three sources:

Failure classifier (Option 1) — missing_capability and knowledge_gap failures
Ralph Loop max retries — when escalation happens (line 173-186 in subagent-stop.js), classify WHY and log it
Entropy collector — recurring patterns it detects but can't auto-fix
Changes to handoff.js
In handoff.js:148-246, add between Section 4 and Section 5:


// Section 4b — Environment Gaps (new)
`## 4b. Environment Gaps Detected`,
...((() => {
  const gapReport = readGapReport();
  const sessionGaps = gapReport.gaps.filter(g =>
    new Date(g.timestamp) > new Date(sessionStartSha ? '2000-01-01' : now)  // filter to this session
  );
  if (sessionGaps.length === 0) return ['_No environment gaps detected this session._'];

  const lines = [];
  const byType = {};
  for (const gap of sessionGaps) {
    (byType[gap.type] ??= []).push(gap);
  }

  if (byType.knowledge_gap) {
    lines.push(`**Knowledge gaps** (${byType.knowledge_gap.length}): Agent lacked context that exists in codebase`);
    for (const g of byType.knowledge_gap.slice(0, 3)) {
      lines.push(`  - ${g.description} → Spec proposed at confidence ${g.proposalConfidence ?? 0.5}`);
    }
  }

  if (byType.architectural_violation) {
    lines.push(`**Architectural violations** (${byType.architectural_violation.length}): Agent broke rules not yet enforced mechanically`);
    for (const g of byType.architectural_violation.slice(0, 3)) {
      lines.push(`  - ${g.description} → Rule proposed for enforcement`);
    }
  }

  if (byType.missing_capability) {
    lines.push(`**Missing capabilities** (${byType.missing_capability.length}): Agent needed tooling that doesn't exist`);
    for (const g of byType.missing_capability.slice(0, 3)) {
      lines.push(`  - ${g.description}`);
    }
  }

  lines.push('');
  lines.push(`**Recommended harness improvements**: Review gaps above and consider:`);
  lines.push(`  - Accept spec proposals via /tw:specs proposals`);
  lines.push(`  - Add missing tools or documentation`);
  lines.push(`  - Add spec rules for recurring architectural violations`);
  return lines;
})()),
'',
What the Handoff Looks Like

## 4b. Environment Gaps Detected

**Knowledge gaps** (2): Agent lacked context that exists in codebase
  - Custom AppError class not in routing map → Spec proposed at confidence 0.5
  - Database migration naming convention unknown → Spec proposed at confidence 0.5

**Architectural violations** (1): Agent broke rules not yet enforced mechanically
  - Service imported from UI layer (src/services/user.ts → src/ui/Modal) → Rule proposed

**Missing capabilities** (1): Agent needed tooling that doesn't exist
  - Attempted to run database seed command but no seed script exists

**Recommended harness improvements**: Review gaps above and consider:
  - Accept spec proposals via /tw:specs proposals
  - Add missing tools or documentation
  - Add spec rules for recurring architectural violations
The Compound Effect
Over multiple sessions, the gap report becomes a harness improvement backlog:

Session 1: "Agent didn't know about AppError" → spec proposed
Session 2: Same gap detected → confidence auto-bumps to 0.6
Session 3: User accepts → spec active at 0.7 → future agents always know
The gap never happens again
Option 4: Does This Require Restructuring?
Short answer: No. The enforcement pipeline we already designed fits naturally with options 1-3. Here's why:

The current flow:


discuss → plan → execute → ralph loop (retry) → verify → handoff
With all 4 options, it becomes:


discuss (9 questions → spec rules generated)          ← enforcement plan Part 4
    ↓
plan (plan-checker validates against rules)            ← enforcement plan Part 5
    ↓
execute (executor receives binding constraints)        ← enforcement plan Part 5
    ↓
ralph loop:
    ├── quality gates (lint/type/test/spec-compliance) ← enforcement plan Part 3
    ├── classifyFailure()                              ← Option 1 (NEW)
    ├── classification-aware proposals                 ← Option 2 (NEW)
    ├── gap report entries                             ← Option 3 (NEW)
    └── retry (with injected missing spec if knowledge_gap)
    ↓
verify (check rules + review criteria)                 ← enforcement plan Part 6
    ↓
handoff (Section 4b: Environment Gaps)                 ← Option 3 (NEW)
Everything slots into existing hooks and modules. The key insight is:

Options 1-3 are the feedback loop — they detect and classify what went wrong, propose targeted fixes, and surface gaps
The enforcement plan (Parts 1-6) is the enforcement mechanism — it provides the rules that prevent classified failures from recurring
They're complementary, not conflicting. The failure classifier (Option 1) feeds the enforcement system (rules). The enforcement system prevents the failures that the classifier would otherwise detect.

The only new files beyond what the enforcement plan already calls for:

classifyFailure() added to quality-gate.js
wasSpecFetchedThisSession() added to spec-engine.js
appendGapReport() / readGapReport() added to state.js
Handoff Section 4b added to handoff.js
gap-report.json state file (new, trivial)
No new hooks, no new agents, no structural changes to the pipeline.

Want me to update the discussion document with all of this, or shall we discuss any of these options further before documenting?

please update the discussion document with all of this and I will check
Read harness_engineering.md
Globpattern: "docs/discuss/**/*"
No files found
Bash Check discuss directory
IN
ls -la /Users/george.li/Documents/nexora/Threadwork/docs/discuss/

OUT
total 0
drwxr-xr-x@  2 george.li  staff   64 Mar 19 22:40 .
drwxr-xr-x@ 11 george.li  staff  352 Mar 19 22:40 ..
The file was removed. Let me recreate it with the full analysis.

Write harness_engineering.md
804 lines
# Harness Engineering: Analysis & Threadwork Gap Assessment

> Based on OpenAI's "Harness Engineering: Leveraging Codex in an Agent-First World" and related coverage.
> Discussion date: 2026-03-19

---

## Sources

- [OpenAI: Harness Engineering (via mirror)](https://jaytaylor.com/notes/node/1770842156000.html)
- [InfoQ: OpenAI Harness Engineering](https://www.infoq.com/news/2026/02/openai-harness-engineering-codex/)
- [Martin Fowler: Harness Engineering](https://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html)
- [NxCode: Complete Guide](https://www.nxcode.io/resources/news/harness-engineering-complete-guide-ai-agent-codex-2026)
- [Eng Leadership Newsletter](https://newsletter.eng-leadership.com/p/how-openais-codex-team-works-and)

---

## Part 1: The 9 Core Problems & Threadwork's Position

OpenAI built a 1M+ line production app with zero manual code over 5 months (3 engineers, ~3.5 PRs/engineer/day). The article surfaces 9 core problems and resolutions.

### Problem 1: Underspecified Environments

**Challenge**: Early progress lagged not from agent capability gaps but insufficient tooling and abstractions in the repo.

**OpenAI Resolution**: Systems-first approach — break goals into building blocks, identify missing capabilities, make them "both legible and enforceable for the agent."

**Threadwork Status: Mostly Implemented**
- `/tw:new-project` scaffolds PROJECT.md, REQUIREMENTS.md, ROADMAP.md
- `/tw:analyze-codebase` maps brownfield repos
- Spec library provides domain knowledge
- Phase workflow (discuss -> plan -> execute -> verify) structures work

**Gap**: No automated **capability gap detection**. When an agent struggles, Threadwork doesn't automatically identify what's missing (tool, doc, guardrail) and feed fixes back into the repo. The Ralph Loop retries but doesn't systematically upgrade the environment.

---

### Problem 2: Human QA Bottleneck

**Challenge**: As agent throughput scaled, human QA became the limiting constraint.

**OpenAI Resolution**: Made the app UI legible to Codex (Chrome DevTools Protocol, DOM snapshots, screenshots). Exposed observability data (LogQL, PromQL). Agents ran 6+ hours autonomously.

**Threadwork Status: Partially Implemented**
- Ralph Loop automates quality gates (tsc, lint, tests, build, security)
- Agents self-correct up to 5 retries
- `/tw:verify-phase` does goal-backward verification
- Entropy collector catches cross-output drift

**Gap**: No runtime observability integration. Threadwork can't give agents access to logs, metrics, traces, or browser state. QA is limited to static analysis (lint/type/test). OpenAI's agents reproduce bugs from telemetry data.

---

### Problem 3: Context Management & Information Overload

**Challenge**: "One big AGENTS.md" failed. "Too much guidance becomes non-guidance. When everything is important, nothing is." Monolithic manuals "rot instantly."

**OpenAI Resolution**: AGENTS.md as table of contents (~100 lines). Structured `docs/` as system of record. Mechanical enforcement via linters. "Doc-gardening" agent for staleness.

**Threadwork Status: Well Implemented**
- **Progressive Disclosure** is first-class. Routing map (~150 tokens) acts as table of contents.
- On-demand `spec_fetch` retrieves full specs only when needed. Saves 20K-80K tokens per phase.
- Spec library organized by domain with YAML frontmatter.
- Session-start injection is compact (~orientation + budget + top-3 store entries).

**Gap**: No doc-gardening agent. Entropy collector scans code diffs but not documentation for staleness or cross-link validation. No mechanical enforcement that docs are up-to-date.

---

### Problem 4: Agent Legibility vs Human Readability

**Challenge**: Code optimized for human reading sometimes obscured intent from agents.

**OpenAI Resolution**: Optimize for agent legibility first. Treat repo as agent's onboarding doc. Favor "boring" tech. Reimplement over opaque dependencies. "Anything not in-context doesn't exist."

**Threadwork Status: Partially Addressed**
- Spec system + Store make project knowledge accessible in-context.
- Everything persists to `.threadwork/` — no state in env vars or human memory.
- Handoffs are self-contained (Section 10 has full resume prompt).

**Gap**: Threadwork doesn't guide users on making their codebase agent-legible. No tooling to audit agent comprehension factors (dependency transparency, in-repo docs completeness, opaque library detection). More of a consulting/guidance gap than a tooling gap.

---

### Problem 5: Architectural Coherence Without Manual Enforcement

**Challenge**: Agent-generated code replicates and compounds existing patterns, including bad ones.

**OpenAI Resolution**: Rigid layered architecture (Types -> Config -> Repo -> Service -> Runtime -> UI). Mechanical enforcement via custom linters. "By enforcing invariants, not micromanaging implementations, we let agents ship fast without undermining the foundation."

**Threadwork Status: Partially Implemented**
- Spec system can encode architectural patterns
- Quality gates run linters
- Entropy collector detects naming drift, import violations, duplicate logic
- Specs tagged `taste_invariant: true` enforced during entropy scans

**Gap: HIGH PRIORITY**. No structural/architectural test framework. Threadwork doesn't enforce dependency direction rules or layer boundaries mechanically. Specs are advisory — agents *can ignore them*. OpenAI uses deterministic linters that fail the build. This is the biggest philosophical gap.

---

### Problem 6: Technical Debt / Garbage Collection

**Challenge**: 20% of Fridays spent cleaning "AI slop". Manual garbage collection doesn't scale.

**OpenAI Resolution**: Encode golden principles as mechanical rules. Run background tasks on regular cadence — scan deviations, update quality grades, open targeted refactoring PRs. Automerge low-risk cleanup.

**Threadwork Status: Well Implemented**
- **Entropy Collector** is exactly this — background agent scanning wave diffs across 6 categories (naming drift, import violations, orphaned artifacts, doc staleness, inconsistent error handling, duplicate logic).
- Minor issues get auto-fix commits. Warning issues queue for next wave.

**Gap**: Entropy collector only runs post-wave, not on a regular cadence. No quality grade tracking over time. No automerge for low-risk cleanups. Could benefit from continuous garbage collection mode independent of phase workflow.

---

### Problem 7: Human Judgment Encoding

**Challenge**: Capturing subjective team preferences in a form agents can apply consistently.

**OpenAI Resolution**: Capture review comments as doc updates. Promote feedback into tooling. Inject custom linter error messages with remediation instructions.

**Threadwork Status: Well Implemented**
- **Spec Proposal Pipeline** with confidence escalation (0.3 -> 0.6 -> 0.7 -> Store)
- Ralph Loop learning signals automatically propose specs from failures
- Cross-session Store promotes patterns globally
- Remediation blocks include `relevant_spec` and `fix_template`

**Gap**: No automated capture from PR review comments. Human feedback during code review doesn't flow into spec proposals. Pipeline triggered by Ralph Loop failures, not human review patterns.

---

### Problem 8: PR Velocity vs Quality Gates

**Challenge**: Conventional blocking merge gates became counterproductive at agent throughput.

**OpenAI Resolution**: Minimal blocking merge gates. Short-lived PRs. "Corrections are cheap, waiting is expensive." Caveat: "This would be irresponsible in a low-throughput environment."

**Threadwork Status: Conservative Approach (Appropriate)**
- Ralph Loop is a blocking gate (up to 5 retries before escalation)
- Quality gates cache per git SHA
- Team model enables parallel execution

**Gap**: No configurable gate strictness. Can't mark certain gates as non-blocking or set "flake tolerance." For high-throughput teams this could become a bottleneck. Consider gate policies (blocking/warning/skip) per gate type.

---

### Problem 9: Agent Review Bottleneck

**Challenge**: Human review became throughput limitation.

**OpenAI Resolution**: Push "almost all review effort" to agent-to-agent. Humans instruct Codex to review its own changes, request additional reviews, iterate until all agent reviewers satisfied.

**Threadwork Status: Partially Implemented**
- Ralph Loop is self-review (quality gates)
- `/tw:verify-phase` is goal-backward verification
- Plan-checker validates plans across 6 dimensions
- Entropy collector reviews cross-output coherence

**Gap: HIGH PRIORITY**. No agent-to-agent review workflow. No dedicated review agent examining PRs for semantic correctness, design quality, or specification compliance. Current review is structural (lint/type/test), not semantic.

---

## Summary Scorecard

| # | Problem | Threadwork Status | Priority |
|---|---------|------------------|----------|
| 1 | Underspecified Environments | Mostly Implemented | Medium |
| 2 | Human QA Bottleneck (observability) | Partially Implemented | Low* |
| 3 | Context Management | **Well Implemented** | -- |
| 4 | Agent Legibility | Partially Addressed | Low |
| 5 | Architectural Enforcement | Partially Implemented | **High** |
| 6 | Garbage Collection | Well Implemented | Medium |
| 7 | Human Judgment Encoding | Well Implemented | Medium |
| 8 | PR Velocity vs Gates | Conservative | Low |
| 9 | Agent-to-Agent Review | Partially Implemented | **High** |

*Runtime observability requires deep integration with each user's stack — may be out of scope.

---

## Part 2: What Threadwork Does Better Than OpenAI's Approach

1. **Token Economics** — OpenAI never mentions token budgets, cost tracking, or variance analysis. Their agents run "6+ hours" with no visibility. Threadwork's budget system with 80/90/95% thresholds and per-task variance is a significant operational advantage.

2. **Session Continuity** — OpenAI assumes long-running single sessions. Threadwork's 10-section handoff with auto-populated decisions enables multi-session projects with no context loss.

3. **Progressive Disclosure** — OpenAI's "AGENTS.md as TOC" is a first step. Threadwork's routing map (~150 tokens) + on-demand spec_fetch with token tracking is more complete.

4. **Confidence-Gated Learning** — The 0.3 -> 0.6 -> 0.7 -> Store pipeline is genuinely novel. OpenAI encodes human judgment manually; Threadwork has an automated escalation path.

5. **Multi-Model Orchestration** — Threadwork assigns Opus/Sonnet/Haiku by task complexity. OpenAI uses a single model throughout.

---

## Part 3: Deep Dive — Advisory vs Mechanical Enforcement

### The Philosophical Divide

OpenAI's core insight:

> "By enforcing invariants, not micromanaging implementations, we let agents ship fast without undermining the foundation."

Their constraints are **deterministic and blocking** — a custom linter fails the build if code imports across layer boundaries. The agent has no choice but to comply.

Threadwork's approach: specs are **injected as context** and agents are trusted to follow them. The Ralph Loop catches violations *after the fact* via lint/type/test gates, but architectural rules like "service layer cannot import from UI" aren't enforced mechanically.

**Arguments for Threadwork's current approach:**
- Threadwork serves diverse codebases — can't assume a specific architecture
- Mechanical enforcement requires users to define rules upfront (high setup cost)
- The spec confidence pipeline is a softer feedback loop that learns over time

**Arguments for adding mechanical enforcement:**
- Advisory specs create a "soft ceiling" — agents can drift without consequence
- Ralph Loop only catches what lint/type/test catch — architectural violations that don't cause build failures slip through
- OpenAI's experience shows agents *will* compound bad patterns if not mechanically prevented

### Where Enforcement Currently Breaks Down

The pipeline has 4 enforcement points — all advisory:

| Stage | Enforcement Point | Type | Problem |
|---|---|---|---|
| **Plan** | plan-checker Dimension 6 "Spec Compliance" | Advisory | Checker flags violations but agent can ignore or misinterpret |
| **Execute** | Executor prompt: "never contradict spec library entries" | Prompt instruction | Relies on agent compliance. No verification. |
| **Execute** | Ralph Loop quality gates | Mechanical (lint/type/test) | Only catches structural violations. "Use jose not jsonwebtoken" isn't a lint rule. |
| **Verify** | Verifier reads specs + decisions | Advisory | Checks requirements coverage, not spec compliance. |

**The fundamental issue**: specs contain prose, not machine-checkable rules. A spec says "Use RS256 for JWT signing" but nothing mechanically verifies the code actually uses RS256.

---

## Part 4: Mechanical Enforcement Plan (Decision: 2026-03-19)

### User Decision

> "Even though we trust agents to follow specs, we still need a way to enforce and validate that rules are being followed. Since AI does the coding, we need to make sure the code follows the spec. We should invest more in the planning stage — more questions are worth it."

**Chosen approach**: Both combined (spec rules engine + structural test framework) with enhanced discuss-phase (9 questions including enforcement and review criteria).

### 4.1 Spec Rules Engine

Extend spec frontmatter with a `rules` array. 5 rule types:

| Type | What it checks | How |
|------|---------------|-----|
| `grep_must_exist` | Pattern MUST appear in matching files | `grep -r` with glob filter |
| `grep_must_not_exist` | Pattern must NOT appear | `grep -r` inverse |
| `import_boundary` | Files in `from` glob cannot import from `cannot_import` globs | Parse import statements |
| `naming_pattern` | Exported names must match regex | Parse exports, test regex |
| `file_structure` | Required file patterns must exist | Glob check |

Example spec with rules:
```yaml
---
domain: backend
specId: SPEC:auth-001
name: JWT Best Practices
tags: [auth, jwt]
rules:
  - type: grep_must_exist
    pattern: "RS256"
    files: "src/lib/auth*.ts"
    message: "JWT must use RS256 algorithm per SPEC:auth-001"
  - type: grep_must_not_exist
    pattern: "require\\(['\"]jsonwebtoken['\"]\\)"
    files: "src/**/*.{ts,js}"
    message: "Use jose library, not jsonwebtoken (SPEC:auth-001)"
  - type: import_boundary
    from: "src/services/**"
    cannot_import: ["src/ui/**", "src/components/**"]
    message: "Service layer cannot import from UI layer (SPEC:arch-001)"
---
```

**Files**: New `lib/rule-evaluator.js`. Modified `lib/spec-engine.js` (add `loadRules()`).

### 4.2 Structural Test Gate

Convention: `.threadwork/structural-tests/` directory with JS files exporting `check(projectRoot)` for complex invariants (circular deps, custom business rules).

```javascript
// .threadwork/structural-tests/no-circular-deps.js
export const name = 'No Circular Dependencies';
export const specId = 'SPEC:arch-001';
export async function check(projectRoot) {
  // Run madge or custom logic
  const { execSync } = await import('child_process');
  try {
    execSync('npx madge --circular src/', { cwd: projectRoot });
    return { passed: true };
  } catch (e) {
    return { passed: false, errors: [`Circular dependencies found: ${e.stdout}`] };
  }
}
```

**Files**: Modified `lib/quality-gate.js` (add `runStructuralTests()`).

### 4.3 Spec Compliance Quality Gate

New gate `runSpecCompliance()` combining spec rules + structural tests. Registered in `runAll()`:

```
Gate order: typecheck → lint → tests → spec-compliance → build → security
```

Runs in Ralph Loop. On failure, `buildRemediationBlock()` produces rule-aware remediation:
```javascript
{
  primary_violation: "SPEC:auth-001 rule violated: Use jose library, not jsonwebtoken",
  relevant_spec: "SPEC:auth-001 | backend/jwt-best-practices",
  fix_template: "Replace require('jsonwebtoken') with import { SignJWT } from 'jose'",
  learning_signal: "spec-rule:auth-001:grep_must_not_exist:jsonwebtoken"
}
```

**Files**: Modified `lib/quality-gate.js`, `hooks/subagent-stop.js`.

### 4.4 Enhanced Discuss-Phase (9 Questions)

Current 5 questions + 4 new enforcement/review questions:

| # | Question | Purpose | Feeds into |
|---|----------|---------|------------|
| 1 | Preferred libraries/frameworks | Context | CONTEXT.md |
| 2 | Patterns to follow | Context | CONTEXT.md |
| 3 | Known constraints | Context | CONTEXT.md |
| 4 | Known risks | Context | CONTEXT.md |
| 5 | Out of scope | Context | CONTEXT.md |
| **6** | **Architectural boundaries** — "What layers exist and what are the allowed dependency directions?" | **Enforcement** | **Spec rules (import_boundary)** |
| **7** | **Forbidden patterns** — "Any libraries, patterns, or approaches that must NOT be used?" | **Enforcement** | **Spec rules (grep_must_not_exist)** |
| **8** | **Naming conventions** — "Any naming rules for files, exports, or variables?" | **Enforcement** | **Spec rules (naming_pattern)** |
| **9** | **Code review focus** — "What should code review focus on beyond passing tests? Acceptance criteria?" | **Verification** | **Verifier instructions in CONTEXT.md** |

Auto-generates spec rules from answers to Q6-8. Review criteria from Q9 stored in CONTEXT.md `## Review Criteria` section.

**Files**: Modified `templates/commands/tw-discuss-phase.md`.

### 4.5 Binding Constraints (Plan-Checker → Executor)

Plan-checker extracts applicable spec rules per plan's file targets and generates `<constraints>` XML:

```xml
<plan id="PLAN-1-2">
  ...
  <constraints generated-by="plan-checker" timestamp="...">
    <constraint specId="SPEC:auth-001" rule="grep_must_exist">
      Files matching src/lib/auth*.ts MUST contain pattern "RS256"
    </constraint>
    <constraint specId="SPEC:arch-001" rule="import_boundary">
      Files in src/services/** MUST NOT import from src/ui/**
    </constraint>
  </constraints>
</plan>
```

Injected into executor prompts via `pre-tool-use.js`. Verifier checks constraint compliance post-execution.

**Files**: Modified `templates/agents/tw-plan-checker.md`, `hooks/pre-tool-use.js`, `templates/agents/tw-verifier.md`.

### 4.6 Enhanced Verifier with Review Criteria

Two new verification dimensions:
1. **Spec Rule Compliance** — run `evaluateRules()` and report
2. **Review Criteria** — check each criterion from CONTEXT.md against code

**Files**: Modified `templates/agents/tw-verifier.md`, `templates/commands/tw-verify-phase.md`.

---

## Part 5: Deep Dive — The "Failure as Signal" Mental Model

### The Core Insight

> When agents struggle, treat it as a signal to identify missing components (tools, guardrails, documentation) and feed fixes back into the repository.

Threadwork's Ralph Loop treats failure as "try again harder" (up to 5 retries). OpenAI treats failure as "the environment is broken — fix the harness, not the agent." These are fundamentally different philosophies:

| | Threadwork (Ralph Loop) | OpenAI (Harness-First) |
|---|---|---|
| Agent fails | Retry with remediation hints | Ask: what's missing in the environment? |
| After max retries | Escalate to human | Improve tooling/docs/guardrails |
| Learning captured in | Spec proposals (confidence 0.3) | Repo-level infrastructure changes |
| Feedback target | Future agent runs | The harness itself |

Threadwork *partially* does this via spec proposals on failure. But proposals target *patterns* ("use JWT refresh rotation"), not *environment gaps* ("agents need a database migration helper tool").

### Why This Matters: The Compound Effect

Consider a concrete example. An executor is building an API endpoint and fails because:
- It doesn't know the project uses a custom error handler
- It writes `throw new Error(...)` instead of `throw new AppError(...)`
- Tests fail because the error middleware expects `AppError`

**Ralph Loop today:**
1. Tests fail → remediation block says "Fix test failure in error-handler.test.js"
2. Agent retries, maybe figures out `AppError` exists, maybe doesn't
3. Next session, a *different* executor hits the same problem
4. Spec proposal created at confidence 0.3 — but it's about the *pattern*, not the *gap*

**Harness-first approach:**
1. Tests fail → system asks: *why* did the agent not know about `AppError`?
2. Answer: there's no spec covering the error handling pattern
3. Action: auto-generate a spec with the pattern + a `grep_must_not_exist` rule for `throw new Error(`
4. Next session, every executor knows about `AppError` from the routing map, and the rule prevents the mistake mechanically

The difference is **where the fix lives**. Ralph Loop fixes the *output* (this code, this session). Harness-first fixes the *input* (the environment, permanently).

### Building Blocks Already in Threadwork

1. **Spec proposals from Ralph Loop** (`hooks/subagent-stop.js:219-231`) — failures create proposals at confidence 0.3. This *is* environment improvement, but slow and pattern-targeted.
2. **Learning signals** (`lib/quality-gate.js:buildRemediationBlock()`) — fingerprints for deduplication. Same signal 3+ times auto-escalates to 0.6. Captures "what went wrong" not "what was missing."
3. **Decision logging** (`lib/state.js:appendDecision()`) — executors log choices in `<decisions>` XML. Feeds into handoffs but not back into the harness.
4. **Entropy collector** (`lib/entropy-collector.js`) — scans for cross-output drift. Could extend to scan for recurring failure patterns.

### The Missing Piece: Failure Classification

When the Ralph Loop hits a failure, it should ask *why* before asking *how to fix it*.

---

## Part 6: Failure-as-Signal Implementation Plan (Decision: 2026-03-19)

Three complementary features that transform the Ralph Loop from "retry harder" to "improve the harness":

### 6.1 Failure Classification in the Ralph Loop

**What changes**: Add a classification step between failure detection and retry in `hooks/subagent-stop.js`. New `classifyFailure()` function in `lib/quality-gate.js`.

**The classifier examines the error to determine *why* it failed:**

```
Failure detected
    │
    ▼
What type of failure is this?
    │
    ├── code_bug (agent made a mistake)
    │     → Retry with remediation (current Ralph Loop behavior)
    │
    ├── knowledge_gap (agent didn't know about X)
    │     → Inject missing spec into retry prompt immediately
    │     → Auto-generate spec proposal at higher confidence
    │
    ├── missing_capability (agent couldn't do X)
    │     → Log as environment gap
    │     → Surface to user in handoff — don't waste retries
    │
    └── architectural_violation (agent broke a rule it wasn't told about)
          → Auto-generate spec RULE (not just prose)
          → Connects to mechanical enforcement system
```

**Classification logic** (new function in `lib/quality-gate.js`):

```javascript
export function classifyFailure(gateResults, specEngine) {
  const classification = {
    type: null,        // 'code_bug' | 'knowledge_gap' | 'missing_capability' | 'architectural_violation'
    confidence: 0,     // 0-1 how confident the classification is
    evidence: '',      // why we classified it this way
    recommendation: '' // what to do about it
  };

  const failed = gateResults.results.filter(r => !r.passed && !r.skipped);
  const errors = failed.flatMap(r => r.errors ?? r.failures ?? []);
  const errorText = errors.join('\n');

  // 1. Knowledge gap: error references something a spec covers
  //    but that spec was never fetched this session
  const relatedSpec = specEngine.findRelatedSpec?.(errorText);
  const specWasFetched = specEngine.wasSpecFetchedThisSession?.(relatedSpec);
  if (relatedSpec && !specWasFetched) {
    classification.type = 'knowledge_gap';
    classification.evidence = `Spec ${relatedSpec} covers this but was not fetched`;
    classification.recommendation = 'Boost routing map relevance for this spec';
    return classification;
  }

  // 2. Architectural violation: error matches an existing spec rule
  const ruleViolation = specEngine.checkRulesAgainstError?.(errorText);
  if (ruleViolation) {
    classification.type = 'architectural_violation';
    classification.evidence = `Violates rule in ${ruleViolation.specId}: ${ruleViolation.message}`;
    classification.recommendation = 'Strengthen rule enforcement or add missing rule';
    return classification;
  }

  // 3. Missing capability: agent tried to use unavailable tool/resource
  if (errorText.match(/command not found|ENOENT|not installed|no such file/i)) {
    classification.type = 'missing_capability';
    classification.evidence = 'Agent attempted to use unavailable tool or resource';
    classification.recommendation = 'Add tooling or document workaround';
    return classification;
  }

  // 4. Default: code bug (agent made a fixable mistake)
  classification.type = 'code_bug';
  classification.recommendation = 'Retry with remediation hints';
  return classification;
}
```

**How it changes the Ralph Loop** (`hooks/subagent-stop.js` lines ~189-240):

```javascript
// Current flow:
const remediation = buildRemediationBlock(gateResult, specEngine, tier);
// → always retry with same approach

// New flow:
const remediation = buildRemediationBlock(gateResult, specEngine, tier);
const classification = classifyFailure(gateResult, specEngine);

// Classification attached to remediation log
remediationLog.push({
  iteration: retries,
  timestamp: new Date().toISOString(),
  classification: classification.type,     // NEW
  evidence: classification.evidence,       // NEW
  primary_violation: remediation.primary_violation,
  relevant_spec: remediation.relevant_spec,
  learning_signal: remediation.learning_signal,
  proposal_queued: false
});

// Classification-aware retry:
if (classification.type === 'knowledge_gap') {
  // Inject the missing spec INTO THIS retry — don't wait for next session
  const specContent = specEngine.fetchSpecById?.(relatedSpecId);
  correctionPrompt += `\n\nRELEVANT SPEC (was not in your routing map):\n${specContent}`;
}

if (classification.type === 'missing_capability') {
  // Log gap, don't waste retries on something that can't be fixed
  appendGapReport({
    type: 'missing_capability',
    description: classification.evidence,
    recommendation: classification.recommendation,
    timestamp: new Date().toISOString()
  });
}
```

**What this gives us:**
- Every failure gets a `type` tag in the remediation log
- `knowledge_gap` failures get the missing spec injected *immediately* (not next session)
- `missing_capability` failures are flagged for humans rather than wasting retries
- Classification data flows into proposals (6.2) and handoffs (6.3)

**Files to modify:**
- `lib/quality-gate.js` — add `classifyFailure()` (~60 lines)
- `lib/spec-engine.js` — add `wasSpecFetchedThisSession()` (track via existing `spec_fetch_log`)
- `hooks/subagent-stop.js` — use classification in retry logic (~30 lines changed)
- `tests/unit/quality-gate-classify.test.js` — new test file for classification paths

---

### 6.2 Fast-Track Spec Proposals from Classified Failures

**What changes**: Currently every Ralph Loop failure creates a spec proposal at confidence 0.3 regardless of failure type (`hooks/subagent-stop.js:219-231`). With classification, proposals become smarter and faster.

**Classification-aware proposal strategy:**

```javascript
// In subagent-stop.js, after classifyFailure():

if (classification.type === 'knowledge_gap') {
  // Higher starting confidence — verified gap, not a guess
  proposeSpecUpdate(
    `gap/${specDomain}`,
    generateKnowledgeGapSpec(classification, remediation),
    remediation.learning_signal,
    {
      source: 'ralph-loop',
      learningSignal: remediation.learning_signal,
      initialConfidence: 0.5,         // starts higher than default 0.3
      failureType: 'knowledge_gap'
    }
  );
}

if (classification.type === 'architectural_violation') {
  // Propose a RULE, not just prose — ready for mechanical enforcement
  proposeSpecUpdate(
    `rule/${specDomain}`,
    generateRuleProposal(classification, remediation),
    remediation.learning_signal,
    {
      source: 'ralph-loop',
      learningSignal: remediation.learning_signal,
      initialConfidence: 0.5,
      failureType: 'architectural_violation',
      proposedRules: [{                // machine-checkable rule
        type: 'grep_must_not_exist',
        pattern: extractViolationPattern(classification),
        files: extractViolationFiles(classification),
        message: classification.evidence
      }]
    }
  );
}

if (classification.type === 'missing_capability') {
  // Don't pollute spec system — create gap report entry instead
  appendGapReport({
    type: 'missing_capability',
    description: classification.evidence,
    recommendation: classification.recommendation,
    timestamp: new Date().toISOString(),
    taskId: ralph.lastTaskId
  });
}

// code_bug: unchanged — standard proposal at 0.3
```

**Changes to proposeSpecUpdate()** (`lib/spec-engine.js:140`):

```javascript
// Current: always starts at 0.3
`confidence: 0.3`,

// New: respect initialConfidence from classified failures
`confidence: ${options.initialConfidence ?? 0.3}`,
```

When `proposedRules` is provided, embed them in the proposal frontmatter — ready to become mechanical enforcement when promoted:

```yaml
---
proposalId: 1711123456-rule-auth
specName: rule/auth
confidence: 0.5
source: ralph-loop
failureType: architectural_violation
proposedRules:
  - type: grep_must_not_exist
    pattern: "require\\(['\"]jsonwebtoken['\"]\\)"
    files: "src/**/*.ts"
    message: "Use jose library, not jsonwebtoken"
---
```

**The new confidence ladder:**

```
Current:
  All failures:          0.3 → (+0.1 per repeat) → 0.6 cap → human accepts → 0.7 → Store

New:
  code_bug:              0.3 → 0.6 cap → human → 0.7 → Store  (unchanged)
  knowledge_gap:         0.5 → 0.6 cap → human → 0.7 → Store  (faster — 2 repeats vs 3)
  architectural_violation: 0.5 + proposed rules → human → 0.7 → mechanical enforcement
  missing_capability:    No proposal → gap report → handoff  (doesn't pollute specs)
```

**Key insight**: This is the bridge between the advisory system and mechanical enforcement. Architectural violations come with *pre-built rules* — when the human accepts, enforcement is mechanical *immediately*.

**Files to modify:**
- `lib/spec-engine.js` — accept `initialConfidence` and `proposedRules` in `proposeSpecUpdate()`
- `hooks/subagent-stop.js` — classification-aware proposal logic
- `tests/unit/spec-engine.test.js` — test new proposal options

---

### 6.3 Gap Reporting in Handoffs

**What changes**: Add **Section 4b: Environment Gaps** to the 10-section handoff document, between "Key Decisions" and "Files Modified".

**New data source — gap-report.json** (new functions in `lib/state.js`):

```javascript
export function appendGapReport(entry) {
  const gapPath = join(process.cwd(), '.threadwork', 'state', 'gap-report.json');
  const existing = existsSync(gapPath)
    ? JSON.parse(readFileSync(gapPath, 'utf8'))
    : { gaps: [] };
  existing.gaps.push(entry);
  writeFileSync(gapPath, JSON.stringify(existing, null, 2), 'utf8');
}

export function readGapReport() {
  const gapPath = join(process.cwd(), '.threadwork', 'state', 'gap-report.json');
  if (!existsSync(gapPath)) return { gaps: [] };
  return JSON.parse(readFileSync(gapPath, 'utf8'));
}
```

Gap entries come from three sources:
1. **Failure classifier** (6.1) — `missing_capability` and `knowledge_gap` classifications
2. **Ralph Loop max retries** (`subagent-stop.js:173-186`) — when escalation happens, classify WHY and log it
3. **Entropy collector** — recurring patterns it detects but can't auto-fix

**Changes to handoff.js** (new Section 4b between lines ~183 and ~196):

Example output in handoff:

```markdown
## 4b. Environment Gaps Detected

**Knowledge gaps** (2): Agent lacked context that exists in codebase
  - Custom AppError class not in routing map → Spec proposed at confidence 0.5
  - Database migration naming convention unknown → Spec proposed at confidence 0.5

**Architectural violations** (1): Agent broke rules not yet enforced mechanically
  - Service imported from UI layer (src/services/user.ts → src/ui/Modal) → Rule proposed

**Missing capabilities** (1): Agent needed tooling that doesn't exist
  - Attempted to run database seed command but no seed script exists

**Recommended harness improvements**: Review gaps above and consider:
  - Accept spec proposals via /tw:specs proposals
  - Add missing tools or documentation
  - Add spec rules for recurring architectural violations
```

**The compound effect across sessions:**
- Session 1: "Agent didn't know about AppError" → spec proposed at 0.5
- Session 2: Same gap detected → confidence auto-bumps to 0.6
- Session 3: User accepts → spec active at 0.7 → future agents always know
- The gap *never happens again*

**Files to modify:**
- `lib/state.js` — add `appendGapReport()`, `readGapReport()`
- `lib/handoff.js` — add Section 4b between current sections 4 and 5
- `hooks/subagent-stop.js` — write gap entries on max retries escalation
- `tests/unit/handoff-gaps.test.js` — new test file

---

## Part 7: How Everything Connects (No Restructuring Required)

Options 1-3 (failure classification, fast-track proposals, gap reporting) and the enforcement plan (Part 4) are **complementary, not conflicting**. They slot into existing hooks and modules:

```
discuss-phase (9 questions → spec rules generated)             ← Part 4.4
    │
    ▼
plan-phase
    │ plan-checker validates against rules                     ← Part 4.5
    │ generates binding constraints per task                   ← Part 4.5
    │
    ▼
execute-phase
    │ executor receives binding constraints + routing map      ← Part 4.5
    │ executor implements task
    │
    ▼
ralph loop:
    ├── quality gates (lint/type/test + spec-compliance)       ← Part 4.3
    ├── classifyFailure()                                      ← Part 6.1 (NEW)
    ├── classification-aware proposals                         ← Part 6.2 (NEW)
    │     code_bug → standard proposal (0.3)
    │     knowledge_gap → fast-track proposal (0.5) + inject missing spec into retry
    │     architectural_violation → proposal with rules (0.5) → becomes enforcement on accept
    │     missing_capability → gap report entry (no proposal, no wasted retries)
    ├── gap report entries                                     ← Part 6.3 (NEW)
    └── retry (with injected missing spec if knowledge_gap)
    │
    ▼
verify-phase
    │ verifier checks requirements + spec rules + review criteria   ← Part 4.6
    │
    ▼
handoff
    │ Section 4: Key Decisions (existing)
    │ Section 4b: Environment Gaps (NEW)                       ← Part 6.3
    │     knowledge gaps → spec proposals to accept
    │     architectural violations → rules to enforce
    │     missing capabilities → tooling to add
    │ Section 5-10: (existing)
```

**The feedback loop**: Failure classifier (6.1) **feeds** the enforcement system (4.3). The enforcement system **prevents** the failures that the classifier would otherwise detect. Over time, the harness self-improves:

```
failure → classify → propose (spec or rule) → human accepts → enforce → failure prevented
```

### Files Summary (All Changes)

| File | Part | Action |
|------|------|--------|
| `lib/rule-evaluator.js` | 4.1 | **Create** — rule evaluation engine (5 types) |
| `lib/spec-engine.js` | 4.1, 6.2 | Modify — `loadRules()`, `wasSpecFetchedThisSession()`, `initialConfidence` support |
| `lib/quality-gate.js` | 4.3, 6.1 | Modify — `runSpecCompliance()`, `runStructuralTests()`, `classifyFailure()`, extend `runAll()` and `buildRemediationBlock()` |
| `lib/state.js` | 6.3 | Modify — `appendGapReport()`, `readGapReport()` |
| `lib/handoff.js` | 6.3 | Modify — add Section 4b |
| `hooks/subagent-stop.js` | 4.3, 6.1, 6.2, 6.3 | Modify — classification-aware retry, proposal, and gap reporting |
| `hooks/pre-tool-use.js` | 4.5 | Modify — inject `<constraints>` from plan XML |
| `templates/commands/tw-discuss-phase.md` | 4.4 | Modify — add questions 6-9, auto-generate spec rules |
| `templates/agents/tw-plan-checker.md` | 4.5 | Modify — enhanced Dimension 6, binding constraints |
| `templates/agents/tw-executor.md` | 4.5 | Modify — document constraint awareness |
| `templates/agents/tw-verifier.md` | 4.6 | Modify — spec rule compliance + review criteria dimensions |
| `templates/commands/tw-verify-phase.md` | 4.6 | Modify — load review criteria from CONTEXT.md |
| `templates/specs/enforcement/` | 4.1 | **Create** — starter spec templates with example rules |
| `tests/unit/rule-evaluator.test.js` | 4.1 | **Create** |
| `tests/unit/quality-gate-compliance.test.js` | 4.3 | **Create** |
| `tests/unit/quality-gate-classify.test.js` | 6.1 | **Create** |
| `tests/unit/handoff-gaps.test.js` | 6.3 | **Create** |
| `tests/integration/spec-enforcement.test.js` | 4.3, 6.1-6.3 | **Create** — end-to-end enforcement + classification flow |

No new hooks. No new agents. No structural changes to the pipeline.
Done. The complete document is at harness_engineering.md with 7 parts:

The 9 Core Problems — full gap mapping with scorecard
Threadwork's Advantages — 5 areas ahead of OpenAI
Advisory vs Mechanical Enforcement — the philosophical divide
Mechanical Enforcement Plan — spec rules, structural tests, enhanced discuss-phase, binding constraints
Failure as Signal Deep Dive — why "retry harder" vs "fix the harness" matters
Failure-as-Signal Implementation — classifier (6.1), fast-track proposals (6.2), gap reporting in handoffs (6.3)
How Everything Connects — full pipeline diagram showing no restructuring needed, with complete file manifest
Let's continue our discussion,  
for Problem 1: Underspecified environments, Threadwork do not have automated capability gap detection, what's we can do to improve this?
Good question. The failure classifier we designed in Part 6 handles the reactive side — detecting gaps after agents fail. But the real OpenAI insight is also proactive: identify missing capabilities before agents hit them.

Let me think about where gaps can be detected across the Threadwork lifecycle:

Where Gaps Can Be Detected

                    PROACTIVE                              REACTIVE
                (before failure)                       (after failure)
                                                    
  /tw:analyze-codebase    /tw:plan-phase          Ralph Loop failure
  /tw:new-project         pre-execution scan       classifier (Part 6.1)
        │                       │                        │
        ▼                       ▼                        ▼
  "Your codebase has      "This plan touches       "Agent failed because
   no spec for your        auth/ but no auth        it didn't know about
   custom error handler"   spec exists"             AppError"
We've designed the reactive side. The proactive side has three natural insertion points:

Approach 1: Codebase Readiness Audit
Threadwork already has /tw:analyze-codebase which maps brownfield repos. But right now it produces an architecture summary — it doesn't assess harness readiness. What if it also produced a gap analysis?

The idea: scan the codebase and ask "if an agent had to work here, what would it stumble on?"

What to scan for:

Signal	Detection Method	Gap Type
Custom base classes (AppError, BaseService, etc.)	Grep for extends + check if covered by any spec	Knowledge gap
Custom middleware/decorators	Grep for common patterns (express middleware, decorators)	Knowledge gap
Internal utility libraries (lib/, utils/, helpers/)	Check if any spec references these directories	Knowledge gap
Non-standard scripts in package.json	Parse scripts beyond standard (test, build, start)	Missing capability awareness
Environment-specific setup (.env.example, docker-compose)	Check existence but no spec documenting setup	Knowledge gap
Import aliases (tsconfig paths, webpack aliases)	Parse tsconfig/webpack for path mappings not in specs	Knowledge gap
Monorepo structure (workspaces, packages/)	Detect workspace config, check for boundary specs	Architectural gap
Custom linter rules (.eslintrc custom rules)	Parse and check if rules are reflected in specs	Enforcement gap
Output: A harness-readiness-report.md with concrete recommendations:


# Harness Readiness Report

## Coverage Score: 62% (15 of 24 patterns covered by specs)

## Gaps Found

### High Priority (agents will likely stumble on these)
1. **Custom error handling** — `src/lib/errors.ts` exports AppError, ValidationError, 
   NotFoundError but no spec covers error handling patterns.
   → Recommendation: Create spec with grep_must_not_exist rule for `new Error(`
   
2. **Database utilities** — `src/lib/db/` has custom query builder, connection pool, 
   migration runner. No spec covers database patterns.
   → Recommendation: Create spec documenting query patterns and migration workflow

3. **Import aliases** — tsconfig.json defines 6 path aliases (@/lib, @/services, etc.) 
   not documented in any spec.
   → Recommendation: Add to architecture spec or create import convention spec

### Medium Priority
4. **Custom scripts** — package.json has 8 non-standard scripts (seed, migrate, 
   generate-types, etc.) not referenced in any spec.
   → Recommendation: Document in a tooling/workflow spec

### Low Priority  
5. **Env variables** — .env.example lists 14 variables. No spec documents which 
   are required vs optional.
Where this lives: Extend lib/spec-engine.js with a auditHarnessReadiness() function. Called from /tw:analyze-codebase and also available as a standalone /tw:readiness command.

The interesting part: the audit can auto-generate draft specs for detected gaps. Not just "you're missing a spec" but "here's a draft spec based on what I found in the code." User reviews and accepts.

Approach 2: Pre-Execution Gap Scanner
Before /tw:execute-phase starts spawning executors, scan the plan's file targets against existing specs. This catches gaps at the last moment before an agent would hit them.

When it runs: In templates/commands/tw-execute-phase.md, after Step 1 (budget check) and before Step 2 (load plans). New Step 1.5.

What it checks:


// lib/spec-engine.js — new function
export function scanPlanForGaps(planXml, allSpecs) {
  const gaps = [];
  
  for (const task of planXml.tasks) {
    const taskFiles = task.files;  // e.g., ["src/services/auth.ts", "src/lib/db/users.ts"]
    
    // 1. Check: do any file targets fall in directories with zero spec coverage?
    for (const file of taskFiles) {
      const dir = dirname(file);
      const hasSpec = allSpecs.some(s => 
        s.rules?.some(r => r.files && minimatch(file, r.files)) ||
        s.tags?.some(t => dir.includes(t))
      );
      if (!hasSpec) {
        gaps.push({
          type: 'uncovered_directory',
          file,
          message: `${dir}/ has no spec coverage — agent may lack context`
        });
      }
    }
    
    // 2. Check: does the task description reference patterns not in any spec?
    const keywords = extractKeywords(task.description); // auth, database, caching, etc.
    for (const keyword of keywords) {
      const hasSpec = allSpecs.some(s => 
        s.tags?.includes(keyword) || s.name?.toLowerCase().includes(keyword)
      );
      if (!hasSpec) {
        gaps.push({
          type: 'uncovered_pattern',
          keyword,
          message: `Task references "${keyword}" but no spec covers this pattern`
        });
      }
    }
    
    // 3. Check: does the plan reference libraries not in any spec?
    const libraries = extractLibraryReferences(task.description);
    for (const lib of libraries) {
      const hasSpec = allSpecs.some(s => s.content?.includes(lib));
      if (!hasSpec) {
        gaps.push({
          type: 'undocumented_library',
          library: lib,
          message: `Library "${lib}" referenced but not documented in specs`
        });
      }
    }
  }
  
  return gaps;
}
What happens when gaps are found:

The executor doesn't just not know about the gap — it gets a heads-up:


Pre-execution gap scan found 2 potential gaps:

1. src/lib/db/ has no spec coverage — agent may lack context
   Action: Injecting directory README if found, or flagging for manual spec creation

2. Task references "caching" but no spec covers this pattern
   Action: Agent will need to discover patterns by reading existing code
   
Proceeding with execution. Gaps will be tracked in gap-report.json.
For each gap found:

If a README or inline docs exist in the uncovered directory → inject them into the executor's context alongside the routing map
If nothing exists → log to gap-report.json and warn the executor: "No spec covers the patterns in src/lib/db/. Read existing files carefully before adding new code."
After execution → if the task succeeds despite the gap, propose a spec from what the agent learned (confidence 0.4 — higher than code_bug at 0.3 but lower than knowledge_gap at 0.5)
Approach 3: Post-Session Gap Aggregation
Gap reports accumulate across sessions. Instead of just listing them in handoffs, we can aggregate and prioritize them.

New function in lib/state.js:


export function aggregateGaps() {
  const report = readGapReport();
  const gaps = report.gaps ?? [];
  
  // Group by description fingerprint (similar gaps across sessions)
  const grouped = {};
  for (const gap of gaps) {
    const key = gap.type + ':' + fingerprint(gap.description);
    if (!grouped[key]) {
      grouped[key] = { ...gap, occurrences: 0, sessions: new Set() };
    }
    grouped[key].occurrences++;
    grouped[key].sessions.add(gap.sessionId ?? 'unknown');
  }
  
  // Sort by frequency — most recurring gaps first
  return Object.values(grouped)
    .sort((a, b) => b.occurrences - a.occurrences)
    .map(g => ({
      ...g,
      sessions: g.sessions.size,
      priority: g.occurrences >= 3 ? 'high' : g.occurrences >= 2 ? 'medium' : 'low',
      recommendation: generateRecommendation(g)
    }));
}
Where it surfaces:

Session-start (hooks/session-start.js): If there are high-priority recurring gaps, inject a one-liner: ⚠ Recurring gap: src/lib/db/ has no spec coverage (hit 4 times across 3 sessions). Consider creating a spec.
/tw:readiness: Shows the full aggregated gap report with trends
Handoff Section 4b: Already designed in Part 6.3, now enriched with occurrence counts
The self-healing loop:


Session 1: Agent fails on AppError → gap detected (1 occurrence)
Session 2: Same gap → (2 occurrences, medium priority)  
Session 3: Same gap → (3 occurrences, HIGH priority)
           Session-start warns: "Recurring gap — create error-handling spec"
           User creates spec → gap never recurs
Or even more automatic:


Session 3: 3 occurrences → auto-generate draft spec from accumulated context
           Proposal created at confidence 0.5 with assembled evidence
           User: /tw:specs proposals → review → accept
How All Three Connect

/tw:analyze-codebase (or /tw:readiness)
    │
    │  Proactive: "Your codebase has these uncovered areas"
    │  Output: harness-readiness-report.md + draft spec proposals
    │
    ▼
/tw:execute-phase  
    │
    │  Pre-execution: "This plan touches uncovered areas"
    │  Output: gap warnings + README injection + gap-report entries
    │
    ▼
Ralph Loop failure
    │
    │  Reactive: "Agent failed because of missing knowledge/capability"
    │  Output: classified gap + fast-track proposal (Part 6.1-6.2)
    │
    ▼
Handoff (Section 4b)
    │
    │  Report: "These gaps were hit this session"
    │  Output: prioritized list with recommendations
    │
    ▼
Next session start
    │
    │  Aggregation: "These gaps keep recurring — fix them"
    │  Output: high-priority warnings injected into orientation
    │
    ▼
User acts (or system auto-proposes)
    │
    │  Resolution: spec created, rule added, tool documented
    │  Gap removed from future reports
The key insight: three layers of detection (proactive audit, pre-execution scan, reactive classification) all feed into the same gap-report.json, which aggregates across sessions and surfaces the highest-impact improvements.

Files Impact (Incremental to What We Already Planned)
File	Action	Description
lib/spec-engine.js	Modify	Add auditHarnessReadiness(), scanPlanForGaps()
lib/state.js	Modify	Add aggregateGaps() (extends existing readGapReport() from Part 6.3)
hooks/session-start.js	Modify	Inject recurring gap warnings from aggregation
templates/commands/tw-readiness.md	Create	New /tw:readiness command
templates/commands/tw-analyze-codebase.md	Modify	Add harness readiness audit to output
templates/commands/tw-execute-phase.md	Modify	Add pre-execution gap scan (Step 1.5)
tests/unit/spec-engine-gaps.test.js	Create	Tests for readiness audit + plan gap scanner
Want me to update the discussion document with this, or do you want to dig into any of these approaches further first?

yes, pelase update the discussion document
Read harness_engineering.md (lines 2-21)
Read harness_engineering.md (from line 351)
Now I have the full document. Let me append Part 8 for the capability gap detection.

Edit harness_engineering.md
Added 519 lines
Done. Part 8 (Automated Capability Gap Detection) and Part 9 (Complete Files Manifest) are now appended to harness_engineering.md. The document now covers:

9 Core Problems — gap mapping + scorecard
Threadwork's Advantages — 5 areas ahead
Advisory vs Mechanical Enforcement — the philosophical divide
Mechanical Enforcement Plan — spec rules, structural tests, discuss-phase, binding constraints
Failure as Signal — why "retry harder" vs "fix the harness" matters
Failure-as-Signal Implementation — classifier, fast-track proposals, gap reporting
How Everything Connects — pipeline diagram, no restructuring needed
Capability Gap Detection — 3-layer proactive detection (readiness audit, pre-execution scan, post-session aggregation) + the self-healing loop
Complete Files Manifest — 10 new files, 15 modified, ~25 test files across all parts
okay. let's discuss the problem 2: Human QA Bottleneck.
Good topic. Let me re-read what we have on Problem 2 and think about what's realistic for Threadwork.

The Problem
OpenAI's solution to the QA bottleneck was making the running application legible to agents — Chrome DevTools Protocol for DOM snapshots/screenshots, local ephemeral stacks per worktree, LogQL/PromQL for querying logs and metrics. Their agents could reproduce bugs from telemetry data and run 6+ hours autonomously.

We flagged this as "Low priority" with the caveat: "requires deep integration with each user's stack — may be out of scope."

But let's challenge that. The real question isn't "can Threadwork integrate with every user's observability stack?" — it's "what QA can Threadwork automate beyond lint/type/test?"

What QA Threadwork Does Today

Static Analysis (automated, in Ralph Loop):
  ✅ TypeScript type checking
  ✅ Linting (eslint/biome/oxlint)
  ✅ Test execution
  ✅ Build verification
  ✅ Security audit (npm audit)

Semantic Verification (agent-driven, in verify-phase):
  ✅ Requirements coverage check
  ✅ Done-condition verification
  ✅ Integration observation
  ⬜ (planned) Spec rule compliance
  ⬜ (planned) Review criteria checking

Runtime Verification:
  ❌ Can the app actually start?
  ❌ Do API endpoints return expected responses?
  ❌ Does the UI render correctly?
  ❌ Do logs show expected behavior?
The entire bottom row is missing. That's the gap OpenAI filled.

What's Actually Feasible
There's a spectrum between "nothing" and "full Chrome DevTools + observability integration":

Level 1: Smoke Test Gate (Low effort, high value)
The simplest possible runtime check: can the app start without crashing?


// New gate in quality-gate.js
export function runSmokeTest() {
  const pkg = join(process.cwd(), 'package.json');
  if (!existsSync(pkg)) return { passed: true, skipped: true, reason: 'No package.json' };
  
  const pkgData = JSON.parse(readFileSync(pkg, 'utf8'));
  const startScript = pkgData.scripts?.start ?? pkgData.scripts?.dev;
  if (!startScript) return { passed: true, skipped: true, reason: 'No start/dev script' };

  // Start the app, wait for it to bind a port or timeout
  const child = spawn('npm', ['run', pkgData.scripts?.dev ? 'dev' : 'start'], {
    cwd: process.cwd(), timeout: 15000, stdio: 'pipe'
  });
  
  return new Promise((resolve) => {
    let output = '';
    child.stdout.on('data', d => { output += d; });
    child.stderr.on('data', d => { output += d; });
    
    // Success signals: "listening on", "ready on", "started on"
    const successPattern = /listening|ready|started|running/i;
    const errorPattern = /error|crash|EADDRINUSE|Cannot find/i;
    
    const check = setInterval(() => {
      if (successPattern.test(output)) {
        clearInterval(check);
        child.kill();
        resolve({ passed: true, errors: [] });
      }
      if (errorPattern.test(output)) {
        clearInterval(check);
        child.kill();
        resolve({ passed: false, errors: [output.slice(-500)] });
      }
    }, 1000);
    
    child.on('exit', (code) => {
      clearInterval(check);
      resolve(code === 0 || successPattern.test(output)
        ? { passed: true, errors: [] }
        : { passed: false, errors: [`App exited with code ${code}: ${output.slice(-500)}`] });
    });
    
    setTimeout(() => {
      clearInterval(check);
      child.kill();
      // No signal either way — treat as warning, not failure
      resolve({ passed: true, errors: [], warning: 'App did not signal ready within 15s' });
    }, 15000);
  });
}
This catches: missing dependencies, broken imports, config errors, port conflicts — things that pass type/lint/test but fail at runtime. Very common with agent-generated code.

Level 2: HTTP Endpoint Verification (Medium effort, high value)
If the app has API endpoints, verify they respond correctly. This builds on the smoke test — the app is already running.

Key idea: The plan XML already has <verification> blocks with specific expectations like "POST /auth/login returns { token, expiresAt }". We can parse those and actually execute HTTP checks.


// New gate: runEndpointVerification(plan)
export async function runEndpointVerification(plan, baseUrl = 'http://localhost:3000') {
  const checks = [];
  
  for (const task of plan.tasks) {
    // Parse verification blocks for HTTP expectations
    const httpChecks = extractHttpExpectations(task.verification);
    // e.g., [{ method: 'POST', path: '/auth/login', expectStatus: 200, expectBody: ['token'] }]
    
    for (const check of httpChecks) {
      try {
        const res = await fetch(`${baseUrl}${check.path}`, {
          method: check.method,
          headers: { 'Content-Type': 'application/json' },
          body: check.body ? JSON.stringify(check.body) : undefined
        });
        
        const passed = check.expectStatus
          ? res.status === check.expectStatus
          : res.ok;
        
        const body = await res.json().catch(() => null);
        const bodyCheck = check.expectBody
          ? check.expectBody.every(key => body && key in body)
          : true;
        
        checks.push({
          endpoint: `${check.method} ${check.path}`,
          passed: passed && bodyCheck,
          status: res.status,
          missingKeys: check.expectBody?.filter(k => !body || !(k in body)) ?? []
        });
      } catch (e) {
        checks.push({
          endpoint: `${check.method} ${check.path}`,
          passed: false,
          error: e.message
        });
      }
    }
  }
  
  return {
    gate: 'endpoint-verification',
    passed: checks.every(c => c.passed),
    checks,
    skipped: checks.length === 0,
    reason: checks.length === 0 ? 'No HTTP expectations found in plan verification blocks' : undefined
  };
}
This is powerful because the expected behavior is already specified in the plan XML — we're just actually checking it at runtime instead of trusting the agent's self-report.

Level 3: Configurable Runtime Verification (Higher effort, extensible)
Instead of hardcoding what to check, let users define runtime verification steps in a structured format. This mirrors how structural tests work (Part 4.2) but for runtime checks.

Convention: .threadwork/runtime-checks/ directory:


// .threadwork/runtime-checks/api-health.js
export const name = 'API Health Check';
export const requires = 'server';  // needs app running

export async function check({ baseUrl, projectRoot }) {
  const res = await fetch(`${baseUrl}/health`);
  if (!res.ok) return { passed: false, errors: ['Health endpoint returned ' + res.status] };
  
  const data = await res.json();
  if (!data.database) return { passed: false, errors: ['Database not connected'] };
  
  return { passed: true };
}

// .threadwork/runtime-checks/no-console-errors.js
export const name = 'No Console Errors in Startup';
export const requires = 'server';

export async function check({ serverOutput }) {
  const errors = serverOutput.split('\n').filter(l => 
    l.includes('Error') || l.includes('WARN') || l.includes('deprecated')
  );
  return {
    passed: errors.length === 0,
    errors: errors.slice(0, 5)
  };
}
Level 4: What's Probably Out of Scope
These are powerful but require too much per-project integration:

Browser automation (Playwright/Puppeteer for UI testing) — useful but huge dependency, complex setup
Full observability integration (LogQL, PromQL, Grafana) — every project is different
Database state verification — too many ORMs and migration tools to generalize
However, Level 4 could be partially addressed by the structural test framework (Part 4.2) — users can write custom checks that do whatever they need, including browser automation. We just don't build it into the core.

What I'd Recommend for Threadwork
The highest-value path is probably Levels 1 + 2, with Level 3 as an extension point:

Level	Value	Effort	Recommendation
1: Smoke test	High — catches runtime crashes	Low — ~60 lines in quality-gate.js	Include in v0.4.0
2: Endpoint verification	High — validates actual behavior	Medium — needs plan parsing + HTTP client	Include in v0.4.0
3: Custom runtime checks	Medium — extensibility	Low — follows structural test pattern	Include as extension point
4: Browser/observability	High but niche	Very high — per-project integration	Out of scope (users can add via Level 3)
The key insight: Levels 1 and 2 require zero configuration from the user. The smoke test auto-detects the start script. Endpoint verification parses expectations that already exist in the plan XML. No new setup required — it just works.

Gate Configuration

// quality-config.json
{
  "typecheck": { "enabled": true, "blocking": true },
  "lint": { "enabled": true, "blocking": true },
  "tests": { "enabled": true, "blocking": true },
  "spec-compliance": { "enabled": true, "blocking": true },
  "smoke-test": { "enabled": true, "blocking": true },
  "endpoint-verification": { "enabled": true, "blocking": false },
  "runtime-checks": { "enabled": true, "blocking": false },
  "build": { "enabled": false, "blocking": false },
  "security": { "enabled": true, "blocking": false }
}
Smoke test is blocking (if the app can't start, nothing works). Endpoint verification and custom runtime checks default to non-blocking (warning) since they may have false positives.

When to Run
These runtime gates are heavier than lint/type/test. They shouldn't run on every Ralph Loop iteration:

Smoke test: Run in Ralph Loop (fast — 15s timeout). If the app can't start, the agent needs to fix that immediately.
Endpoint verification: Run only in verify-phase, not Ralph Loop. It's a post-execution check, not an iterative fix target.
Custom runtime checks: Run only in verify-phase.

Ralph Loop gates:     typecheck → lint → tests → spec-compliance → smoke-test
Verify-phase gates:   all Ralph Loop gates + endpoint-verification + runtime-checks
What's your thinking on these levels? Does Level 1+2 with Level 3 as extension feel right, or do you want to go deeper on any specific approach?

Yes, Level 1 + 2 with Level 3 as extension is good,  but it will also good if development something that can't test with browser or API, for example a plugin for Obsidian,  Threadwork can ask user to provide the details of environment and Threadwork to validate,
That's a really important point. Not everything is a web server with HTTP endpoints. Consider:

Obsidian plugins — loaded in dev mode, no API to hit
VS Code extensions — requires Extension Development Host
CLI tools — run commands, check output
Library packages — consumed by other code, no runtime
Desktop apps (Electron) — GUI, no browser
Browser extensions — loaded into Chrome, can't automate easily
The common thread: the user knows how to validate their project, but Threadwork doesn't ask.

The Concept: Verification Profiles
During the discuss-phase, Threadwork should ask: "How is this project validated in practice?" Then build a verification profile that drives all runtime checking.

Where to Ask: Discuss-Phase Question 10
This becomes part of the enhanced discuss-phase (Part 4.4). Adding to our 9 questions:

#	Question	Feeds into
10	Verification environment — "How do you validate this project works? (web app, CLI, plugin, library, other)"	Verification profile in project.json
Follow-up questions depend on the answer:


User: "It's an Obsidian plugin"
    │
    ▼
Follow-ups:
  - "How do you build it?" → "npm run build" 
  - "What's the build output?" → "main.js in project root"
  - "What must the manifest contain?" → "id, name, version, minAppVersion"
  - "How do you test it manually?" → "Load in Obsidian dev vault, open settings, 
     check the plugin appears, toggle it on, verify the ribbon icon shows"
  - "Any automated checks possible?" → "manifest.json must be valid JSON with 
     required fields, main.js must export an onload function"
The Verification Profile
Stored in project.json under a new verification key:


{
  "verification": {
    "type": "plugin",
    "platform": "obsidian",
    "build": {
      "command": "npm run build",
      "outputs": ["main.js", "manifest.json", "styles.css"]
    },
    "automated": [
      {
        "name": "Manifest validation",
        "check": "json_schema",
        "file": "manifest.json",
        "requiredFields": ["id", "name", "version", "minAppVersion", "description", "author"]
      },
      {
        "name": "Build output exports",
        "check": "export_exists",
        "file": "main.js",
        "exports": ["onload", "onunload"]
      },
      {
        "name": "No banned APIs",
        "check": "grep_must_not_exist",
        "pattern": "eval\\(|document\\.write",
        "files": "src/**/*.ts",
        "message": "Obsidian plugins must not use eval() or document.write()"
      }
    ],
    "manual": [
      {
        "step": "Load plugin in dev vault",
        "expected": "Plugin appears in Settings → Community Plugins",
        "critical": true
      },
      {
        "step": "Toggle plugin on",
        "expected": "No errors in console, ribbon icon appears",
        "critical": true
      },
      {
        "step": "Open command palette",
        "expected": "Plugin commands are listed",
        "critical": false
      }
    ]
  }
}
What This Enables: Three Verification Layers
Layer A: Automated checks (run in gates)

These are things we can verify mechanically, even for non-web projects:

Project Type	Automated Checks
Obsidian plugin	manifest.json valid, main.js exports onload/onunload, no banned APIs, build output exists
VS Code extension	package.json has activationEvents, main entry exists, contributes section valid
CLI tool	Binary exists after build, --help returns exit code 0, --version outputs version matching package.json
Library	All declared exports resolve, types compile, peer dependencies satisfied
Electron app	Main process starts without crash, preload scripts exist
Browser extension	manifest.json valid (v2 or v3), content scripts reference existing files
These run as part of the runtime-checks gate (Level 3) using a new profile-aware runner:


// lib/verification-profile.js
export function runProfileChecks(profile) {
  const results = [];
  
  for (const check of profile.automated ?? []) {
    switch (check.check) {
      case 'json_schema':
        results.push(verifyJsonSchema(check.file, check.requiredFields));
        break;
      case 'export_exists':
        results.push(verifyExports(check.file, check.exports));
        break;
      case 'grep_must_not_exist':
        results.push(verifyNoForbiddenPatterns(check.pattern, check.files));
        break;
      case 'command_runs':
        results.push(verifyCommandRuns(check.command, check.expectOutput));
        break;
      case 'file_exists':
        results.push(verifyFilesExist(check.files));
        break;
      case 'build_output':
        results.push(verifyBuildOutputs(profile.build));
        break;
    }
  }
  
  return {
    gate: 'profile-verification',
    passed: results.every(r => r.passed),
    results,
    skipped: results.length === 0
  };
}
Layer B: Guided manual verification (enhanced UAT.md)

For things that can't be automated, the verifier already generates UAT.md. But currently it's generic. With the verification profile, it becomes project-type aware:


# Phase 1 Manual Verification Steps

## Environment Setup
Platform: Obsidian (plugin)
Prerequisites:
- Obsidian installed with dev vault at ~/obsidian-dev/
- Community plugins enabled in settings

## Critical Checks (must pass before merge)

### 1. Plugin Loading
- Run `npm run build` in project root
- Copy main.js, manifest.json, styles.css to ~/obsidian-dev/.obsidian/plugins/my-plugin/
- Restart Obsidian
- [ ] **Expected**: Plugin appears in Settings → Community Plugins
- [ ] **Verify**: No errors in developer console (Ctrl+Shift+I)

### 2. Core Functionality  
- Toggle plugin ON in settings
- [ ] **Expected**: Ribbon icon appears in left sidebar
- [ ] **Expected**: Command palette (Ctrl+P) shows "My Plugin: Do Thing"

### 3. Phase-Specific Checks (from plan verification blocks)
- Open a note and trigger the plugin command
- [ ] **Expected**: [specific behavior from task verification block]

## Non-Critical Checks
- [ ] Settings tab renders without errors
- [ ] Plugin gracefully handles empty notes
The key: the manual steps come from the verification profile, not from the agent guessing. The user defined them during discuss-phase, so they're accurate and project-specific.

Layer C: User feedback loop (post-verification)

After the user runs manual checks, they can report results back:


User: /tw:verify-manual

Threadwork: Running through manual verification for Phase 1:

1. Plugin Loading — did the plugin appear in Settings?
   [Pass] [Fail] [Skip]

2. Core Functionality — did the ribbon icon appear?
   [Pass] [Fail] [Skip]
   
3. ...
Failed manual checks become:

Gap report entries (feeds into Part 8 aggregation)
Verifier FAIL status (blocks phase completion if critical: true)
Learning signals — if the same manual check fails across sessions, propose automating it
Profile Templates
To reduce setup cost, Threadwork can offer starter profiles for common project types. During discuss-phase:


Threadwork: "What type of project is this?"
  [Web App]  [CLI Tool]  [Library]  [Obsidian Plugin]  
  [VS Code Extension]  [Browser Extension]  [Other]

User: "Obsidian Plugin"

Threadwork: "Loading Obsidian plugin verification template. 
 I'll pre-fill common checks — please confirm or adjust:
 
 Build command: npm run build (correct?)
 Build outputs: main.js, manifest.json, styles.css (correct?)
 Required manifest fields: id, name, version, minAppVersion (add more?)
 Required exports: onload, onunload (add more?)
 
 Manual checks I've pre-filled:
 1. Plugin appears in Settings → Community Plugins
 2. No console errors on load
 3. Commands appear in command palette
 
 Add any project-specific manual checks?"
Starter templates stored in templates/verification-profiles/:


templates/verification-profiles/
├── web-app.json          # smoke test + endpoint verification
├── cli-tool.json         # command execution + output checks
├── library.json          # export verification + type checking  
├── obsidian-plugin.json  # manifest + build output + manual loading
├── vscode-extension.json # package.json validation + activation
├── browser-extension.json # manifest v3 + content script checks
└── electron-app.json     # main process start + preload checks
How This Integrates with the Gate System

Ralph Loop gates (every iteration):
  typecheck → lint → tests → spec-compliance → smoke-test*

Verify-phase gates (end of phase):
  all Ralph Loop gates
  + endpoint-verification*     (web-app profile only)
  + profile-verification       (automated checks from profile)
  + runtime-checks             (custom .threadwork/runtime-checks/)

Manual verification (user-driven):
  + UAT.md generated from profile manual steps
  + /tw:verify-manual for structured feedback
  + critical manual checks block phase completion

* smoke-test and endpoint-verification only run if profile.type matches
The profile type determines which gates are relevant:

Profile Type	Smoke Test	Endpoint Verification	Profile Checks	Manual Steps
web-app	Yes	Yes	Optional	Optional
cli-tool	No	No	command_runs, file_exists	Optional
library	No	No	export_exists, build_output	Minimal
obsidian-plugin	No	No	json_schema, export_exists, build_output	Yes (critical)
vscode-extension	No	No	json_schema, export_exists	Yes (critical)
custom	Configurable	Configurable	User-defined	User-defined
Files Impact (Incremental)
File	Action	Description
lib/verification-profile.js	Create	Profile loader, profile-aware check runner, manual check tracker
lib/quality-gate.js	Modify	Add runSmokeTest(), runEndpointVerification(), runProfileChecks(), update runAll() gate sequence
templates/verification-profiles/	Create	7 starter profile templates
templates/commands/tw-discuss-phase.md	Modify	Add question 10 (verification environment) + profile template loading
templates/commands/tw-verify-phase.md	Modify	Profile-aware UAT.md generation
templates/commands/tw-verify-manual.md	Create	/tw:verify-manual command for structured user feedback
templates/agents/tw-verifier.md	Modify	Use profile for targeted verification, generate profile-aware UAT.md
tests/unit/verification-profile.test.js	Create	Profile loading, check execution
tests/unit/quality-gate-smoke.test.js	Create	Smoke test + endpoint verification
Want me to update the discussion document with all of this?

yes, please update the discussion document with all
Read harness_engineering.md (from line 796)
Now I have the full document. Let me append Part 10 (Runtime QA) and update Part 9 (manifest).

Edit harness_engineering.md
Added 583 lines
Done. The document now has Part 9 (Runtime QA) and updated Part 10 (manifest). Here's what was added:

Part 9: Runtime QA Beyond Lint/Type/Test — 7 sections:

9.1 Smoke Test Gate — zero-config, can the app start?
9.2 Endpoint Verification — parses plan XML for HTTP expectations and actually checks them
9.3 Verification Profiles — the user tells Threadwork how to validate (Obsidian plugin, CLI tool, VS Code extension, etc.) with automated checks + manual steps stored in project.json
9.4 Profile-aware UAT.md — manual verification steps generated from profile + plan, not generic guesses
9.5 /tw:verify-manual — structured manual feedback that feeds back into the gap system
9.6 Gate execution strategy — which gates run where (Ralph Loop vs verify-phase vs manual)
9.7 Files impact — 10 new files including lib/verification-profile.js, 7 profile templates, /tw:verify-manual command
Part 10 updated with the complete manifest: 14 new files, 17 modified, 10 new test files across all parts.

okay let's discuss Problem 3: Context Management & Information Overload,  I need the mechaincal enforement for docs are update-to-date.
Let me check how specs currently track references to code and how the entropy collector works, so I can give you concrete details.

Read entropy-collector.js
Grep "function (getRelevantSpecs|findRelatedSpec|buildRoutingMap|fetchSpecById|generateSpecIds)" (in /Users/george.li/Documents/nexora/Threadwork/lib/spec-engine.js)
84 lines of output
Read tw-entropy-collector.md
Good. Now I have the full picture. Here's my analysis:

The Current State
Threadwork's entropy collector (category 4) already scans for documentation staleness — but only within the wave diff, only for inline code comments/docstrings, and only as an agent-driven heuristic (not mechanical). It doesn't touch specs at all.

The spec system has no staleness detection. A spec could reference src/lib/auth.ts — if that file gets renamed to src/lib/authentication.ts, the spec silently becomes stale. No gate catches this.

Three Layers of Doc Staleness
There are actually three distinct problems:

Layer 1: Spec-to-Code Reference Decay
Specs reference code — files, functions, libraries, patterns. When code changes, those references break silently.

Detectable signals:

Spec mentions file path src/lib/auth.ts → file renamed or deleted
Spec says "use jose library" → jose removed from package.json
Spec has code example import { SignJWT } from 'jose' → that export no longer exists
Spec rule targets files: "src/lib/auth*.ts" → no files match that glob anymore
Spec references SPEC:auth-001 → that spec ID was removed or renamed
Layer 2: Spec Age vs Code Churn
Even if references are valid, a spec can be semantically stale — the code has evolved but the spec hasn't been updated to reflect new patterns.

Detectable signals:

Spec updated date is old AND files referenced by the spec have been modified since
Spec covers a directory where >50% of files changed since the spec was last updated
Spec's domain tags match files with high recent git churn
Layer 3: Cross-Spec Consistency
Specs can contradict each other — one says "use jose" and another says "use jsonwebtoken." Or specs can have orphaned cross-references.

Detectable signals:

Two specs in the same domain have contradictory rules
Spec references another spec ID that doesn't exist
Multiple specs cover overlapping file globs with conflicting rules
Mechanical Enforcement Design
I'd propose a doc-freshness gate — a new quality gate that runs alongside spec-compliance, plus an extension to the entropy collector to cover specs, plus a new /tw:docs-health command.

Mechanism 1: Doc-Freshness Gate (Mechanical, in Ralph Loop)
A new gate runDocFreshness() that checks hard, mechanical things:


// lib/doc-freshness.js — new module

export function checkDocFreshness(specsDir, projectRoot) {
  const violations = [];

  for (const spec of loadAllSpecs(specsDir)) {
    // 1. Dead file references
    const referencedFiles = extractFileReferences(spec.content);
    for (const ref of referencedFiles) {
      const resolved = resolveGlob(ref, projectRoot);
      if (resolved.length === 0) {
        violations.push({
          type: 'dead_reference',
          specId: spec.data.specId,
          specFile: spec.path,
          reference: ref,
          message: `Spec ${spec.data.specId} references "${ref}" but no matching files exist`,
          severity: 'error'
        });
      }
    }

    // 2. Dead spec cross-references
    const crossRefs = extractSpecReferences(spec.content); // [SPEC:auth-001], etc.
    for (const ref of crossRefs) {
      if (!specIdExists(ref, specsDir)) {
        violations.push({
          type: 'dead_cross_reference',
          specId: spec.data.specId,
          reference: ref,
          message: `Spec ${spec.data.specId} references ${ref} but that spec ID doesn't exist`,
          severity: 'error'
        });
      }
    }

    // 3. Dead library references
    const libraries = extractLibraryReferences(spec.content);
    const installedDeps = getInstalledDependencies(projectRoot);
    for (const lib of libraries) {
      if (!installedDeps.includes(lib)) {
        violations.push({
          type: 'dead_library_reference',
          specId: spec.data.specId,
          library: lib,
          message: `Spec ${spec.data.specId} references library "${lib}" but it's not in package.json`,
          severity: 'warning'
        });
      }
    }

    // 4. Stale rule targets (rules whose file globs match nothing)
    for (const rule of spec.data.rules ?? []) {
      if (rule.files) {
        const matches = resolveGlob(rule.files, projectRoot);
        if (matches.length === 0) {
          violations.push({
            type: 'empty_rule_target',
            specId: spec.data.specId,
            rule: rule.type,
            glob: rule.files,
            message: `Rule in ${spec.data.specId} targets "${rule.files}" but no files match`,
            severity: 'warning'
          });
        }
      }
    }

    // 5. Age-based staleness (spec old + referenced files changed)
    const specUpdated = spec.data.updated ? new Date(spec.data.updated) : null;
    if (specUpdated) {
      const referencedFilesFlat = extractFileReferences(spec.content)
        .flatMap(ref => resolveGlob(ref, projectRoot));
      const filesChangedSinceSpec = referencedFilesFlat.filter(f => {
        try {
          const lastModified = getFileLastModifiedFromGit(f, projectRoot);
          return lastModified > specUpdated;
        } catch { return false; }
      });
      
      if (filesChangedSinceSpec.length > 0) {
        const ratio = filesChangedSinceSpec.length / referencedFilesFlat.length;
        if (ratio > 0.5) {
          violations.push({
            type: 'age_staleness',
            specId: spec.data.specId,
            updatedAt: spec.data.updated,
            changedFiles: filesChangedSinceSpec.length,
            totalFiles: referencedFilesFlat.length,
            message: `Spec ${spec.data.specId} last updated ${spec.data.updated} but ${filesChangedSinceSpec.length}/${referencedFilesFlat.length} referenced files changed since then`,
            severity: 'warning'
          });
        }
      }
    }
  }

  return {
    gate: 'doc-freshness',
    passed: violations.filter(v => v.severity === 'error').length === 0,
    violations,
    errors: violations.filter(v => v.severity === 'error'),
    warnings: violations.filter(v => v.severity === 'warning'),
    skipped: false
  };
}
What's blocking vs warning:

dead_reference (file doesn't exist) → blocking (spec is definitely wrong)
dead_cross_reference (spec ID doesn't exist) → blocking
dead_library_reference (library not installed) → warning (might be optional)
empty_rule_target (rule glob matches nothing) → warning (files might not exist yet)
age_staleness (spec old, code changed) → warning (needs human judgment)
Gate registration:


Gate order: typecheck → lint → tests → spec-compliance → doc-freshness → smoke-test → build → security
In quality-config.json:


{
  "doc-freshness": { "enabled": true, "blocking": true }
}
Mechanism 2: Spec-Aware Entropy Collector (Post-Wave)
Extend the entropy collector's scope to include specs. Currently it only scans code diffs. Add a 7th scan category:

New category 7: Spec Staleness

When a wave modifies code, check if any specs covering that code need updating:


Wave diff includes changes to src/lib/auth.ts
    │
    ▼
Which specs reference src/lib/auth.ts or src/lib/auth*?
    │
    ▼
SPEC:auth-001 — JWT Best Practices
    │
    ▼
Did the wave change any of the following?
  - Function signatures that the spec documents
  - Import patterns that the spec prescribes
  - Error handling approaches the spec covers
    │
    ▼
If yes → entropy issue: "Spec SPEC:auth-001 may be stale after changes to src/lib/auth.ts"
         severity: warning
         action: queue spec review for next session
This extends the entropy collector template to add:


### 7. Spec Staleness
Specs that reference files modified in this wave and may need updating.

- Minor: spec references a file that was modified but the changes don't affect the spec's content
  (e.g., formatting changes, unrelated function changes)
- Warning: spec references a file where the changes directly affect patterns the spec documents
  (e.g., function signatures changed, new error types added, import patterns modified)

For warning severity:
- Note which spec needs review and what changed
- Do NOT auto-update specs — specs require human review
- Queue as spec-review-needed in the report
Mechanism 3: Automatic Spec Timestamp Tracking
When code changes, automatically track which specs might need review. New function in lib/spec-engine.js:


export function trackSpecStaleness(changedFiles, allSpecs) {
  const staleSpecs = [];
  
  for (const spec of allSpecs) {
    const specReferences = extractFileReferences(spec.content);
    const specRuleTargets = (spec.data.rules ?? [])
      .filter(r => r.files)
      .map(r => r.files);
    
    const allTargets = [...specReferences, ...specRuleTargets];
    
    // Check if any changed files match this spec's references
    const affectedFiles = changedFiles.filter(f =>
      allTargets.some(target => minimatch(f, target))
    );
    
    if (affectedFiles.length > 0) {
      staleSpecs.push({
        specId: spec.data.specId,
        specFile: spec.path,
        affectedFiles,
        lastUpdated: spec.data.updated,
        confidence: affectedFiles.length / Math.max(allTargets.length, 1)
      });
    }
  }
  
  return staleSpecs;
}
Called from post-tool-use.js: After each tool call that modifies a file, check if the modified file is referenced by any spec. If yes, append to a spec-staleness-tracker.json:


{
  "potentially_stale": [
    {
      "specId": "SPEC:auth-001",
      "triggeredBy": "src/lib/auth.ts",
      "changedAt": "2026-03-20T10:30:00Z",
      "specLastUpdated": "2026-03-15",
      "reviewed": false
    }
  ]
}
This tracker is:

Read by doc-freshness gate for age-based checks
Included in handoff Section 4b (environment gaps) as "specs needing review"
Surfaced by /tw:docs-health command
Cleared when user runs /tw:specs review and confirms specs are current
Mechanism 4: /tw:docs-health Command
A dashboard command that shows the health of all docs/specs:


# Documentation Health Report

## Spec Freshness

| Spec | Last Updated | Files Referenced | Files Changed Since | Status |
|------|-------------|-----------------|-------------------|--------|
| SPEC:auth-001 | 2026-03-15 | 4 | 2 (50%) | ⚠ Review needed |
| SPEC:be-002 | 2026-03-18 | 3 | 0 (0%) | ✅ Fresh |
| SPEC:fe-001 | 2026-02-28 | 8 | 6 (75%) | ❌ Likely stale |

## Reference Integrity

| Issue | Spec | Reference | Status |
|-------|------|-----------|--------|
| Dead file reference | SPEC:auth-001 | src/lib/old-auth.ts | ❌ File not found |
| Dead spec reference | SPEC:fe-001 | SPEC:removed-001 | ❌ Spec ID not found |

## Rule Target Coverage

| Spec | Rule | Glob | Matches |
|------|------|------|---------|
| SPEC:auth-001 | grep_must_exist | src/lib/auth*.ts | 2 files ✅ |
| SPEC:arch-001 | import_boundary | src/services/** | 12 files ✅ |
| SPEC:old-001 | naming_pattern | src/legacy/** | 0 files ⚠ |

## Cross-Spec Consistency

No contradictions found. ✅

## Recommendations
1. Review SPEC:auth-001 — 2 referenced files changed since last update
2. Fix dead reference in SPEC:auth-001 — src/lib/old-auth.ts no longer exists
3. Remove or update SPEC:old-001 — rule target matches no files
How It All Connects

Code changes (during execution)
    │
    ▼
post-tool-use.js
    │ Track: which specs reference the changed files?
    │ Write to spec-staleness-tracker.json
    │
    ▼
Ralph Loop (subagent-stop.js)
    │ Gate: doc-freshness
    │   ├── Dead file references → BLOCKING (spec is wrong)
    │   ├── Dead spec cross-references → BLOCKING
    │   ├── Dead library references → WARNING
    │   ├── Empty rule targets → WARNING
    │   └── Age staleness → WARNING
    │
    ▼
Wave completes → Entropy Collector
    │ New category 7: Spec Staleness
    │ "Wave changed src/lib/auth.ts — SPEC:auth-001 may need review"
    │ severity: warning, queued for review
    │
    ▼
Verify-phase
    │ Doc-freshness gate runs again (thorough check)
    │ Report includes spec health alongside requirements
    │
    ▼
Handoff
    │ Section 4b: "Specs needing review: SPEC:auth-001 (2 files changed since update)"
    │
    ▼
Next session / User action
    │ /tw:docs-health → full dashboard
    │ /tw:specs review → mark specs as reviewed, update timestamps
    │ Gap aggregation: recurring staleness → auto-propose spec update
Remediation in Ralph Loop
When doc-freshness fails with a blocking violation (dead reference), the remediation block guides the agent:


{
  primary_violation: "SPEC:auth-001 references src/lib/old-auth.ts but file doesn't exist",
  relevant_spec: "SPEC:auth-001 | backend/jwt-best-practices",
  fix_template: "Update spec SPEC:auth-001: replace reference to src/lib/old-auth.ts with the correct current file path. Check if src/lib/auth.ts or src/lib/authentication.ts exists.",
  learning_signal: "doc-freshness:dead_reference:SPEC:auth-001"
}
The agent can fix spec references during execution — this is a legitimate auto-fix because the file just moved, the pattern is the same.

Files Impact
File	Action	Description
lib/doc-freshness.js	Create	Reference integrity checker, age staleness detector, cross-spec consistency validator
lib/quality-gate.js	Modify	Add runDocFreshness() to gate sequence, extend buildRemediationBlock() for doc-freshness violations
lib/spec-engine.js	Modify	Add trackSpecStaleness(), extractFileReferences(), extractSpecReferences()
hooks/post-tool-use.js	Modify	Track file changes against spec references → write spec-staleness-tracker.json
hooks/subagent-stop.js	Modify	Include doc-freshness in gate remediation
templates/agents/tw-entropy-collector.md	Modify	Add category 7: Spec Staleness
templates/commands/tw-docs-health.md	Create	/tw:docs-health dashboard command
tests/unit/doc-freshness.test.js	Create	Dead references, cross-references, library references, age staleness, rule targets
tests/integration/doc-freshness.test.js	Create	Full flow: code change → staleness tracked → gate catches → remediation
Want me to update the discussion document with all of this?

how about the knowledge that learn for implementation / testing, should we maintain as well
Great point. There's a whole category of knowledge that agents discover during implementation and testing that currently falls through the cracks. Let me think about what this looks like.

The Problem: Knowledge That Dies With the Session
During execution, agents learn things that aren't documented anywhere:

What's learned	Where it lives today	What happens next session
"Run generate-types before npm test"	Nowhere — agent figured it out via trial and error	Next agent hits the same error, wastes 2 Ralph Loop retries
"Set NODE_ENV=test or DB tests hit production"	Maybe in a handoff note if human remembers	Next agent may not read the handoff
"Auth middleware silently swallows errors — always check response body"	Decision log if the executor bothered to log it	Decision logs are per-plan, not discoverable
"Tests must run sequentially — they share a test database"	Remediation log (Ralph Loop failure)	Spec proposal at 0.3, takes 3+ sessions to promote
"UserService.findById() throws, doesn't return null — always wrap in try/catch"	Agent's context window, gone after session	Completely lost
"The webhook endpoint needs X-Signature header or returns 401"	Agent discovered during integration testing	Lost unless manually documented
These are all verified, working knowledge — the agent discovered them through actual implementation and testing. They're more reliable than spec proposals from failure patterns because they come from success, not just failure.

Three Categories of Implementation Knowledge
Category A: Setup & Workflow Knowledge
Things you need to know before you can work effectively:

Build order dependencies (generate-types before test)
Required environment variables and their values
Service dependencies (needs Redis running, needs DB seeded)
Special test commands or flags (--runInBand, --forceExit)
Category B: API & Integration Knowledge
Things you learn by actually calling the code:

Function behavior that differs from what you'd expect (throws vs returns null)
Required headers, auth tokens, rate limits
Silent failure modes (middleware swallowing errors)
Undocumented parameters or side effects
Category C: Edge Cases & Gotchas
Things you discover when things break:

Test order dependencies
Race conditions and timing issues
Platform-specific behavior (works on Mac, fails in CI)
Library version quirks
What Threadwork Has Today (And Why It's Not Enough)

Agent discovers knowledge during execution
    │
    ├── Decision log (lib/state.js:appendDecision)
    │     Only captures CHOICES, not DISCOVERIES
    │     Scoped to plan XML — not discoverable by other plans
    │
    ├── Spec proposals (ralph-loop failures)
    │     Only triggered by FAILURE, not by success
    │     Starts at confidence 0.3 — slow to promote
    │     Captures the pattern, not the operational knowledge
    │
    ├── Handoff section 4 (key decisions)
    │     Only auto-populated from <decisions> XML
    │     Human must manually add other knowledge
    │
    ├── Store (cross-session, confidence ≥ 0.7)
    │     Only gets there after long promotion pipeline
    │     Designed for patterns, not operational notes
    │
    └── Lost forever
          Most implementation knowledge goes here
The fundamental gap: there's no way for an agent to say "I learned something useful" and have it captured in a format that future agents will see.

Proposed: Implementation Knowledge Capture
Mechanism 1: Agent Discovery Protocol
Add a protocol to the executor template where agents can flag discovered knowledge during execution. New virtual tool knowledge_note:


<!-- In templates/agents/tw-executor.md — new section -->

## Discovery Protocol

During implementation, you will discover things about the codebase that aren't documented.
When you discover something that would help future agents working in this area, emit a
knowledge note:

Call the `knowledge_note` tool with:
- **category**: setup | api | edge_case | testing | workflow
- **scope**: file path, directory, or module this applies to
- **summary**: one sentence — what you learned
- **evidence**: how you verified this (test passed, error resolved, etc.)
- **critical**: true if ignoring this will cause failures

Examples:
- category: setup, scope: "tests/", summary: "Tests must run with --runInBand flag due to shared DB",
  evidence: "Parallel run caused test failures, sequential run passes", critical: true
- category: api, scope: "src/lib/auth.ts", summary: "UserService.findById() throws NotFoundError instead of returning null",
  evidence: "Caught during T-1-2-3 implementation, wrapped in try/catch", critical: false
- category: testing, scope: "src/services/webhook.ts", summary: "Webhook endpoint requires X-Signature header computed as HMAC-SHA256 of body",
  evidence: "Discovered via 401 response during integration test", critical: true
Intercepted by pre-tool-use.js: The knowledge_note tool is a virtual tool (like spec_fetch). When an agent calls it, the hook:

Writes the note to .threadwork/state/knowledge-notes.json
If critical: true, immediately adds to the routing map for other agents in this session
After the session, promotes notes to spec proposals

// In hooks/pre-tool-use.js — handle knowledge_note interception
if (toolName === 'knowledge_note') {
  const note = {
    id: `KN-${Date.now()}`,
    category: payload.category,
    scope: payload.scope,
    summary: payload.summary,
    evidence: payload.evidence,
    critical: payload.critical ?? false,
    discoveredBy: agentName,
    taskId: currentTaskId,
    timestamp: new Date().toISOString(),
    verified: true  // agent verified it during implementation
  };
  
  appendKnowledgeNote(note);
  
  // If critical, inject into routing map for remaining agents this session
  if (note.critical) {
    addToSessionContext(note);
  }
  
  return { intercept: true, result: `Knowledge note ${note.id} recorded.` };
}
Mechanism 2: Knowledge Note Lifecycle
Notes follow a lifecycle similar to spec proposals but faster, because they come from verified execution:


Agent discovers something → knowledge_note tool call
    │
    ▼
knowledge-notes.json (session-scoped)
    │ critical notes → injected into same-session agents immediately
    │
    ▼
Session end → handoff
    │ Section 4c: "Implementation Knowledge Discovered"
    │ Lists all notes with category, scope, summary
    │
    ▼
Next session → session-start.js
    │ Inject critical notes from previous session alongside Store entries
    │ "⚠ Previous session learned: Tests must run with --runInBand"
    │
    ▼
Promotion evaluation (automatic)
    │ Notes with matching scope that survive 2+ sessions without contradiction
    │ → Promoted to spec proposal at confidence 0.5
    │ → If scope matches existing spec, propose amendment rather than new spec
    │
    ▼
Spec system
    │ Goes through normal proposal → acceptance → enforcement pipeline
    │ Knowledge note becomes permanent, maintained, freshness-checked
Mechanism 3: Knowledge Notes in Routing Map
When building the routing map for an agent, include relevant knowledge notes alongside specs:


── SPEC ROUTING MAP ─────────────────────────────────
Task context: implement webhook handler...
Available specs (fetch by ID when needed):
  [SPEC:be-002]  backend/api-design      — API endpoint patterns
  [SPEC:test-001] testing/standards       — Unit test structure

Implementation notes for this scope:
  ⚠ [KN-critical] src/services/webhook.ts — Webhook endpoint requires X-Signature
    header computed as HMAC-SHA256 of body (verified in previous session)
  [KN] tests/ — Tests must run with --runInBand due to shared DB
─────────────────────────────────────────────────────
Critical notes are always shown. Non-critical notes shown only when their scope matches the task's file targets.

Mechanism 4: Knowledge Note Freshness
Knowledge notes are subject to the same doc-freshness system (Part 10 in our document):

Note references src/lib/auth.ts → if that file is deleted/renamed, note is flagged as stale
Note says "use --runInBand" → if test runner changes (e.g., jest → vitest), note may be stale
Note scope matches files with high churn → periodically re-verify

// In lib/doc-freshness.js — extend to check knowledge notes
export function checkKnowledgeNoteFreshness(notes, projectRoot) {
  const stale = [];
  
  for (const note of notes) {
    // Check if scope still exists
    const scopeExists = existsSync(join(projectRoot, note.scope)) ||
      resolveGlob(note.scope, projectRoot).length > 0;
    
    if (!scopeExists) {
      stale.push({
        noteId: note.id,
        reason: `Scope "${note.scope}" no longer exists`,
        action: 'remove'
      });
      continue;
    }
    
    // Check if scope files changed since note was created
    const scopeFiles = resolveGlob(note.scope + '/**', projectRoot);
    const changedSince = scopeFiles.filter(f => {
      const lastMod = getFileLastModifiedFromGit(f, projectRoot);
      return lastMod > new Date(note.timestamp);
    });
    
    if (changedSince.length > 0 && note.category === 'api') {
      // API knowledge is particularly sensitive to code changes
      stale.push({
        noteId: note.id,
        reason: `${changedSince.length} files in scope changed since note was created`,
        action: 'reverify'
      });
    }
  }
  
  return stale;
}
Stale note actions:

remove: Scope no longer exists → delete the note
reverify: Code changed → mark as verified: false, agent must re-verify next time it works in that scope
Unverified notes show with a ⚠ unverified marker in the routing map
Mechanism 5: Handoff Section 4c
Add to the handoff document between Section 4b (Environment Gaps) and Section 5 (Files Modified):


## 4c. Implementation Knowledge Discovered

### Critical (injected into future sessions automatically)
- [KN-1711234567] **tests/** — Tests must run with --runInBand flag due to shared DB
  Discovered by: tw-executor during T-1-2-3 | Evidence: parallel run failures resolved
  
- [KN-1711234890] **src/services/webhook.ts** — Webhook requires X-Signature header (HMAC-SHA256)
  Discovered by: tw-executor during T-1-3-1 | Evidence: 401 → 200 after adding header

### Non-Critical
- [KN-1711235000] **src/lib/auth.ts** — UserService.findById() throws, doesn't return null
  Discovered by: tw-executor during T-1-2-1 | Evidence: caught during try/catch wrapping
Mechanism 6: Automatic Promotion to Specs
Knowledge notes that prove durable get promoted to the spec system:


// In lib/spec-engine.js — new function
export function promoteKnowledgeNotes(notes, allSpecs) {
  const promotable = notes.filter(n =>
    n.verified &&
    n.sessionsSurvived >= 2 &&  // survived 2+ sessions without contradiction
    !n.promoted
  );
  
  for (const note of promotable) {
    // Find if an existing spec covers this scope
    const existingSpec = allSpecs.find(s =>
      extractFileReferences(s.content).some(ref =>
        note.scope.includes(ref) || ref.includes(note.scope)
      )
    );
    
    if (existingSpec) {
      // Propose amendment to existing spec
      proposeSpecUpdate(
        existingSpec.data.specId,
        appendKnowledgeToSpec(existingSpec, note),
        `Implementation knowledge: ${note.summary}`,
        {
          source: 'knowledge-note',
          initialConfidence: 0.5,
          noteId: note.id
        }
      );
    } else {
      // Propose new spec for this scope
      const domain = inferDomain(note.scope);
      proposeSpecUpdate(
        `knowledge/${domain}/${scopeToName(note.scope)}`,
        generateSpecFromNotes(notes.filter(n => n.scope === note.scope)),
        `Discovered knowledge for ${note.scope}`,
        {
          source: 'knowledge-note',
          initialConfidence: 0.5,
          noteId: note.id
        }
      );
    }
    
    note.promoted = true;
    note.promotedAt = new Date().toISOString();
  }
}
The full lifecycle:


Agent discovers knowledge during execution
    │ knowledge_note tool call
    ▼
knowledge-notes.json
    │ critical → inject into same-session agents
    │ all → included in handoff Section 4c
    ▼
Next session
    │ critical notes injected at session-start
    │ relevant notes in routing map
    ▼
Survives 2+ sessions without contradiction
    │ auto-promote to spec proposal at confidence 0.5
    │ amend existing spec or create new one
    ▼
Spec system
    │ normal proposal → acceptance → enforcement pipeline
    │ subject to doc-freshness checks
    ▼
Permanent, maintained, freshness-checked knowledge
How This Connects to Doc-Freshness
Knowledge notes plug into the doc-freshness system we just designed:

Doc-freshness gate checks notes for stale scopes alongside specs
Entropy collector category 7 (spec staleness) also checks if knowledge notes contradict the current code
/tw:docs-health shows knowledge note health alongside spec health
Gap aggregation (Part 8.3) counts knowledge notes as "resolved gaps" when they cover previously-detected gap areas
The key insight: knowledge notes are the bridge between "agent learned something" and "the harness knows it permanently." They're faster than spec proposals (immediate injection for critical notes) but still flow into the same long-term maintenance system.

Files Impact (Incremental)
File	Action	Description
lib/knowledge-notes.js	Create	Note CRUD, promotion logic, freshness checking, routing map integration
hooks/pre-tool-use.js	Modify	Intercept knowledge_note virtual tool, inject critical notes into routing map
hooks/session-start.js	Modify	Inject critical knowledge notes from previous sessions
hooks/post-tool-use.js	Modify	Track note freshness when files change
lib/doc-freshness.js	Modify	Add checkKnowledgeNoteFreshness()
lib/spec-engine.js	Modify	Add promoteKnowledgeNotes(), include notes in routing map building
lib/handoff.js	Modify	Add Section 4c (Implementation Knowledge Discovered)
templates/agents/tw-executor.md	Modify	Add Discovery Protocol section with knowledge_note tool usage
templates/agents/tw-debugger.md	Modify	Add Discovery Protocol (debugger discovers edge cases)
templates/commands/tw-docs-health.md	Modify	Include knowledge note health in dashboard
tests/unit/knowledge-notes.test.js	Create	Note lifecycle, promotion, freshness
tests/integration/knowledge-capture.test.js	Create	End-to-end: discovery → capture → inject → promote
Want me to update the discussion document with both the doc-freshness enforcement (Part 10) and the knowledge note system (Part 11)?

can you list the doc file format that we use in the Threadwork?
Agent:Explore Threadwork doc formats
IN
I need to catalog every documentation/state file format used in the Threadwork project. For each file, I need: the file path pattern, the format (JSON, markdown, YAML frontmatter, XML, etc.), and the key fields/structure.

Please explore these areas thoroughly:

1. **State files** in `.threadwork/state/` — project.json, checkpoint.json, token-log.json, model-switch-log.json, active-task.json, completed-tasks.json, completed-plans.json, quality-config.json, ralph-state.json, hook-log.json, execution-log.json, team-session.json, gap-report.json, spec-staleness-tracker.json
2. **Spec files** in `.threadwork/specs/` — look at the frontmatter format, what fields are used (domain, specId, name, tags, confidence, updated, taste_invariant, rules, etc.)
3. **Plan files** in `.threadwork/state/phases/phase-N/plans/` — the XML format with tasks, verification, done-condition, decisions, constraints
4. **Handoff files** in `.threadwork/workspace/handoffs/` — the markdown format with 10 sections
5. **Entropy reports** in `.threadwork/state/phases/phase-N/` — JSON format
6. **Proposal files** in `.threadwork/specs/proposals/` — frontmatter format
7. **Store entries** in `~/.threadwork/store/` — format + store-index.json
8. **Journal files** in `.threadwork/workspace/journals/`
9. **CONTEXT.md** files in `.threadwork/state/phases/phase-N/`
10. **PROJECT.md, REQUIREMENTS.md, ROADMAP.md** in `.threadwork/state/`
11. **SUMMARY.md** and **VERIFICATION.md** and **UAT.md** per phase
12. **Templates** — look at templates/specs/ for starter spec formats, templates/agents/ for agent prompt format, templates/commands/ for command format

Read the actual source code in lib/ and hooks/ to find where these files are created/read to understand their exact schema. Also check install/init.js for initial scaffolding formats and install/update.js for migration formats.

Give me a comprehensive catalog with exact field names and example values where possible.
Bash
IN
find /Users/george.li/Documents/nexora/Threadwork -type f -name "*.js" | grep -E "(lib|hooks|install)" | head -20

OUT
/Users/george.li/Documents/nexora/Threadwork/install/update.js
/Users/george.li/Documents/nexora/Threadwork/install/codex.js
/Users/george.li/Documents/nexora/Threadwork/install/init.js
/Users/george.li/Documents/nexora/Threadwork/install/status.js
/Users/george.li/Documents/nexora/Threadwork/install/claude-code.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/stringify.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/engines.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/to-file.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/excerpt.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/parse.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/utils.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/engine.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/gray-matter/lib/defaults.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/commander/lib/argument.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/commander/lib/option.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/commander/lib/error.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/commander/lib/command.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/commander/lib/help.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/commander/lib/suggestSimilar.js
/Users/george.li/Documents/nexora/Threadwork/node_modules/argparse/lib/action.js
Bash
IN
ls -la /Users/george.li/Documents/nexora/Threadwork/lib/ | head -40

OUT
total 336
drwxr-xr-x@ 16 george.li  staff    512 Mar 11 10:37 .
drwxr-xr-x@ 20 george.li  staff    640 Mar 12 11:24 ..
-rw-r--r--@  1 george.li  staff  18434 Mar  5 13:01 blueprint-diff.js
-rw-r--r--@  1 george.li  staff   7826 Mar  3 22:40 entropy-collector.js
-rw-r--r--@  1 george.li  staff   3674 Mar  3 10:05 git.js
-rw-r--r--@  1 george.li  staff  12351 Mar  5 13:01 handoff.js
-rw-r--r--@  1 george.li  staff   5803 Mar  3 10:05 journal.js
-rw-r--r--@  1 george.li  staff   7946 Mar  5 13:01 model-switcher.js
-rw-r--r--@  1 george.li  staff  14883 Mar  3 22:40 quality-gate.js
-rw-r--r--@  1 george.li  staff   3940 Mar  3 10:05 runtime.js
-rw-r--r--@  1 george.li  staff   5984 Mar  3 10:05 skill-tier.js
-rw-r--r--@  1 george.li  staff  17719 Mar  3 22:40 spec-engine.js
-rw-r--r--@  1 george.li  staff  16107 Mar  3 22:40 state.js
-rw-r--r--@  1 george.li  staff  11207 Mar  3 22:40 store.js
-rw-r--r--@  1 george.li  staff   9498 Mar  3 10:05 team-coordinator.js
-rw-r--r--@  1 george.li  staff  15292 Mar 11 10:37 token-tracker.js
Bash
IN
ls -la /Users/george.li/Documents/nexora/Threadwork/hooks/ | head -20

OUT
total 104
drwxr-xr-x@  7 george.li  staff   224 Mar 12 11:19 .
drwxr-xr-x@ 20 george.li  staff   640 Mar 12 11:24 ..
-rw-r--r--@  1 george.li  staff  9108 Mar 12 11:19 post-tool-use.js
-rw-r--r--@  1 george.li  staff  9012 Mar 12 11:19 pre-tool-use.js
-rw-r--r--@  1 george.li  staff  6542 Mar 12 11:19 session-start.js
-rw-r--r--@  1 george.li  staff  9497 Mar 12 11:19 subagent-stop.js
-rw-r--r--@  1 george.li  staff  4679 Mar  3 10:05 test-harness.js
Read state.js
Read handoff.js
Read store.js
Read spec-engine.js
Read token-tracker.js
Read init.js
Read entropy-collector.js
Read journal.js
Bash
IN
find /Users/george.li/Documents/nexora/Threadwork/templates -type f | head -30

OUT
/Users/george.li/Documents/nexora/Threadwork/templates/specs/frontend/react-patterns.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/frontend/styling.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/backend/api-design.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/backend/auth.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/testing/testing-standards.md
/Users/george.li/Documents/nexora/Threadwork/templates/specs/index.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-spec-writer.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-debugger.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-executor.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-verifier.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-plan-checker.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-planner.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-researcher.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-entropy-collector.md
/Users/george.li/Documents/nexora/Threadwork/templates/agents/tw-dispatch.md
/Users/george.li/Documents/nexora/Threadwork/templates/pricing.json
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-status.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-estimate.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-recover.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-model.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-discuss-phase.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-entropy.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-execute-phase.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-audit-milestone.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-parallel.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-clear.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-plan-phase.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-handoff.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-tokens.md
/Users/george.li/Documents/nexora/Threadwork/templates/commands/tw-tier.md
Read react-patterns.md
Read tw-executor.md
Bash
IN
find /Users/george.li/Documents/nexora/Threadwork/.threadwork -type f -name "*.xml" 2>/dev/null | head -5

OUT
(Bash completed with no output)
Bash
IN
grep -r "PLAN-" /Users/george.li/Documents/nexora/Threadwork/tests --include="*.js" | head -10

OUT
/Users/george.li/Documents/nexora/Threadwork/tests/unit/team-coordinator.test.js:  it('maps PLAN-1-2 to tw-executor-plan-1-2', () => {
/Users/george.li/Documents/nexora/Threadwork/tests/unit/team-coordinator.test.js:    const result = mod.getWorkerNamesForWave(['PLAN-1-2']);
/Users/george.li/Documents/nexora/Threadwork/tests/unit/team-coordinator.test.js:    const result = mod.getWorkerNamesForWave(['PLAN-2-1', 'PLAN-2-3', 'PLAN-2-5']);
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:const planXml = `<plan id="PLAN-1-1" phase="1" milestone="1">
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:const planPath = join(tmpDir, '.threadwork', 'state', 'phases', 'phase-1', 'plans', 'PLAN-1-1.xml');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:  join(tmpDir, '.threadwork', 'state', 'phases', 'phase-1', 'plans', 'PLAN-1-0.xml'),
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:  `<plan id="PLAN-1-0"><title>Old Plan</title><tasks></tasks></plan>`,
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:    appendDecision('PLAN-1-1', 'T-1-1-1', {
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:    appendDecision('PLAN-1-1', 'T-1-1-1', {
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:    const decisions = readDecisions('PLAN-1-1');
Read state-decisions.test.js
Read quality-gate.js
Read model-switcher.js
Read pricing.json
Read token-tracker.js (lines 201-350)
Read tw-execute-phase.md
Bash
IN
grep -r "<plan" /Users/george.li/Documents/nexora/Threadwork/tests --include="*.js" -A 20 | head -80

OUT
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:const planXml = `<plan id="PLAN-1-1" phase="1" milestone="1">
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-  <title>Test Plan</title>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-  <tasks>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    <task id="T-1-1-1">
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      <description>Test task</description>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      <files>src/test.ts</files>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      <verification>tsc --noEmit passes</verification>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      <done-condition>Tests pass</done-condition>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      <status>IN_PROGRESS</status>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    </task>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-  </tasks>
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-</plan>`;
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-const planPath = join(tmpDir, '.threadwork', 'state', 'phases', 'phase-1', 'plans', 'PLAN-1-1.xml');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-writeFileSync(planPath, planXml, 'utf8');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-// Also write a v0.1.x plan (no <decisions> block, no project.json needed)
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-writeFileSync(
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-  join(tmpDir, '.threadwork', 'state', 'phases', 'phase-1', 'plans', 'PLAN-1-0.xml'),
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js:  `<plan id="PLAN-1-0"><title>Old Plan</title><tasks></tasks></plan>`,
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-  'utf8'
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-);
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-// Import state module
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-const { appendDecision, readDecisions, readSessionDecisions } = await import('../../lib/state.js');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-describe('appendDecision', () => {
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-  test('adds well-formed <decision> element to plan XML', () => {
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    appendDecision('PLAN-1-1', 'T-1-1-1', {
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      choice: 'Used RS256 instead of HS256',
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      rationale: 'External services need public-key verification.',
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-      alternativesConsidered: 'HS256 (rejected: secret sharing required)'
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    });
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    const updated = readFileSync(planPath, 'utf8');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    assert.ok(updated.includes('<decisions>'), 'Should have <decisions> block');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    assert.ok(updated.includes('<decision task="T-1-1-1"'), 'Should reference task T-1-1-1');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    assert.ok(updated.includes('RS256'), 'Should contain choice text');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    assert.ok(updated.includes('External services'), 'Should contain rationale');
/Users/george.li/Documents/nexora/Threadwork/tests/unit/state-decisions.test.js-    assert.ok(updated.includes('HS256'), 'Should contain alternatives');
Bash
IN
grep -r "execution-log" /Users/george.li/Documents/nexora/Threadwork/lib --include="*.js" -B 3 -A 3 | head -40

OUT
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-/**
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js- * Check if all tasks in the current wave are DONE or SKIPPED.
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js: * Reads the execution-log.json for the given phase.
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js- *
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js: * @param {object} executionLog - Parsed execution-log.json content
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js- * @returns {boolean}
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js- */
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-export function isWaveComplete(executionLog) {
--
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js- * @returns {object|null}
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js- */
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-export function readExecutionLog(phaseId) {
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js:  const logPath = join(phaseDir(phaseId), 'execution-log.json');
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-  if (!existsSync(logPath)) return null;
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-  try { return JSON.parse(readFileSync(logPath, 'utf8')); } catch { return null; }
/Users/george.li/Documents/nexora/Threadwork/lib/entropy-collector.js-}
Read post-tool-use.js
Bash
IN
grep -r "hook-log" /Users/george.li/Documents/nexora/Threadwork/lib --include="*.js" -A 2 | head -20

OUT
(Bash completed with no output)
Here's the complete catalog of every doc/state file format in Threadwork:

File Format Catalog
JSON State Files (.threadwork/state/)
File	Key Fields	Written By
project.json	projectName, currentPhase, skillTier, sessionBudget, cost_budget, model_switch_policy, teamMode	init.js
token-log.json	sessionBudget, sessionUsed, sessionCostUsed, tasks[], spec_fetch_tokens, spec_fetch_log[]	token-tracker.js
checkpoint.json	phase, milestone, activeTask, planId, branch, lastSha, tokenBudgetRemaining	handoff.js, state.js
active-task.json	taskId, planId, startedAt	state.js
completed-tasks.json	tasks[]{id, completedAt}	state.js
completed-plans.json	plans[]{id, completedAt}	state.js
quality-config.json	typecheck, lint, tests, build, security — each {enabled, blocking}	init.js
model-switch-log.json	switches[]{task_id, from, to, reason, policy}	model-switcher.js
team-session.json	teamName, phaseId, waveId, plans[], workers{}	post-tool-use.js
hook-log.json	NDJSON: {timestamp, level, hook, message} per line	all hooks
.gate-cache.json	{commit_sha}: {typecheck, lint, tests, build, security}	quality-gate.js
Per-Phase Files (.threadwork/state/phases/phase-N/)
File	Format	Key Fields
execution-log.json	JSON	phase, tasks[]{id, status, startedAt, completedAt}, waves{startSha, endSha}
entropy-report-wave-W.json	JSON	wave, phase, issues[]{type, severity, file, description, auto_fix}, auto_fixed, queued_for_next_wave
CONTEXT.md	Markdown	Library decisions, patterns, constraints, risks, out of scope
SUMMARY.md	Markdown	Plan execution summary per executor
VERIFICATION.md	Markdown	Requirements table, quality gates, integration notes
UAT.md	Markdown	Manual verification steps
plans/PLAN-N-M.xml	XML	<plan> with <tasks>, <decisions>, <constraints>
Spec System (.threadwork/specs/)
File	Format	Key Frontmatter
{domain}/{name}.md	Markdown + gray-matter	domain, specId, name, tags[], confidence, updated, taste_invariant
proposals/{id}.md	Markdown + gray-matter	proposalId, specName, reason, confidence, source, learningSignal, promoted
index.md	Markdown	Auto-generated spec index
Workspace (.threadwork/workspace/)
File	Format	Structure
handoffs/YYYY-MM-DD-N.md	Markdown	10 sections: Overview, Completed, InProgress, Decisions, Files, Tokens, Git, QA, Action, Resume
journals/YYYY-MM-DD-N.md	Markdown	Phase, Completed, InProgress, Decisions, Files, Tokens, Next Action
Global (~/.threadwork/)
File	Format	Key Fields
pricing.json	JSON	models{haiku, sonnet, opus}{input, output}
store/store-index.json	JSON	entries[]{entryId, domain, key, confidence, tags, filename}
store/{domain}/{date}-{name}.md	Markdown + gray-matter	domain, key, entryId, confidence, tags, projects[], source
Templates (source repo, copied on init)
File	Format	Frontmatter
templates/agents/tw-*.md	Markdown + YAML	name, description, model, allowed-tools
templates/commands/tw-*.md	Markdown + YAML	name, description, argument-hint, allowed-tools
Markdown Files (.threadwork/state/)
File	Format
PROJECT.md	Free-form project vision and principles
REQUIREMENTS.md	Bullet list: - REQ-NNN: description
ROADMAP.md	Heading hierarchy: ## Milestone N → ### Phase N
Now — back to your question about maintaining implementation/testing knowledge and doc freshness. Looking at this catalog, the key observation is:

Knowledge lives in too many places, and none of them are freshness-checked:

Specs have updated dates but nothing validates them
CONTEXT.md has no timestamp or reference tracking
Plan <decisions> are per-plan, not discoverable across plans
Store entries have confidence but no staleness mechanism
Handoffs capture knowledge but it's write-once, never maintained
No file format currently supports "implementation discovery" notes
This reinforces the need for both the doc-freshness gate (mechanical validation of references) and the knowledge note system (capturing what agents learn). Want me to update the discussion document with the doc-freshness enforcement (Part 10) and knowledge notes (Part 11) now?

thanks, please update the discussion document with both the doc-freshness enforcement (Part 10) and the knowledge note system (Part 11)
Read harness_engineering.md (from line 1541)
Now let me replace Part 10 (the old manifest) with the new Part 10 (doc-freshness), Part 11 (knowledge notes), and Part 12 (updated manifest).

Edit harness_engineering.md
Added 788 lines
Done. The document now has 12 parts:

9 Core Problems — gap mapping + scorecard
Threadwork's Advantages — 5 areas ahead
Advisory vs Mechanical Enforcement — philosophical divide
Mechanical Enforcement Plan — spec rules, structural tests, discuss-phase, binding constraints
Failure as Signal — "retry harder" vs "fix the harness"
Failure-as-Signal Implementation — classifier, fast-track proposals, gap reporting
How Everything Connects — pipeline diagram
Capability Gap Detection — readiness audit, pre-execution scan, aggregation
Runtime QA — smoke test, endpoint verification, verification profiles, manual feedback
Doc-Freshness Enforcement — mechanical gate for reference integrity, staleness tracking, entropy collector category 7, /tw:docs-health
Implementation Knowledge Capture — knowledge_note virtual tool, discovery protocol, lifecycle (capture → inject → promote → spec), freshness checking, handoff Section 4c
Complete Files Manifest — 18 new files, 21 modified, 14 test files
okay, let's discuss the Problem 4: Agent Legibility vs Human Readability,  do we discuss this before?
Yes, we covered it briefly in Part 1 of the document. Here's what we said:

Current status: Partially Addressed

Spec system + Store make project knowledge accessible in-context
Everything persists to .threadwork/ — no state in env vars or human memory
Handoffs are self-contained (Section 10 has full resume prompt)
Gap: Threadwork doesn't guide users on making their codebase agent-legible. No tooling to audit agent comprehension factors (dependency transparency, in-repo docs completeness, opaque library detection). More of a consulting/guidance gap than a tooling gap.

We rated it Low priority.

But here's the thing — we've actually addressed a significant chunk of this gap through Parts 8-11 without explicitly calling it out:

OpenAI Principle	Threadwork Coverage (After Our Discussions)
"Anything not in-context doesn't exist"	Part 8: Readiness audit detects undocumented patterns, utility dirs, import aliases, env vars
"Treat repo as agent's onboarding doc"	Part 11: Knowledge notes capture tribal knowledge and inject into routing map
"Favor boring tech, reimplement over opaque deps"	Not addressed — this is a user choice, not harness-enforceable
"Ensure all dependencies can be reasoned about in-repo"	Part 10: Doc-freshness detects dead library references; Part 8: flags undocumented libraries
"Optimize for agent legibility first"	Part 4.4: Discuss-phase Q6-9 capture architectural rules; Part 9.3: Verification profiles document how to validate
The main remaining gap is the "favor boring tech / reimplement over opaque deps" principle. That's a design philosophy, not something a harness can enforce mechanically. OpenAI could do it because they controlled the codebase from day zero — they chose to reimplement p-limit as a custom map-with-concurrency helper so agents could reason about it fully in-repo.

The question is: should Threadwork do anything more here, or is the readiness audit (Part 8) + knowledge notes (Part 11) + doc-freshness (Part 10) already sufficient coverage?

A few things we could add:

Opaque dependency detection in the readiness audit — flag libraries with no TypeScript types, no JSDoc, or that wrap native modules (these are hard for agents to reason about)
Agent legibility score as part of /tw:readiness — rate directories by how agent-friendly they are (has README? has types? has specs? has knowledge notes?)
Discuss-phase guidance — when setting up a new project, suggest "agent-legible" practices as part of the onboarding
But honestly, these feel like diminishing returns compared to Parts 8-11. What's your take — is there something specific about agent legibility you want to address, or should we move on to the remaining problems (5-9)?

we can leave the problem 4 and review it once all problem be discuss,  now let's go with Problem 5: Architectural Coherence Without Manual Enforcement,  have we discuss problem 5 before
Yes — Problem 5 is actually the one we discussed most thoroughly. It was the first deep dive we did and it became Part 4 of the document (Mechanical Enforcement Plan).

Here's a summary of what we already designed:

Component	Part	Status
Spec rules engine (5 rule types in frontmatter)	4.1	Designed with code
Structural test gate (.threadwork/structural-tests/)	4.2	Designed with code
Spec compliance quality gate (runSpecCompliance())	4.3	Designed with code
Enhanced discuss-phase (Q6: arch boundaries, Q7: forbidden patterns, Q8: naming)	4.4	Designed
Binding constraints (plan-checker → <constraints> XML → executor)	4.5	Designed with code
Enhanced verifier (spec rule compliance + review criteria)	4.6	Designed
Plus the failure classifier (Part 6.1) has an architectural_violation category that auto-proposes spec rules when agents break undocumented boundaries.

And the readiness audit (Part 8.1) detects monorepo/workspace structures without boundary specs.

I think Problem 5 is our most complete coverage. The one thing we haven't discussed that OpenAI specifically calls out is:

"Code can only depend 'forward' through layers (Types → Config → Repo → Service → Runtime → UI). Cross-cutting concerns enter through a single interface: Providers."

Our import_boundary rule type handles this, but we haven't discussed whether Threadwork should offer a layered architecture template — a pre-built set of import boundary rules that users can adopt during discuss-phase. Something like:


Threadwork: "Does your project follow a layered architecture?"
  [Yes — define layers]  [No — skip]

User: "Yes"

Threadwork: "Define your layers in dependency order (each can only import from layers above it):
  1. types
  2. config  
  3. repo
  4. services
  5. ui

Generating import_boundary rules for all 10 directional constraints..."
But this might be over-engineering — the discuss-phase Q6 already asks about architectural boundaries.

Want to move on to Problem 6 (Garbage Collection), or is there something about architectural enforcement you want to explore further?

yes, let's move on to problem 6
Problem 6 (Technical Debt / Garbage Collection) — we rated this Well Implemented with a Medium priority gap. Here's where we stand:

What OpenAI does:

Encode "golden principles" as mechanical rules
Run background Codex tasks on regular cadence (not just post-wave)
Scan deviations, update quality grades, open targeted refactoring PRs
Automerge low-risk cleanup
Quote: "Technical debt is like a high-interest loan — pay it down continuously"
What Threadwork has:

Entropy Collector — background agent scanning wave diffs across 6 categories (naming drift, import violations, orphaned artifacts, doc staleness, inconsistent error handling, duplicate logic)
Minor issues get auto-fix commits (chore: [entropy-collector] <desc>)
Warning issues queue for next wave
The gaps we identified:

Gap	Description
Only post-wave	Entropy collector runs after wave completion, not on a regular cadence
No quality grade tracking	No trend data — is the codebase getting better or worse over time?
No automerge for PRs	Auto-fix commits are made but no PR workflow for larger cleanups
No scheduled/periodic runs	Can't run independently of the phase workflow
But honestly — with everything we've designed in Parts 4, 8, 10, and 11, most of these gaps are now partially covered by other systems:

Spec rules (Part 4) mechanically prevent many categories of debt from being introduced
Doc-freshness gate (Part 10) catches documentation staleness mechanically, not just via entropy scan
Readiness audit (Part 8) detects uncovered areas proactively
Knowledge notes (Part 11) capture workarounds that would otherwise become hidden debt
The remaining unique value the entropy collector provides is cross-output drift detection — things that pass individual quality gates but accumulate into problems when viewed together. No other system catches "three different developers used three different naming conventions in the same wave."

The real question is: does Threadwork need to run garbage collection outside the phase workflow? OpenAI runs background tasks on a regular cadence because their agents work continuously. Threadwork is session-based — there's a human starting and ending each session. The cadence is naturally tied to the phase workflow.

I think there are two things worth considering:

1. Quality Trend Tracking
The entropy collector produces reports per wave, but there's no aggregation. Over 10 waves, is naming drift increasing or decreasing? Are import violations getting worse?

This would be a lightweight addition — aggregate entropy reports into a trend file:


{
  "trends": {
    "naming_drift": { "wave_1": 3, "wave_2": 1, "wave_3": 0 },
    "import_violations": { "wave_1": 0, "wave_2": 2, "wave_3": 4 },
    "duplicate_logic": { "wave_1": 1, "wave_2": 1, "wave_3": 2 }
  },
  "overall_direction": "mixed",
  "worsening_categories": ["import_violations", "duplicate_logic"]
}
Surfaced in /tw:entropy and handoff. When a category is trending worse across 3+ waves, auto-propose a spec rule to prevent it mechanically — bridging from advisory (entropy report) to enforcement (spec rule).

2. On-Demand Entropy Scan
Instead of scheduled cadence (which doesn't fit the session model), add a command /tw:entropy scan that runs the entropy collector against the full recent diff (not just wave diff). Users can run it anytime — during code review, before a release, or at session start.

This is much simpler than a scheduled system and fits Threadwork's session-based model. The entropy collector already exists — it just needs a different trigger path.

These are relatively small additions compared to what we've been designing. Do you want me to add these to the document, or do you think Problem 6 is sufficiently covered by the existing entropy collector plus the systems from Parts 4/8/10/11?

yes, please update to discussion document,  also I believe AI will be give more priviledge and do more and more in the future and human may only check the last result, so can threadwork provide the mode that have less human interactions?
Good insight. Let me think about both pieces — the Problem 6 additions are straightforward, but the autonomy mode is a significant design question that touches multiple parts of the system.

Let me read the current document ending to know where to append.

Read harness_engineering.md (lines 2141-2170)
Read harness_engineering.md (from line 2531)
Now let me append Part 13 (Garbage Collection improvements) and Part 14 (Autonomous Mode), then update Part 12 (manifest).

Edit harness_engineering.md
Added 489 lines
Done. Two new parts added:

Part 12: Enhanced Garbage Collection

Quality Trend Tracking — aggregate entropy reports across waves, detect worsening categories, auto-escalate to spec rules when a category trends worse across 3+ data points
On-Demand Entropy Scan — /tw:entropy scan runs the collector against any diff range, not just post-wave
Part 13: Autonomous Operation Mode — three levels of autonomy:

Supervised	Guided	Autonomous
Human touchpoints per phase	~9	~3 (failures only)	~0 (milestone end only)
Discuss-phase	Answer all 10 Qs	Auto-fill, ask only new	Fully auto-fill
Plan approval	Always ask	Auto-approve if checker passes	Auto-approve, auto-fix issues
Ralph Loop retries	5	8	10, then skip-and-log
Spec proposal acceptance	0.7 (human only)	0.6 (auto-accept)	0.5 (auto-accept + auto-rules)
Manual verification	Immediate	Defer to milestone end	Skip entirely
Session chaining	Manual resume	Manual resume	Auto-handoff + auto-resume
Primary review point	Every step	Failures + milestone end	Milestone audit only
Safety rails always apply: no auto git push, no destructive ops, no security changes, no budget bypass, no self-modifying autonomy level.

The document now has 14 parts. Want to continue with Problems 7-9, or discuss any of this further?

yes, please continue with problems 7
Problem 7 (Human Judgment Encoding) — we rated this Well Implemented with a Medium priority gap.

What OpenAI does:

Capture review comments and refactoring PRs as documentation updates
Promote feedback directly into tooling when documentation falls short
Inject custom linter error messages with remediation instructions for agents
"Once encoded, rules apply everywhere at once"
What Threadwork has:

Spec Proposal Pipeline with confidence escalation (0.3 → 0.6 → 0.7 → Store)
Ralph Loop learning signals automatically propose specs from failures
Cross-session Store promotes patterns globally
Remediation blocks include relevant_spec and fix_template
The gap we identified: No automated capture from PR review comments. Human feedback during code review doesn't flow into spec proposals.

But — what we've designed since then covers much of this:

OpenAI Practice	Threadwork Coverage (After Parts 4-13)
"Capture review comments as doc updates"	Part 11: Knowledge notes let agents capture discoveries. But human review comments still aren't captured.
"Promote feedback into tooling"	Part 4: Spec rules become mechanical enforcement. Part 6.2: Proposals from failures auto-promote.
"Custom linter error messages with remediation"	Part 4.3: Spec compliance gate with rule-aware remediation blocks.
"Once encoded, rules apply everywhere"	Part 4.1: Spec rules apply to all agents via routing map + quality gate.
The remaining gap is specifically: human review feedback → spec proposals. When a human reviews an agent's PR and says "don't do X, do Y instead," that feedback currently goes nowhere except the PR comment thread. The next agent working in the same area will make the same mistake.

This is actually a small, focused problem. Here's what I'd propose:

Approach: Review Feedback Capture
Two mechanisms:

1. /tw:feedback command — After reviewing agent output (code, PR, or verify-phase results), the human can capture feedback:


User: /tw:feedback "Always use AppError instead of Error in service layer"
  --scope src/services/
  --rule grep_must_not_exist "new Error(" 
This immediately:

Creates a spec proposal at confidence 0.6 (human-provided = higher than agent-discovered)
If --rule is provided, includes the machine-checkable rule in the proposal
If --scope is provided, creates a knowledge note (critical) for immediate injection
The confidence is 0.6 instead of the usual 0.3 because it's direct human judgment — one step away from acceptance at 0.7.

2. Verify-phase feedback integration — When the verifier generates VERIFICATION.md and the human reviews it, any corrections the human makes become feedback:

In guided/autonomous mode (Part 13), /tw:audit-milestone already asks the human to review. We can add a structured feedback step:


Threadwork: "You're reviewing Phase 2 verification. Any feedback for future phases?"
  [No feedback]  [Add feedback]

User: "Add feedback"

Threadwork: "What should agents do differently?"

User: "API responses should always include a 'requestId' field for traceability"

Threadwork: "Got it. Creating spec proposal at confidence 0.6.
  Scope: src/services/ + src/app/api/
  Shall I also create a spec rule? (grep_must_exist for 'requestId')"
This makes the milestone audit (Part 13) a natural feedback capture point — the human is already reviewing, so the marginal cost of providing feedback is low.

Files Impact
File	Action	Description
templates/commands/tw-feedback.md	Create	/tw:feedback command — captures human review feedback as spec proposals + knowledge notes
lib/spec-engine.js	Modify	Accept human feedback source with confidence 0.6
templates/commands/tw-audit-milestone.md	Modify	Add structured feedback capture step
This is intentionally lightweight — 1 new command, 2 file modifications. The heavy lifting (proposal pipeline, rule enforcement, knowledge notes) is already designed in Parts 4-11.

That's pretty contained. Want me to update the document and move on to Problem 8, or do you want to discuss the feedback capture further?

yes, please update the document which the problem 5 - 7 and then move on to problem 8.
Read harness_engineering.md (lines 2631-2650)
Good, I know the insertion point. Let me add Parts for Problems 5-7 discussion status (as brief status notes since 5 was already designed and 6-7 are now covered), then append the feedback capture for Problem 7.

Edit harness_engineering.md
Added 93 lines
Now let me update the manifest to include the Problem 7 files.

Edit harness_engineering.md
Added 1 line
Edit harness_engineering.md
Modified
Done. Problems 5-7 discussion status and the /tw:feedback command are now in the document.

Now let's move to Problem 8: PR Velocity vs Quality Gates.

We rated this Conservative Approach (Low priority). Here's where we stand:

What OpenAI does:

Minimal blocking merge gates
Short-lived PRs
Address test flakes with follow-up runs rather than blocking indefinitely
"Corrections are cheap, waiting is expensive"
Caveat: "This would be irresponsible in a low-throughput environment"
What Threadwork has:

Ralph Loop is a blocking gate (up to 5 retries)
Quality gates cache per git SHA
Team model enables parallel execution
Gap we identified: No configurable gate strictness — can't mark certain gates as non-blocking or set "flake tolerance."

But — Part 13 (Autonomous Mode) already addresses most of this:

OpenAI Practice	Part 13 Coverage
Minimal blocking gates	Autonomous mode: Ralph Loop skips-and-logs after 10 retries instead of blocking
Corrections are cheap	Autonomous mode: proceed on verification failure, attempt auto-fix, log gaps
Short-lived PRs	Not directly addressed — Threadwork doesn't manage PR workflow
Test flake tolerance	Not addressed yet
The one remaining piece is flake tolerance — test flakes are a real problem with agent-generated code. A test might fail due to timing, network, or shared state rather than actual bugs. Currently every test failure triggers the Ralph Loop, burning retries on flakes.

Flake Detection
A simple approach: if the same test fails, then passes on immediate re-run without code changes, it's a flake.


// In lib/quality-gate.js — extend runTests()
export function runTests(filter, options = {}) {
  const result = runTestsOnce(filter);
  
  if (!result.passed && options.flakyRetries > 0) {
    // Re-run failed tests without code changes
    const rerunResult = runTestsOnce(filter);
    if (rerunResult.passed) {
      // Tests passed on re-run — mark failures as flaky
      return {
        ...rerunResult,
        passed: true,
        flakyTests: result.failures,
        warning: `${result.failures.length} flaky test(s) detected — passed on re-run`
      };
    }
  }
  
  return result;
}
Configuration in quality-config.json:


{
  "tests": {
    "enabled": true,
    "blocking": true,
    "flakyRetries": 1,
    "flakyThreshold": 3
  }
}
flakyRetries: how many times to re-run failed tests before declaring real failure (default: 1)
flakyThreshold: after a test is detected as flaky N times, auto-add it to a flaky test tracker
Flaky tests tracked in .threadwork/state/flaky-tests.json:


{
  "tests": [
    {
      "name": "auth > refresh token > should handle concurrent refresh",
      "occurrences": 3,
      "firstSeen": "2026-03-15",
      "lastSeen": "2026-03-20"
    }
  ]
}
Surfaced in:

Handoff: "3 flaky tests detected this session"
/tw:docs-health: flaky test trend
Gap report: recurring flaky tests as testing_gap entries
This is a small, focused addition. The broader PR velocity question is handled by the autonomy levels in Part 13.

Gate Strictness Configuration
We originally identified this gap: can't mark gates as non-blocking. But actually, quality-config.json already has blocking: boolean per gate. The gap is that there's no easy way to change it — the user has to manually edit the JSON.

Add to /tw:autonomy command:


/tw:autonomy gate security non-blocking
/tw:autonomy gate tests blocking
/tw:autonomy gate endpoint-verification non-blocking
This is a thin wrapper around editing quality-config.json — minimal implementation.

That's quite contained for Problem 8. Want me to update the document with this and move to Problem 9, or discuss further?

yes, and then move to problem 9
Read harness_engineering.md (lines 2711-2730)
Read harness_engineering.md (lines 2741-2760)
Good. Let me insert Problem 8 after Problem 7 status and before Part 12.

Edit harness_engineering.md
Added 98 lines
Now let's tackle Problem 9: Agent Review Bottleneck — the last one, and one we flagged as High Priority.

What OpenAI does:

Push "almost all review effort" to agent-to-agent
Humans instruct Codex to review its own changes
Request additional agent reviews (both local and cloud)
Respond to human and agent feedback
Iterate in loop until all agent reviewers satisfied
What Threadwork has:

Ralph Loop = self-review (structural: lint/type/test)
/tw:verify-phase = goal-backward verification by tw-verifier
Plan-checker validates plans across 6 dimensions
Entropy collector reviews cross-output coherence
The gap: No agent-to-agent review workflow. No dedicated review agent examining code for semantic correctness, design quality, or specification compliance. Current review is structural (does it compile, does it lint, do tests pass), not semantic (does it do what was asked, is the design clean, does it match the spec's intent).

This is distinct from what the verifier does — the verifier checks at phase end whether requirements are met. The gap is per-task semantic review before the Ralph Loop, catching things like:

Function does the wrong thing but passes tests (tests are incomplete)
Code works but uses an anti-pattern the specs discourage
Implementation is correct but the design is fragile (tight coupling, no error handling for edge cases)
Code duplicates existing functionality the agent didn't discover
Proposed: tw-reviewer Agent
A new 10th agent that reviews executor output between execution and the Ralph Loop quality gates:


Executor completes task
    │
    ▼
tw-reviewer examines the diff              ← NEW
    │ Checks: semantic correctness, spec intent,
    │ design quality, duplication, edge cases
    │
    ├── Pass → proceed to Ralph Loop quality gates
    │
    └── Reject → send feedback to executor
          │ Executor revises based on feedback
          │ Re-submit to tw-reviewer
          │ Loop up to 2 iterations
          │
          ▼
        Ralph Loop quality gates (lint/type/test/spec-compliance/doc-freshness)
What tw-reviewer checks (that quality gates can't):

Requirement alignment — does the code actually satisfy the task's <done-condition>? Not just "tests pass" but "the behavior matches what was asked"
Spec intent compliance — the spec says "use the repository pattern" — does the code actually follow that pattern, or just technically not violate the grep rule?
Design quality — tight coupling, missing error handling for edge cases, hardcoded values that should be configurable
Duplication detection — did the executor reinvent something that already exists in src/lib/utils/?
Knowledge note check — do any existing knowledge notes apply to the changed files?
Agent template (templates/agents/tw-reviewer.md):


# tw-reviewer — Code Review Analyst

## Role
You are the Code Review Analyst for Threadwork. You review executor output for
semantic correctness, design quality, and spec compliance BEFORE quality gates run.

You are Sonnet-class. You see the task diff, the task spec, and relevant specs.
Your job is to catch issues that lint/type/test cannot: wrong behavior, bad design,
missed edge cases, duplicated functionality.

## Inputs
- **task_diff**: git diff of files changed by the executor for this task
- **task_spec**: the `<task>` XML block from the plan (description, files, verification, done-condition)
- **relevant_specs**: specs that match the task's file targets (fetched via routing map)
- **knowledge_notes**: critical + scope-matching notes for changed files
- **existing_utilities**: list of exports from src/lib/, src/utils/, src/helpers/ (if they exist)

## Review Checklist

### 1. Requirement Alignment
Does the code satisfy the task's `<done-condition>`?
- Read the done-condition carefully
- Trace through the code to verify the behavior matches
- Flag if the code does something adjacent but not exactly what was asked

### 2. Spec Intent
Do relevant specs prescribe patterns that should be followed here?
- Check if the code follows the spec's recommended approach, not just avoids violations
- Flag if the code technically passes spec rules but misses the spirit

### 3. Design Quality
- Are there hardcoded values that should be constants or config?
- Is error handling present for likely failure modes?
- Are there tight couplings that could be avoided?
- Is the code testable? (Can key functions be unit tested in isolation?)

### 4. Duplication Check
- Compare new functions against existing_utilities
- Flag if a new helper duplicates existing functionality
- Suggest using the existing utility instead

### 5. Knowledge Note Check
- Do any knowledge notes warn about patterns in these files?
- Is the code consistent with discovered knowledge?

## Output Format

Return one of:

**APPROVE** — no semantic issues found
```json
{ "decision": "approve", "notes": "optional brief comment" }
REQUEST_CHANGES — issues found, executor should revise


{
  "decision": "request_changes",
  "issues": [
    {
      "type": "requirement_alignment|spec_intent|design_quality|duplication|knowledge",
      "file": "src/services/auth.ts",
      "line": 42,
      "description": "Function returns null on not-found but done-condition says it should throw NotFoundError",
      "suggestion": "Change line 42 to throw new NotFoundError(`User ${id} not found`)"
    }
  ]
}
Constraints
Maximum 2 review iterations per task (avoid infinite loops)
If issues are minor and non-blocking, APPROVE with notes rather than requesting changes
Do not duplicate what quality gates check — skip lint, type, and test concerns
Target: complete review in under 10,000 tokens


### When tw-reviewer Runs

Not on every task — that would be expensive. Configurable:

```json
// project.json
{
  "review": {
    "enabled": true,
    "mode": "selective",  // "all" | "selective" | "off"
    "triggers": {
      "fileCount": 3,           // review if task touches 3+ files
      "securityKeywords": true, // review if task involves auth/crypto/secrets
      "newFiles": true,         // review if task creates new files
      "complexityHigh": true    // review if task estimate > 40K tokens
    }
  }
}
all: review every task (highest quality, most expensive)
selective: review only tasks matching triggers (balanced)
off: skip review entirely (fastest, relies on quality gates only)
Autonomy level affects defaults:

Supervised: selective (default triggers)
Guided: selective (same triggers)
Autonomous: all (compensates for less human review)
Integration Point
The reviewer runs in hooks/subagent-stop.js before quality gates, not after. This means:


// In hooks/subagent-stop.js — new section before quality gate execution

if (reviewEnabled && shouldReview(taskContext)) {
  const reviewResult = await spawnReviewer({
    taskDiff: getTaskDiff(),
    taskSpec: currentTaskXml,
    relevantSpecs: getRelevantSpecs(taskContext),
    knowledgeNotes: getNotesForScope(taskContext.files),
    existingUtilities: scanExistingUtilities()
  });

  if (reviewResult.decision === 'request_changes') {
    // Send review feedback to executor for revision
    const reviewPrompt = formatReviewFeedback(reviewResult.issues);
    writeRalphState({ ...ralph, reviewIteration: (ralph.reviewIteration ?? 0) + 1 });

    if ((ralph.reviewIteration ?? 0) < 2) {
      // Re-invoke executor with review feedback
      process.stdout.write(JSON.stringify({
        action: 'block',
        retry: true,
        message: reviewPrompt,
        source: 'tw-reviewer'
      }));
      return;
    }
    // Max review iterations reached — proceed to quality gates anyway
    logHook('WARNING', `tw-reviewer: max iterations reached, proceeding to gates`);
  }
}

// Then run quality gates as before
const gateResult = await runAll({ skipCache: true });
Review Feedback Format
When the reviewer requests changes, the feedback is injected into the executor's retry prompt:


CODE REVIEW FEEDBACK (iteration 1 of 2)

Issues found by tw-reviewer:

1. [requirement_alignment] src/services/auth.ts:42
   Function returns null on not-found but done-condition says it should throw NotFoundError.
   Suggestion: throw new NotFoundError(`User ${id} not found`)

2. [duplication] src/lib/helpers/format-date.ts
   This duplicates existing function formatISO() in src/utils/date.ts.
   Suggestion: Import and use formatISO() instead of creating a new helper.

Please address these issues and resubmit.
Review Results in Reports
Review outcomes tracked in execution-log.json and surfaced in:

SUMMARY.md: "2 tasks reviewed, 1 had issues resolved in 1 iteration"
Handoff Section 8: "Review: 5 tasks reviewed, 3 approved immediately, 2 revised"
Entropy trends: review rejection patterns tracked as data point
This is the most significant new agent since the v0.2.0 roster. It closes the last High Priority gap from the scorecard. Files impact:

File	Action	Description
templates/agents/tw-reviewer.md	Create	Code review analyst agent template
hooks/subagent-stop.js	Modify	Add pre-gate review step, reviewer spawn, review iteration tracking
lib/quality-gate.js	Modify	Expose scanExistingUtilities() for duplication check
templates/commands/tw-execute-phase.md	Modify	Document review step in execution flow
Want me to update the document with Problems 8 and 9, or discuss the reviewer design further?

yes, please update the document with problem 8 and 9
Read harness_engineering.md (lines 2791-2800)
Read harness_engineering.md (lines 2816-2845)
Good, now let me insert Problem 9 after Problem 8's verdict and before Part 12.

Edit harness_engineering.md
Added 249 lines
Now update the manifest to include the new files.

Edit harness_engineering.md
Added 1 line
Edit harness_engineering.md
Added 2 lines
Done. All 9 problems are now discussed and documented. Here's the final scorecard:

#	Problem	Original Status	After Discussion	Key Addition
1	Underspecified Environments	Mostly Implemented	Fully Covered	Part 8: 3-layer gap detection (readiness audit, pre-execution scan, aggregation)
2	Human QA Bottleneck	Partially Implemented	Fully Covered	Part 9: Smoke test, endpoint verification, verification profiles, manual feedback
3	Context Management	Well Implemented	Fully Covered	Part 10: Doc-freshness gate, Part 11: Knowledge notes
4	Agent Legibility	Partially Addressed	Mostly Covered	Covered by Parts 8/10/11; remaining is user guidance, not tooling
5	Architectural Enforcement	Partially Implemented	Fully Covered	Part 4: Spec rules, structural tests, binding constraints
6	Garbage Collection	Well Implemented	Fully Covered	Part 12: Quality trends, on-demand entropy scan
7	Human Judgment Encoding	Well Implemented	Fully Covered	/tw:feedback command, milestone audit feedback
8	PR Velocity vs Gates	Conservative	Fully Covered	Flake detection, gate strictness CLI, Part 13 autonomy
9	Agent-to-Agent Review	Partially Implemented	Fully Covered	tw-reviewer agent (10th agent), pre-gate semantic review
Plus Part 13: Autonomous Operation Mode — three levels (supervised/guided/autonomous) that address the future where AI has more privilege and humans review only final results.

All 9 OpenAI problems now have designed solutions. Want to revisit Problem 4 now, or discuss next steps for turning this into a blueprint?

based on the discussion,  would you please create a plan / blueprint document (docs/blueprint_v0.3.2.md, I would like to mark the verstion to 0.3.2) for implement the changes on threadwork project?  please go through all discussion and rethink the threadwork design/structure if need, and implement the change based on the design and priority.
Read blueprint_v0.3.0.md
Read blueprint_v0.2.0.md
Read blueprint_v0.3.0.md (lines 101-180)
Now I have a good understanding of the blueprint format. Let me also check the current architecture doc briefly, then create the comprehensive blueprint.

Read blueprint_v0.3.0.md (lines 501-530)
Now I have the blueprint format down. Let me create the comprehensive v0.3.2 blueprint. This is a large document so I'll write it directly.

Write blueprint_v0.3.2.md
956 lines
# Threadwork v0.3.2 — Update Blueprint

> **Version**: 0.3.2
> **Status**: Upgrade spec — applies to existing Threadwork projects
> **Prerequisite**: Threadwork v0.3.x already initialized
> **Upgrade command**: `threadwork update --to v0.3.2`

---

## Why This Update Exists: The Harness Engineering Gap Analysis

This upgrade is informed by OpenAI's "Harness Engineering: Leveraging Codex in an Agent-First World" article, which documents how 3 engineers built a 1M+ line production app with zero manual code over 5 months. A systematic comparison of their 9 core problems against Threadwork's capabilities revealed that while Threadwork excels at context management, token economics, and session continuity, it has significant gaps in **mechanical enforcement**, **semantic review**, **runtime verification**, **failure classification**, and **autonomous operation**.

The full analysis is documented in `docs/discuss/harness_engineering.md` (14-part discussion covering all 9 problems). This blueprint implements the highest-impact changes as 8 upgrades organized into 3 priority tiers.

### Key Architectural Insight

Threadwork v0.3.x specs are **advisory** — injected as context, agents trusted to follow them. OpenAI's constraints are **mechanical** — deterministic rules that fail the build. v0.3.2 bridges this gap by adding machine-checkable rules to specs, a compliance quality gate, and a failure classification system that improves the harness itself rather than just retrying.

### Design Principles for This Release

1. **Additive only** — no existing state files, specs, or handoffs are altered by the upgrade
2. **Progressive adoption** — each upgrade works independently; users can adopt incrementally
3. **Backward compatible** — specs without `rules` frontmatter continue to work unchanged
4. **Existing patterns** — follow the virtual-tool interception pattern (like `spec_fetch`), the quality gate registration pattern, and the entropy collector spawn pattern

---

## What Changes in v0.3.2

8 upgrades in 3 priority tiers. All changes are additive or in-place modifications.

| Tier | # | Upgrade | Key Files | New Commands |
|------|---|---------|-----------|-------------|
| **T1** | 1 | Spec Rules Engine + Compliance Gate | `lib/rule-evaluator.js` (new), `lib/quality-gate.js` | — |
| **T1** | 2 | Failure Classification + Fast-Track Proposals | `lib/quality-gate.js`, `hooks/subagent-stop.js` | — |
| **T1** | 3 | tw-reviewer Agent (Agent-to-Agent Review) | `templates/agents/tw-reviewer.md` (new) | — |
| **T2** | 4 | Doc-Freshness Gate + Knowledge Notes | `lib/doc-freshness.js` (new), `lib/knowledge-notes.js` (new) | `/tw:docs-health` |
| **T2** | 5 | Enhanced Discuss-Phase (10 Questions) | `templates/commands/tw-discuss-phase.md` | — |
| **T2** | 6 | Runtime Verification (Smoke Test + Profiles) | `lib/verification-profile.js` (new), `lib/quality-gate.js` | `/tw:verify-manual` |
| **T3** | 7 | Capability Gap Detection + Readiness Audit | `lib/spec-engine.js` | `/tw:readiness` |
| **T3** | 8 | Autonomous Operation Mode | `lib/autonomy.js` (new) | `/tw:autonomy` |

**Tier 1** — Core enforcement loop. Must ship together as they form an integrated system.
**Tier 2** — Knowledge and verification layer. Each upgrade is independent.
**Tier 3** — Proactive detection and autonomy. Builds on Tier 1+2 foundations.

---

## Tier 1: Core Enforcement Loop

### Upgrade 1: Spec Rules Engine + Compliance Gate

**Source**: Discussion Parts 4.1, 4.2, 4.3
**Problem addressed**: #5 (Architectural Coherence) — specs are advisory, agents can ignore them
**v0.3.x behavior**: Specs contain prose only. Quality gates check lint/type/test but not spec compliance.
**v0.3.2 behavior**: Specs can define machine-checkable `rules` in frontmatter. A new `spec-compliance` gate enforces them mechanically in the Ralph Loop.

#### 1.1 Spec frontmatter `rules` field

Specs gain an optional `rules` array. Existing specs without `rules` continue to work unchanged.

```yaml
---
domain: backend
specId: SPEC:auth-001
name: JWT Best Practices
tags: [auth, jwt]
rules:
  - type: grep_must_exist
    pattern: "RS256"
    files: "src/lib/auth*.ts"
    message: "JWT must use RS256 algorithm per SPEC:auth-001"
  - type: grep_must_not_exist
    pattern: "require\\(['\"]jsonwebtoken['\"]\\)"
    files: "src/**/*.{ts,js}"
    message: "Use jose library, not jsonwebtoken (SPEC:auth-001)"
  - type: import_boundary
    from: "src/services/**"
    cannot_import: ["src/ui/**", "src/components/**"]
    message: "Service layer cannot import from UI layer"
  - type: naming_pattern
    pattern: "^use[A-Z]"
    files: "src/hooks/**/*.ts"
    target: "export_names"
    message: "Hooks must start with 'use' prefix"
  - type: file_structure
    must_exist: ["src/services/*/index.ts"]
    message: "Services must have index.ts entry point"
---
```

5 rule types at launch (extensible):

| Type | What it checks | How |
|------|---------------|-----|
| `grep_must_exist` | Pattern MUST appear in matching files | `grep -r` with glob filter |
| `grep_must_not_exist` | Pattern must NOT appear | `grep -r` inverse |
| `import_boundary` | Files in `from` glob cannot import from `cannot_import` globs | Parse `import`/`require` statements |
| `naming_pattern` | Exported names in matching files must match regex | Parse exports, test regex |
| `file_structure` | Required file glob patterns must exist | Glob existence check |

#### 1.2 New module: `lib/rule-evaluator.js`

```javascript
/**
 * lib/rule-evaluator.js — Spec rule evaluation engine
 *
 * Evaluates machine-checkable rules from spec frontmatter against
 * the current working tree. Each rule type has a dedicated evaluator.
 * Returns structured violations with specId, file, message, and evidence.
 */

export function evaluateRules(rules, projectRoot)
// Returns: { passed: boolean, violations: Violation[] }
// Violation: { specId, ruleType, message, files: string[], evidence: string }

export function evaluateGrepMustExist(rule, projectRoot)
export function evaluateGrepMustNotExist(rule, projectRoot)
export function evaluateImportBoundary(rule, projectRoot)
export function evaluateNamingPattern(rule, projectRoot)
export function evaluateFileStructure(rule, projectRoot)
```

#### 1.3 New gate: `runSpecCompliance()` in `lib/quality-gate.js`

Combines spec rules + optional structural tests (`.threadwork/structural-tests/` directory):

```javascript
export async function runSpecCompliance(options = {}) {
  const rules = loadRulesFromSpecs(specsDir);
  const ruleResult = evaluateRules(rules, projectRoot);
  const structResult = await runStructuralTests(projectRoot);
  return {
    gate: 'spec-compliance',
    passed: ruleResult.passed && structResult.passed,
    violations: ruleResult.violations,
    structuralFailures: structResult.errors,
    skipped: rules.length === 0 && structResult.tests === 0
  };
}
```

Gate order becomes: `typecheck → lint → tests → spec-compliance → build → security`

Default config: `{ "spec-compliance": { "enabled": true, "blocking": true } }`

#### 1.4 Extend `buildRemediationBlock()` for spec violations

When spec-compliance fails, the remediation block includes the exact rule violated:

```javascript
{
  primary_violation: "SPEC:auth-001 rule violated: Use jose library, not jsonwebtoken",
  relevant_spec: "SPEC:auth-001 | backend/jwt-best-practices",
  fix_template: "Replace require('jsonwebtoken') with import { SignJWT } from 'jose'",
  learning_signal: "spec-rule:auth-001:grep_must_not_exist:jsonwebtoken"
}
```

#### 1.5 Structural test convention

Users can create `.threadwork/structural-tests/*.js` files for complex invariants:

```javascript
// .threadwork/structural-tests/no-circular-deps.js
export const name = 'No Circular Dependencies';
export const specId = 'SPEC:arch-001';
export async function check(projectRoot) {
  // Custom logic — run madge, depcheck, etc.
  return { passed: true }; // or { passed: false, errors: [...] }
}
```

#### Files to modify

| File | Action |
|------|--------|
| `lib/rule-evaluator.js` | **Create** — 5 rule type evaluators + `evaluateRules()` orchestrator |
| `lib/quality-gate.js` | Modify — add `runSpecCompliance()`, `runStructuralTests()`, register in `runAll()`, extend `buildRemediationBlock()` |
| `lib/spec-engine.js` | Modify — add `loadRulesFromSpecs()` to parse `rules` from frontmatter |
| `hooks/subagent-stop.js` | Modify — pass specEngine to `buildRemediationBlock()` for rule-aware remediation |
| `templates/specs/enforcement/` | **Create** — starter spec templates with example rules |

#### Tests

- `tests/unit/rule-evaluator.test.js` — each rule type evaluator, edge cases (empty globs, no matches)
- `tests/unit/quality-gate-compliance.test.js` — `runSpecCompliance()` with mock specs containing rules
- `tests/integration/spec-enforcement.test.js` — full flow: spec with rules → violating code → gate catches → remediation

---

### Upgrade 2: Failure Classification + Fast-Track Proposals

**Source**: Discussion Parts 6.1, 6.2, 6.3
**Problem addressed**: #1 (Underspecified Environments) — Ralph Loop retries without improving the harness
**v0.3.x behavior**: Every failure creates a spec proposal at confidence 0.3 regardless of failure type.
**v0.3.2 behavior**: Failures are classified by type. Classification drives proposal confidence, retry strategy, and gap reporting.

#### 2.1 New function: `classifyFailure()` in `lib/quality-gate.js`

```javascript
export function classifyFailure(gateResults, specEngine) {
  // Returns: { type, confidence, evidence, recommendation }
  // Types:
  //   'code_bug'                — agent made a fixable mistake → retry normally
  //   'knowledge_gap'           — agent didn't know about X → inject missing spec + propose at 0.5
  //   'missing_capability'      — agent needed unavailable tool → log gap, don't waste retries
  //   'architectural_violation' — agent broke undocumented rule → propose rule at 0.5
}
```

**Classification logic priority:**
1. Check if error relates to a spec that exists but wasn't fetched → `knowledge_gap`
2. Check if error matches an existing spec rule → `architectural_violation`
3. Check if error is about missing tool/resource → `missing_capability`
4. Default → `code_bug`

#### 2.2 New function: `wasSpecFetchedThisSession()` in `lib/spec-engine.js`

Track which specs were fetched via the existing `spec_fetch_log` in `token-log.json`:

```javascript
export function wasSpecFetchedThisSession(specId) {
  const log = readTokenLog();
  return (log.spec_fetch_log ?? []).some(entry => entry.specId === specId);
}
```

#### 2.3 Classification-aware Ralph Loop

In `hooks/subagent-stop.js`, after `buildRemediationBlock()`:

```javascript
const classification = classifyFailure(gateResult, specEngine);

// Attach to remediation log
remediationLog.push({
  iteration: retries,
  classification: classification.type,
  evidence: classification.evidence,
  ...existingFields
});

// Classification-aware behavior:
if (classification.type === 'knowledge_gap') {
  // Inject missing spec into THIS retry prompt
  const specContent = specEngine.fetchSpecById(relatedSpecId);
  correctionPrompt += `\n\nRELEVANT SPEC:\n${specContent}`;
}

if (classification.type === 'missing_capability') {
  // Don't waste retries — log gap and allow completion
  appendGapReport({ type: 'missing_capability', ... });
}
```

#### 2.4 Classification-aware proposal confidence

```
code_bug:              0.3 → 0.6 cap → human → 0.7 → Store  (unchanged)
knowledge_gap:         0.5 → 0.6 cap → human → 0.7 → Store  (faster)
architectural_violation: 0.5 + proposed rules → human → 0.7 → enforcement
missing_capability:    No proposal → gap report → handoff
```

Changes to `proposeSpecUpdate()` in `lib/spec-engine.js`:
- Accept `initialConfidence` option (default 0.3, knowledge_gap/architectural_violation use 0.5)
- Accept `proposedRules` option — embed machine-checkable rules in proposal frontmatter

#### 2.5 Gap reporting

New functions in `lib/state.js`:
- `appendGapReport(entry)` — write to `.threadwork/state/gap-report.json`
- `readGapReport()` — read all gap entries

New handoff Section 4b in `lib/handoff.js`:

```markdown
## 4b. Environment Gaps Detected

**Knowledge gaps** (2): Agent lacked context that exists in codebase
  - Custom AppError class not in routing map → Spec proposed at confidence 0.5

**Missing capabilities** (1): Agent needed tooling that doesn't exist
  - Attempted to run database seed command but no seed script exists
```

#### Files to modify

| File | Action |
|------|--------|
| `lib/quality-gate.js` | Modify — add `classifyFailure()` |
| `lib/spec-engine.js` | Modify — add `wasSpecFetchedThisSession()`, accept `initialConfidence` and `proposedRules` in `proposeSpecUpdate()` |
| `lib/state.js` | Modify — add `appendGapReport()`, `readGapReport()` |
| `lib/handoff.js` | Modify — add Section 4b (Environment Gaps) |
| `hooks/subagent-stop.js` | Modify — classification-aware retry, proposal, and gap reporting |

#### Tests

- `tests/unit/quality-gate-classify.test.js` — each classification path
- `tests/unit/handoff-gaps.test.js` — Section 4b generation

---

### Upgrade 3: tw-reviewer Agent (Agent-to-Agent Review)

**Source**: Discussion Problem 9
**Problem addressed**: #9 (Agent Review Bottleneck) — no semantic review, only structural
**v0.3.x behavior**: Ralph Loop checks lint/type/test. No semantic review of code logic or design.
**v0.3.2 behavior**: New tw-reviewer agent examines executor output for semantic correctness, spec intent compliance, design quality, and duplication before quality gates run.

#### 3.1 New agent: `templates/agents/tw-reviewer.md`

Role: Code Review Analyst (Sonnet-class)

**Inputs**: task_diff, task_spec (XML), relevant_specs, knowledge_notes, existing_utilities

**Review checklist** (5 checks that quality gates can't do):
1. **Requirement alignment** — does code satisfy the `<done-condition>`?
2. **Spec intent** — does code follow spec's recommended approach, not just avoid violations?
3. **Design quality** — hardcoded values, missing error handling, tight coupling?
4. **Duplication** — does new code duplicate existing utilities?
5. **Knowledge note check** — is code consistent with discovered knowledge?

**Output**: `{ "decision": "approve" | "request_changes", "issues": [...] }`

**Constraints**: Max 2 review iterations per task. Under 10,000 tokens. Skip lint/type/test concerns.

#### 3.2 Review configuration in `project.json`

```json
{
  "review": {
    "enabled": true,
    "mode": "selective",
    "triggers": {
      "fileCount": 3,
      "securityKeywords": true,
      "newFiles": true,
      "complexityHigh": true
    }
  }
}
```

Modes: `all` | `selective` (default) | `off`

#### 3.3 Integration in `hooks/subagent-stop.js`

Review runs **before** quality gates. Flow:

```
Executor completes → tw-reviewer (if triggers match) → quality gates (lint/type/test/spec-compliance)
```

On `request_changes`: send review feedback to executor, re-invoke. Max 2 review iterations before proceeding to gates.

#### 3.4 Helper: `scanExistingUtilities()` in `lib/quality-gate.js`

Scans `src/lib/`, `src/utils/`, `src/helpers/` for exported functions/classes. Returns list for duplication check input to reviewer.

#### Files to modify

| File | Action |
|------|--------|
| `templates/agents/tw-reviewer.md` | **Create** — code review analyst agent template |
| `hooks/subagent-stop.js` | Modify — add pre-gate review step, reviewer spawn, review iteration tracking |
| `lib/quality-gate.js` | Modify — expose `scanExistingUtilities()` |

#### Tests

- `tests/unit/reviewer-integration.test.js` — review triggering, iteration limits, feedback formatting
- `tests/integration/agent-review.test.js` — execute → review → feedback → revise → gates

---

## Tier 2: Knowledge & Verification Layer

### Upgrade 4: Doc-Freshness Gate + Knowledge Notes

**Source**: Discussion Parts 10.1, 10.2, 11.1-11.8
**Problems addressed**: #3 (Context Management — docs rot), #7 (Human Judgment — knowledge lost)
**v0.3.x behavior**: No staleness detection for specs. Agent-discovered knowledge dies with the session.
**v0.3.2 behavior**: New doc-freshness gate catches stale spec references mechanically. New knowledge_note virtual tool captures agent discoveries for future sessions.

#### 4.1 Doc-freshness gate: `lib/doc-freshness.js` (new module)

```javascript
export function checkDocFreshness(specsDir, projectRoot)
// Checks:
//   dead_reference (error, blocking) — spec references file that doesn't exist
//   dead_cross_reference (error, blocking) — spec references specId that doesn't exist
//   dead_library_reference (warning) — spec references library not in package.json
//   empty_rule_target (warning) — rule glob matches no files
//   age_staleness (warning) — spec old + >50% referenced files changed since
```

Registered in `runAll()` gate sequence after spec-compliance:
`typecheck → lint → tests → spec-compliance → doc-freshness → smoke-test → build → security`

#### 4.2 Spec staleness tracking in `hooks/post-tool-use.js`

After each file modification, check if any spec references the changed file. Write to `.threadwork/state/spec-staleness-tracker.json`. Read by doc-freshness gate and surfaced in handoff.

#### 4.3 Entropy collector category 7: Spec Staleness

Extend `templates/agents/tw-entropy-collector.md` with 7th scan category. When a wave modifies code, check if any specs covering that code need updating. Warning severity — queued for human review, never auto-updated.

#### 4.4 Knowledge note virtual tool

New `knowledge_note` tool intercepted by `hooks/pre-tool-use.js` (same pattern as `spec_fetch`):

```javascript
// Agent calls:
knowledge_note({
  category: 'setup',           // setup | api | edge_case | testing | workflow
  scope: 'tests/',             // file path or directory
  summary: 'Tests must run with --runInBand due to shared DB',
  evidence: 'Parallel run caused failures, sequential passes',
  critical: true               // if true, inject into same-session agents immediately
})
```

Stored in `.threadwork/state/knowledge-notes.json`. Module: `lib/knowledge-notes.js`.

#### 4.5 Knowledge notes in routing map

`buildRoutingMap()` includes scope-matching knowledge notes alongside specs:

```
── SPEC ROUTING MAP ─────────────────────────────────
Available specs:
  [SPEC:be-002]  backend/api-design      — API patterns

Implementation notes for this scope:
  ⚠ [KN-critical] tests/ — Tests must run with --runInBand (verified 2026-03-19)
─────────────────────────────────────────────────────
```

#### 4.6 Knowledge note lifecycle

```
Agent discovers → knowledge_note tool → knowledge-notes.json
  → critical: inject same-session
  → all: inject next session via session-start
  → survives 2+ sessions → auto-promote to spec proposal at confidence 0.5
  → human accepts → becomes permanent spec, subject to doc-freshness
```

#### 4.7 Handoff Section 4c: Implementation Knowledge Discovered

Lists all knowledge notes from the session, categorized by critical/non-critical/promoted.

#### 4.8 New command: `/tw:docs-health`

Dashboard showing spec freshness, reference integrity, rule target coverage, knowledge note health, cross-spec consistency.

#### 4.9 Discovery Protocol in executor/debugger templates

Add section to `templates/agents/tw-executor.md` and `templates/agents/tw-debugger.md` instructing agents to call `knowledge_note` when they discover something useful.

#### Files to modify

| File | Action |
|------|--------|
| `lib/doc-freshness.js` | **Create** — reference integrity, age staleness, cross-spec consistency |
| `lib/knowledge-notes.js` | **Create** — note CRUD, promotion, freshness, routing map integration |
| `lib/quality-gate.js` | Modify — add `runDocFreshness()` to gate sequence |
| `lib/spec-engine.js` | Modify — `trackSpecStaleness()`, `extractFileReferences()`, knowledge notes in `buildRoutingMap()`, `promoteKnowledgeNotes()` |
| `lib/handoff.js` | Modify — add Section 4c (Implementation Knowledge) |
| `hooks/pre-tool-use.js` | Modify — intercept `knowledge_note` virtual tool |
| `hooks/post-tool-use.js` | Modify — track spec staleness on file changes, track knowledge note freshness |
| `hooks/session-start.js` | Modify — inject critical knowledge notes, increment sessionsSurvived |
| `templates/agents/tw-executor.md` | Modify — add Discovery Protocol |
| `templates/agents/tw-debugger.md` | Modify — add Discovery Protocol |
| `templates/agents/tw-entropy-collector.md` | Modify — add category 7: Spec Staleness |
| `templates/commands/tw-docs-health.md` | **Create** — `/tw:docs-health` command |

#### Tests

- `tests/unit/doc-freshness.test.js` — dead references, cross-references, age staleness
- `tests/unit/knowledge-notes.test.js` — note lifecycle, promotion, freshness
- `tests/integration/doc-freshness.test.js` — code change → staleness → gate → remediation
- `tests/integration/knowledge-capture.test.js` — discovery → capture → inject → promote

---

### Upgrade 5: Enhanced Discuss-Phase (10 Questions)

**Source**: Discussion Parts 4.4, 9.3.1
**Problems addressed**: #5 (Architectural Coherence), #2 (QA Bottleneck), #7 (Human Judgment)
**v0.3.x behavior**: Discuss-phase asks 5 questions (libraries, patterns, constraints, risks, out of scope).
**v0.3.2 behavior**: 10 questions. New questions capture enforcement rules, review criteria, and verification environment. Answers auto-generate spec rules and verification profiles.

#### 5.1 New questions

| # | Question | Feeds Into |
|---|----------|------------|
| 1-5 | (existing) | CONTEXT.md |
| **6** | Architectural boundaries — layers and dependency directions | Spec rules (`import_boundary`) |
| **7** | Forbidden patterns — libraries/approaches that must NOT be used | Spec rules (`grep_must_not_exist`) |
| **8** | Naming conventions — rules for files, exports, variables | Spec rules (`naming_pattern`) |
| **9** | Code review focus — acceptance criteria beyond tests | CONTEXT.md `## Review Criteria` → verifier |
| **10** | Verification environment — how project is validated (web app, CLI, plugin, library) | Verification profile in `project.json` → Upgrade 6 |

#### 5.2 Auto-generate spec rules from answers

After user answers Q6-8, auto-create spec file (e.g., `specs/enforcement/phase-N-rules.md`) with machine-checkable rules. User reviews before planning.

#### 5.3 Auto-fill in guided/autonomous mode (Upgrade 8)

In guided mode: auto-fill from previous CONTEXT.md, only ask new questions. In autonomous mode: fully auto-fill, no questions.

#### Files to modify

| File | Action |
|------|--------|
| `templates/commands/tw-discuss-phase.md` | Modify — add questions 6-10, auto-generate spec rules, profile template loading |

---

### Upgrade 6: Runtime Verification (Smoke Test + Verification Profiles)

**Source**: Discussion Parts 9.1-9.7
**Problem addressed**: #2 (Human QA Bottleneck) — no runtime verification beyond lint/type/test
**v0.3.x behavior**: Quality gates are static analysis only.
**v0.3.2 behavior**: Smoke test gate (can the app start?), endpoint verification (do APIs respond correctly?), and verification profiles for non-web projects (Obsidian plugins, CLI tools, VS Code extensions, libraries).

#### 6.1 Smoke test gate: `runSmokeTest()` in `lib/quality-gate.js`

Auto-detects start/dev script from package.json. Starts app, waits for success signal or error, kills after 15s timeout. Zero configuration. Runs in Ralph Loop for web-app profiles.

#### 6.2 Endpoint verification: `runEndpointVerification()` in `lib/quality-gate.js`

Parses plan XML `<verification>` blocks for HTTP expectations. Actually executes HTTP checks against running app. Runs in verify-phase only (not Ralph Loop).

#### 6.3 Verification profiles: `lib/verification-profile.js` (new module)

Stored in `project.json` under `verification` key. Configured during discuss-phase Q10. Defines:
- `type`: web-app | cli-tool | library | obsidian-plugin | vscode-extension | browser-extension | electron-app | custom
- `build`: command + expected outputs
- `automated`: array of checks (json_schema, export_exists, command_runs, file_exists, grep_must_not_exist)
- `manual`: array of steps with expected outcomes and critical flag

Check types: `verifyJsonSchema()`, `verifyExports()`, `verifyCommandRuns()`, `verifyFilesExist()`, `verifyNoForbiddenPatterns()`

#### 6.4 Profile templates: `templates/verification-profiles/`

7 starter templates: web-app, cli-tool, library, obsidian-plugin, vscode-extension, browser-extension, electron-app. Loaded during discuss-phase Q10 and customized by user.

#### 6.5 Profile-aware UAT.md

Verifier generates UAT.md from verification profile `manual` steps + plan `<verification>` blocks. Project-type-aware, not generic.

#### 6.6 New command: `/tw:verify-manual`

Structured manual feedback. User reports pass/fail/skip for each manual check. Critical failures block phase completion. Failed checks become gap report entries. Recurring failures suggest automation.

#### 6.7 Gate execution strategy

```
Ralph Loop:    typecheck → lint → tests → spec-compliance → doc-freshness → smoke-test*
Verify-phase:  all Ralph Loop gates + endpoint-verification* + profile-verification + runtime-checks
Manual:        UAT.md + /tw:verify-manual (critical checks block phase completion)
```

*Only for applicable profile types.

#### 6.8 Flake tolerance

Extend `runTests()` with `flakyRetries` option. If test fails then passes on re-run without code changes, mark as flaky. Track in `.threadwork/state/flaky-tests.json`.

#### Files to modify

| File | Action |
|------|--------|
| `lib/verification-profile.js` | **Create** — profile loader, check runner, manual check tracker |
| `lib/quality-gate.js` | Modify — `runSmokeTest()`, `runEndpointVerification()`, `runProfileChecks()`, flaky retry logic |
| `templates/verification-profiles/` | **Create** — 7 starter profile templates |
| `templates/commands/tw-verify-manual.md` | **Create** — `/tw:verify-manual` command |
| `templates/commands/tw-verify-phase.md` | Modify — profile-aware UAT.md generation |
| `templates/agents/tw-verifier.md` | Modify — use profile, include manual results |

#### Tests

- `tests/unit/verification-profile.test.js` — profile loading, each check type
- `tests/unit/quality-gate-smoke.test.js` — smoke test, endpoint verification, flake detection
- `tests/integration/runtime-verification.test.js` — profile detection, gate execution, manual feedback

---

## Tier 3: Proactive Detection & Autonomy

### Upgrade 7: Capability Gap Detection + Readiness Audit

**Source**: Discussion Parts 8.1-8.3, 12.1-12.2
**Problems addressed**: #1 (Underspecified Environments), #6 (Garbage Collection trends)
**v0.3.x behavior**: No proactive detection of codebase gaps. Entropy collector has no trend tracking.
**v0.3.2 behavior**: Readiness audit scans codebase for agent-unfriendly patterns. Pre-execution gap scanner checks plans against spec coverage. Gap aggregation across sessions. Entropy trend tracking.

#### 7.1 Readiness audit: `auditHarnessReadiness()` in `lib/spec-engine.js`

Scans for: custom base classes, utility directories, non-standard scripts, import aliases, env variables, monorepo structures — all without spec coverage. Produces coverage score and gap list.

Auto-generate draft specs for fixable gaps: `/tw:readiness --fix`

#### 7.2 Pre-execution gap scanner: `scanPlanForGaps()` in `lib/spec-engine.js`

Before `/tw:execute-phase`, scan plan file targets against specs. Warn about uncovered directories, undocumented patterns, unknown libraries. Inject directory READMEs as context fallback.

Runs as Step 1.5 in execute-phase (after budget check, before loading plans).

#### 7.3 Gap aggregation: `aggregateGaps()` in `lib/state.js`

Group gaps by fingerprint across sessions. Priority: 3+ occurrences = high, 2 = medium, 1 = low. Auto-generate draft specs at 3+ occurrences. Inject high-priority warnings at session-start.

#### 7.4 Entropy trend tracking

New `updateTrends()` in `lib/entropy-collector.js`. Aggregate entropy reports into `.threadwork/state/entropy-trends.json`. Classify per-category direction (worsening/improving/stable). Auto-escalate worsening categories to spec rule proposals.

#### 7.5 On-demand entropy scan

New subcommand: `/tw:entropy scan [--since <sha|tag|date>]`. Runs entropy collector against any diff range. Reuses existing agent with different inputs.

#### 7.6 New command: `/tw:readiness`

Shows harness readiness report: coverage score, gaps by priority, auto-fixable count, recommendations.

#### Files to modify

| File | Action |
|------|--------|
| `lib/spec-engine.js` | Modify — `auditHarnessReadiness()`, `scanPlanForGaps()`, `generateDraftSpecForGap()` |
| `lib/state.js` | Modify — `aggregateGaps()` |
| `lib/entropy-collector.js` | Modify — `updateTrends()`, trend classification |
| `hooks/session-start.js` | Modify — inject recurring gap warnings |
| `hooks/post-tool-use.js` | Modify — call `updateTrends()` after entropy report |
| `templates/commands/tw-readiness.md` | **Create** — `/tw:readiness` command |
| `templates/commands/tw-analyze-codebase.md` | Modify — add readiness audit |
| `templates/commands/tw-execute-phase.md` | Modify — pre-execution gap scan (Step 1.5) |
| `templates/commands/tw-entropy.md` | Modify — add `scan` subcommand |

#### Tests

- `tests/unit/spec-engine-gaps.test.js` — readiness audit, plan gap scanner
- `tests/unit/state-gaps.test.js` — gap aggregation
- `tests/unit/entropy-trends.test.js` — trend tracking, worsening detection
- `tests/integration/gap-detection.test.js` — proactive audit + pre-execution scan + aggregation

---

### Upgrade 8: Autonomous Operation Mode

**Source**: Discussion Part 13
**Problem addressed**: Future-proofing — AI will do more, humans will review less
**v0.3.x behavior**: Human approves every step (~9 interaction points per phase).
**v0.3.2 behavior**: Three autonomy levels. Supervised (current), guided (~3 touchpoints), autonomous (~0 touchpoints per phase, review at milestone end).

#### 8.1 Autonomy levels

New setting in `project.json`: `"autonomyLevel": "supervised" | "guided" | "autonomous"`

| Behavior | Supervised | Guided | Autonomous |
|---|---|---|---|
| Discuss-phase | Ask all 10 | Auto-fill, ask new only | Fully auto-fill |
| Plan approval | Always ask | Auto-approve if checker passes | Auto-approve + auto-fix |
| Ralph Loop max retries | 5 | 8 | 10 (then skip-and-log) |
| Spec proposal auto-accept | 0.7 (human only) | 0.6 | 0.5 |
| Model switch policy | Per config | Auto | Auto |
| Manual verification | Immediate | Defer to milestone | Skip |
| Session chaining | Manual | Manual | Auto-handoff + auto-resume |
| Primary review point | Every step | Failures + milestone | Milestone audit only |

#### 8.2 New module: `lib/autonomy.js`

```javascript
export function getAutonomyLevel()           // read from project.json
export function getMaxRetries(level)          // 5 | 8 | 10
export function getAutoAcceptThreshold(level) // 0.7 | 0.6 | 0.5
export function shouldAutoApprovePlan(level, checkReport)
export function shouldAutoFillDiscuss(level, previousContext)
export function shouldDeferManualVerification(level)
export function shouldAutoChainSessions(level)
```

#### 8.3 Safety rails (all levels)

Never automatic regardless of autonomy level:
- Git push / PR creation
- Destructive operations (delete branches, reset, force-push)
- Security-sensitive changes (detected via keyword matching)
- Budget exceeding (handoff generated, autonomous auto-resumes but respects budget)
- Quality gate configuration changes
- Autonomy level changes (human only)

#### 8.4 Session chaining (autonomous only)

At 95% budget in autonomous mode: auto-generate handoff, write auto-resume marker. Next session-start detects marker and auto-resumes.

#### 8.5 Milestone audit as primary review point

In guided/autonomous mode, `/tw:audit-milestone` becomes the main human review point. Shows: phase results, automated verification, items requiring review, aggregated manual checks, knowledge notes, entropy trends, feedback capture.

#### 8.6 Human feedback capture: `/tw:feedback`

```
/tw:feedback "Always use AppError instead of Error in service layer"
  --scope src/services/
  --rule grep_must_not_exist "new Error("
```

Creates spec proposal at confidence 0.6 (human-provided). Integrates into milestone audit flow.

#### 8.7 Gate strictness CLI

```
/tw:autonomy gate security non-blocking
/tw:autonomy gate tests blocking
```

#### Files to modify

| File | Action |
|------|--------|
| `lib/autonomy.js` | **Create** — autonomy level logic, thresholds, auto-approval |
| `hooks/subagent-stop.js` | Modify — autonomy-aware retries, skip-and-log |
| `hooks/session-start.js` | Modify — auto-resume from marker |
| `hooks/post-tool-use.js` | Modify — auto-handoff at 95% budget |
| `hooks/pre-tool-use.js` | Modify — auto-approve model switches per level |
| `lib/spec-engine.js` | Modify — auto-accept per autonomy threshold |
| `templates/commands/tw-autonomy.md` | **Create** — `/tw:autonomy` command |
| `templates/commands/tw-feedback.md` | **Create** — `/tw:feedback` command |
| `templates/commands/tw-discuss-phase.md` | Modify — auto-fill per autonomy |
| `templates/commands/tw-plan-phase.md` | Modify — auto-approve per autonomy |
| `templates/commands/tw-execute-phase.md` | Modify — autonomy-aware execution |
| `templates/commands/tw-verify-phase.md` | Modify — auto-proceed, defer manual |
| `templates/commands/tw-audit-milestone.md` | Modify — enhanced audit + feedback capture |

#### Tests

- `tests/unit/autonomy.test.js` — level thresholds, auto-approval, session chaining
- `tests/integration/autonomous-flow.test.js` — full phase without human interaction

---

## Binding Constraints (Cross-Cutting, Tier 1+2)

**Source**: Discussion Parts 4.5, 4.6

This is not a standalone upgrade but a cross-cutting enhancement that integrates Upgrades 1, 3, 4, and 5:

### Plan-checker generates `<constraints>` XML

After validating plans, the plan-checker extracts applicable spec rules per plan's file targets and generates a `<constraints>` block:

```xml
<constraints generated-by="plan-checker" timestamp="...">
  <constraint specId="SPEC:auth-001" rule="grep_must_exist">
    Files matching src/lib/auth*.ts MUST contain pattern "RS256"
  </constraint>
</constraints>
```

### Executor receives constraints

`hooks/pre-tool-use.js` injects `<constraints>` from plan XML alongside the routing map. Executors see both advisory context (specs) and non-negotiable rules (constraints).

### Verifier checks constraint compliance

`templates/agents/tw-verifier.md` adds two new verification dimensions:
1. **Spec Rule Compliance** — `evaluateRules()` results
2. **Review Criteria** — check each criterion from CONTEXT.md `## Review Criteria`

### Files to modify

| File | Action |
|------|--------|
| `templates/agents/tw-plan-checker.md` | Modify — enhanced Dimension 6, binding constraint generation |
| `templates/agents/tw-executor.md` | Modify — document constraint awareness |
| `templates/agents/tw-verifier.md` | Modify — spec rule compliance + review criteria dimensions |
| `hooks/pre-tool-use.js` | Modify — inject constraints from plan XML |

---

## Updated Agent Roster (10 Agents)

| Agent | Model | Role | New/Modified |
|-------|-------|------|-------------|
| tw-planner | Opus | Generates XML plans | — |
| tw-researcher | Opus | Domain research | — |
| tw-executor | Sonnet | Implements tasks | Modified (constraint awareness, Discovery Protocol) |
| tw-verifier | Sonnet | Goal-backward verification | Modified (spec compliance, review criteria, profiles) |
| tw-plan-checker | Sonnet | Validates plans (6 dimensions) | Modified (binding constraints) |
| **tw-reviewer** | **Sonnet** | **Semantic code review** | **New** |
| tw-debugger | Opus | Hypothesis-driven debugging | Modified (Discovery Protocol) |
| tw-dispatch | Haiku | Parallel work coordinator | — |
| tw-spec-writer | Haiku | Extracts patterns → proposals | — |
| tw-entropy-collector | Haiku | Post-wave drift scan | Modified (category 7: Spec Staleness) |

---

## Updated Quality Gate Sequence

```
Ralph Loop:
  typecheck → lint → tests → spec-compliance → doc-freshness → smoke-test*

Verify-phase:
  all Ralph Loop gates + endpoint-verification* + profile-verification + runtime-checks

* profile-type dependent
```

Default config additions to `quality-config.json`:

```json
{
  "spec-compliance": { "enabled": true, "blocking": true },
  "doc-freshness": { "enabled": true, "blocking": true },
  "smoke-test": { "enabled": true, "blocking": true },
  "endpoint-verification": { "enabled": true, "blocking": false },
  "profile-verification": { "enabled": true, "blocking": true },
  "runtime-checks": { "enabled": true, "blocking": false }
}
```

---

## New State Files

| File | Format | Written By |
|------|--------|-----------|
| `.threadwork/state/gap-report.json` | JSON | `lib/state.js` — gap entries from classifier, pre-execution scan, entropy |
| `.threadwork/state/knowledge-notes.json` | JSON | `lib/knowledge-notes.js` — agent-discovered knowledge |
| `.threadwork/state/spec-staleness-tracker.json` | JSON | `hooks/post-tool-use.js` — specs affected by code changes |
| `.threadwork/state/entropy-trends.json` | JSON | `lib/entropy-collector.js` — per-category trend data |
| `.threadwork/state/flaky-tests.json` | JSON | `lib/quality-gate.js` — flaky test tracker |

All operational — added to `.gitignore` block.

---

## New Virtual Tools

| Tool | Intercepted By | Purpose |
|------|---------------|---------|
| `knowledge_note` | `hooks/pre-tool-use.js` | Agents capture implementation discoveries |

Extends existing virtual tool pattern (`spec_fetch`, `store_fetch`).

---

## New Commands

| Command | Upgrade | Purpose |
|---------|---------|---------|
| `/tw:docs-health` | 4 | Dashboard: spec freshness, reference integrity, knowledge note health |
| `/tw:verify-manual` | 6 | Structured manual verification feedback |
| `/tw:readiness` | 7 | Harness readiness audit: coverage score, gaps, recommendations |
| `/tw:autonomy` | 8 | Set autonomy level, configure gate strictness |
| `/tw:feedback` | 8 | Capture human review feedback as spec proposals |
| `/tw:entropy scan` | 7 | On-demand entropy scan against any diff range |

---

## Migration Path: `threadwork update --to v0.3.2`

The update command performs the following idempotent steps:

1. Copy new/updated hooks to `.threadwork/hooks/`
2. Copy new/updated lib modules to `.threadwork/lib/`
3. Copy new agent templates to project's agent directory
4. Copy new command templates to project's command directory
5. Copy verification profile templates to `templates/verification-profiles/`
6. Copy enforcement spec templates to `.threadwork/specs/enforcement/`
7. Add new fields to `project.json` with defaults:
   - `review: { enabled: true, mode: "selective", triggers: {...} }`
   - `autonomyLevel: "supervised"`
   - `verification: null` (populated by discuss-phase Q10)
8. Add new gates to `quality-config.json` with defaults
9. Add new state files to `.gitignore` block
10. Create empty state files: `gap-report.json`, `knowledge-notes.json`, `entropy-trends.json`, `flaky-tests.json`

No existing state files, specs, journals, handoffs, or plans are altered.

---

## Complete Files Manifest

### New Files (22)

| File | Upgrade |
|------|---------|
| `lib/rule-evaluator.js` | 1 |
| `lib/doc-freshness.js` | 4 |
| `lib/knowledge-notes.js` | 4 |
| `lib/verification-profile.js` | 6 |
| `lib/autonomy.js` | 8 |
| `templates/agents/tw-reviewer.md` | 3 |
| `templates/commands/tw-docs-health.md` | 4 |
| `templates/commands/tw-verify-manual.md` | 6 |
| `templates/commands/tw-readiness.md` | 7 |
| `templates/commands/tw-autonomy.md` | 8 |
| `templates/commands/tw-feedback.md` | 8 |
| `templates/specs/enforcement/` | 1 |
| `templates/verification-profiles/` (7 files) | 6 |
| `tests/unit/rule-evaluator.test.js` | 1 |
| `tests/unit/quality-gate-compliance.test.js` | 1 |
| `tests/unit/quality-gate-classify.test.js` | 2 |
| `tests/unit/doc-freshness.test.js` | 4 |
| `tests/unit/knowledge-notes.test.js` | 4 |
| `tests/unit/verification-profile.test.js` | 6 |
| `tests/unit/quality-gate-smoke.test.js` | 6 |
| `tests/unit/spec-engine-gaps.test.js` | 7 |
| `tests/unit/state-gaps.test.js` | 7 |
| `tests/unit/entropy-trends.test.js` | 7 |
| `tests/unit/autonomy.test.js` | 8 |
| `tests/unit/reviewer-integration.test.js` | 3 |
| `tests/unit/handoff-gaps.test.js` | 2 |
| `tests/integration/spec-enforcement.test.js` | 1 |
| `tests/integration/agent-review.test.js` | 3 |
| `tests/integration/doc-freshness.test.js` | 4 |
| `tests/integration/knowledge-capture.test.js` | 4 |
| `tests/integration/runtime-verification.test.js` | 6 |
| `tests/integration/gap-detection.test.js` | 7 |
| `tests/integration/autonomous-flow.test.js` | 8 |

### Modified Files (26)

| File | Upgrades |
|------|----------|
| `lib/spec-engine.js` | 1, 2, 4, 7, 8 |
| `lib/quality-gate.js` | 1, 2, 3, 4, 6 |
| `lib/state.js` | 2, 7 |
| `lib/handoff.js` | 2, 4 |
| `lib/entropy-collector.js` | 7 |
| `hooks/subagent-stop.js` | 1, 2, 3, 4, 8 |
| `hooks/pre-tool-use.js` | 4, 8, binding |
| `hooks/post-tool-use.js` | 4, 7 |
| `hooks/session-start.js` | 4, 7, 8 |
| `templates/commands/tw-discuss-phase.md` | 5, 8 |
| `templates/commands/tw-plan-phase.md` | 8 |
| `templates/commands/tw-execute-phase.md` | 7, 8 |
| `templates/commands/tw-verify-phase.md` | 6, 8, binding |
| `templates/commands/tw-analyze-codebase.md` | 7 |
| `templates/commands/tw-entropy.md` | 7 |
| `templates/commands/tw-audit-milestone.md` | 8 |
| `templates/agents/tw-plan-checker.md` | binding |
| `templates/agents/tw-executor.md` | 4, binding |
| `templates/agents/tw-debugger.md` | 4 |
| `templates/agents/tw-verifier.md` | 6, binding |
| `templates/agents/tw-entropy-collector.md` | 4 |
| `install/update.js` | migration |
| `bin/threadwork.js` | migration |
Done. The blueprint is at blueprint_v0.3.2.md.

Here's the structure:

3 Priority Tiers, 8 Upgrades:

Tier	Upgrade	Problem(s) Solved	Key Deliverable
T1	1. Spec Rules Engine + Compliance Gate	#5	lib/rule-evaluator.js, 5 rule types, new quality gate
T1	2. Failure Classification + Fast-Track Proposals	#1	classifyFailure(), 4 failure types, gap reporting
T1	3. tw-reviewer Agent	#9	10th agent, semantic code review before quality gates
T2	4. Doc-Freshness + Knowledge Notes	#3, #7	lib/doc-freshness.js, lib/knowledge-notes.js, knowledge_note tool
T2	5. Enhanced Discuss-Phase (10 Qs)	#5, #2, #7	Auto-generate spec rules, verification profiles
T2	6. Runtime Verification + Profiles	#2	Smoke test, endpoint verification, 7 profile templates
T3	7. Gap Detection + Readiness Audit	#1, #6	/tw:readiness, entropy trends, pre-execution scan
T3	8. Autonomous Operation Mode	Future	3 levels, /tw:autonomy, /tw:feedback, session chaining
Plus binding constraints as a cross-cutting enhancement and a migration path via threadwork update --to v0.3.2.

Totals: 5 new lib modules, 1 new agent, 5 new commands, 7 verification profile templates, ~30 new test files, 26 modified files.

